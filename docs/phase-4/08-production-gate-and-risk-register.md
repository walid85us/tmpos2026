# 08 — Production Gate & Risk Register

**Scope:** the single authoritative register of gates that must clear before production go-live. Every gate has an owner (role), milestone, required evidence, a pass/fail rule, current state, and blocking severity. **Go-live is authorized separately by the owner after M9**, only when every blocking gate is `PASS`.

**Severity:** `BLOCKER` (go-live cannot proceed), `HIGH` (must be resolved or explicitly owner-waived), `MED` (resolve or accept with documented risk).
**Current state:** `OPEN` (not started/in progress), `PARTIAL`, `PASS`.

| Gate | Risk it closes | Owner | Milestone | Evidence required | Pass rule | Current | Severity |
|---|---|---|---|---|---|---|---|
| **G-HIST** | M0 historical exploitation can't be disproven (historical **evidence limitation**, not a waivable active vulnerability) | Security | M8 (investigation) → M9 (disposition) | **M8:** bounded (defined scope + timebox), **read-only** attempt to retrieve existing authorized historical evidence (Cloud Audit Logs / version history) **without enabling new retroactive collection**; classify if present. **If the investigation surfaces evidence of actual PII/CHD exposure, the statutory breach-notification obligation activates** (G-PRIVACY / G-INCIDENT). **M9:** if no reconstructing evidence exists, explicit **owner risk acknowledgement** documenting: exposure window existed; current state showed no privileged mismatch; Firestore `auditEvents` empty; historical non-exploitation unprovable; fail-closed rules active; compensating controls + future monitoring established | M8 evidence reviewed & no unremediated compromise (breach-notification triggered if exposure found), OR M9 owner risk-acknowledgement recorded with all six points | OPEN | HIGH |
| **G-EMU** | Firestore rules unproven semantically | QA | M2 | Emulator semantic suite green in a Java-enabled env | Suite passes; rerun in CI before any rules deploy | **PARTIAL — the semantic suite passes locally (41/41, ephemeral Nix JDK21 + `emulators:exec` on a `demo-` project, no creds) and in CI, which runs it on every push to `main` (`ci.yml` quality job, step "Firestore semantic emulator suite"; green on `8883473d`, run 34779503875); static guard retained (21/21). The pass rule's rerun before any rules deploy still applies; moving the gate to `PASS` is the owner's decision** | BLOCKER |
| **G-4USER** | 4 noncanonical users hold client-only roles | Platform IAM | M5 | Migration executed per [04](./04-canonical-iam-and-four-user-migration.md); post-migration authz verification | All four canonically mapped & verified; none unmapped in prod | OPEN | BLOCKER |
| **G-CPLOGIN** | No separate Backend CP login/session | Platform | M4 | Admin login exchange + HttpOnly/Secure/SameSite cookie + CSRF + MFA + step-up; real-socket test | Separate admin session boundary live; tenant session cannot reach admin | **OPEN — M4 slices 1–2 are delivered in the runtime only (committed in `dd89ff9`):** separate administrative and tenant/store session boundaries with host-distinct origin allowlists (enforced at composition), HttpOnly/Secure/SameSite=Strict `__Host-` cookies, session-bound CSRF, login client and account limits, admin MFA and recent authentication (a provider-verified second factor and an authentication at most 5 minutes old), admission revalidation (admin 1 min, tenant 5 min) with per-principal revocation, bounded port calls, and a real-socket suite (`server/runtime/sessions.test.ts`) in which neither boundary's session reaches the other's routes. Nothing is live: the provider-aware composition root (`server/composition/productionSessions.ts`) refuses to compose — no durable session store and no admin admission or authorizer adapter exist (G-DBROLE) — and step-up for sensitive actions and the login/logout audit record are not delivered.

**M4-ADMIN-UI-P1 (committed in `dd89ff9`) adds the browser half:**
- The admin console signs in through the existing Firebase client SDK, using an in-memory instance.
- It completes the provider's MFA challenge.
- It exchanges the one ID token at `POST /admin/v1/session/login`.
- It keeps the session CSRF token in memory only.
- It renders the read-only shell only after `GET /admin/v1/session` confirms the session.
- In production it runs only on a configured administration origin and refuses everywhere else.

It is not live either: with no composed boundary, the console reports sign-in as unavailable.

**M4-ADMIN-UI-P2 (committed in `dd89ff9`) adds:**
- A read-only Command Center read model (`server/runtime/commandCenter.ts`): `GET /admin/v1/command-center` behind the admin session and `{ scope: 'platform', permission: 'view_command_center' }`, with no state-changing operation. It maps an injected reader port into a bounded view (posture counts, per-area attention counts, governance signals, service health; each section available, unavailable or not configured, with `asOf` and `stale`), and no reader string reaches the output. The route is uncomposed — neither the composition root nor the deployable entry imports it — and no authoritative source exists (M7h), so its data is synthetic: test adapters and hermetic browser interception only. The console dashboard (`/admin`) refreshes manually only, and an unavailable section never shows a number, including a reading that ages past 24 hours while the page is open (counted from the server's generation instant).
- The admin second factor is TOTP only, in the console and the server login policy: phone needs reCAPTCHA hosts the admin CSP refuses ([03](./03-backend-control-plane-login-session-blueprint.md) §2a).

**Pre-operation security requirement.** This blocks production session activation, operational data access (the Command Center's included) and every sensitive admin action, and nothing in M4-ADMIN-UI-P2 bypasses it; it does not block the read-only shell.

The problem is that concurrent requests on one session can interleave admission revalidation with revocation:
- A request authenticated just before a concurrent denial can still run its handler.
- The store contract does not refuse a `validatedAt` or `lastSeenAt` update, or a login's session create, issued after a concurrent `revokePrincipal`.

What is required:
- The durable store must make those updates and creates conditional on the record still being live and on the principal's current security version.
- Sensitive handlers must re-verify that version inside their own write transaction.

**Deployment invariants (M4-ADMIN-UI-P2).** Before any admin host serves the console:
- The Firebase project must not enforce reCAPTCHA (Enterprise) for email/password sign-in while the admin CSP excludes reCAPTCHA; otherwise every admin password sign-in fails closed as "unavailable".
- HSTS `includeSubDomains` requires every subdomain of the admin host — and of the runtime host, where `server/runtime/server.ts` adds HSTS under the production classification — to serve HTTPS.
- The Firebase web API key the console uses must not rely on an HTTP-referrer restriction: the Auth SDK sends its identity-toolkit requests with `referrerPolicy: 'no-referrer'` (and the admin document's `Referrer-Policy` is `no-referrer`), so a referrer-restricted key refuses every admin sign-in.
- The admin host must serve the console through `createAdminWebListener`, with `/admin/v1/*` routed to the runtime — never a static host with SPA fallback. No admin-host deployment exists yet. | BLOCKER |
| **G-CLIENTAUTH** | Browser role/`sessionStorage` establishes authority | Platform | M5 | Server derives authorization; client role/entitlement demoted to render-only | No server decision depends on client-supplied role/entitlement/status | OPEN | BLOCKER |
| **G-IAM** | Canonical IAM not enforced server-side | Platform IAM | M5 | Server authorization service resolves identity→membership→role→perm→entitlement→status | Deny-by-default; authz-matrix suite green | OPEN | BLOCKER |
| **G-IAMUNIFY** | Two conflicting permission orderings (manage↔approve) | Platform IAM | M5 | Unified catalog + comparator; re-derivation of affected tenant grants | One ordering; no effective grant silently changed; tests prove parity | OPEN | HIGH |
| **G-ENTITLE** | Entitlements/permissions are forgeable `sessionStorage` | Platform | M5/M7f | `tenant_feature_entitlement` server-materialized; permissions server-owned | No entitlement/permission authority in client storage | OPEN | HIGH |
| **G-STATUS** | Suspended/read-only/overdue not enforced | Platform IAM | M5 | Status-driven deny tests; write-block on read_only/overdue | Fail-closed on non-active status server-side | OPEN | HIGH |
| **G-PERSIST** | Business writes lost on reload (no durable store) | Domain teams | M7a–M7e | Durable tables + repository/integration tests per domain | Every migrated domain persists across reload; tests green | OPEN | BLOCKER |
| **G-PCI** | Integrated payment capability with no card data touching TM POS2026 | Product/Security | M7b | **DECIDED (direction):** integrated card-present via PCI-certified **semi-integrated terminal/provider** + hosted/tokenized CNP + cash + external-terminal fallback; **TM POS2026 never receives/stores/logs/exposes** raw PAN/CVV/track/PIN/unencrypted CHD or processor creds in browser storage. Provider selected in M7b via documented criteria; auth/capture/settlement/void/refund/dispute/reconciliation; verified webhooks; idempotency + duplicate-charge prevention; durable audit; credential encryption/rotation; provider-outage behavior; terminal/device assignment scoped to tenant/store. **PCI validation category confirmed with provider/acquirer/QSA before production — no SAQ category claimed until integration architecture establishes eligibility.** As a multi-tenant orchestrator holding provider credentials, terminal-device assignments, and payment tokens, the platform's **own PCI service-provider scope (TPSP / Payment-Facilitator classification, provider responsibility matrix, per-tenant AoC obligations)** is validated with the QSA/acquirer alongside the merchant category — not assumed away. **Acceptance also requires: STORE-owned gateway configuration (Store Settings → Payments, not System Owner CP) + explicit canonical store-scoped permission ([04 §2.1](./04-canonical-iam-and-four-user-migration.md)) + credential isolation (OAuth/hosted-preferred, envelope-encrypted, store-scoped reference, no browser secret) + multi-store isolation + verified webhooks + payment lifecycle/reconciliation + validated PCI scope.** | Provider selected + integration architecture built + **store-owned config + canonical store permission + credential/multi-store isolation** + merchant **and** service-provider PCI classification validated + evidence (webhook/idempotency/reconciliation/audit/redaction) PASS | OPEN | BLOCKER |
| **G-PRIVACY** | Privacy & DSAR foundation for multi-tenant PII | Legal/Platform | M1 (contracts) → M5 (authz) → M6 (audit/workflow/retention) → M7a–h (domain export/correct/delete) → M8 (ops) → M9 (evidence + legal sign-off) | **DECIDED (direction): implement, not exclude.** Data inventory/classification; tenant-controller/platform-processor split; identity verification; tenant approval + platform escalation; cross-domain discovery; export/correction/deletion-anonymization (where legally permitted); retention/legal-hold/restriction/portability; consent/notice linkage; provider propagation; immutable audit; deadline/status tracking; failure/partial-completion handling; backup & deletion limitations; documented jurisdiction/legal-review boundaries. **No universal-compliance claim** — final policy/jurisdiction requires owner/legal review. | DSAR export/correction/deletion flows implemented & tested across PII domains + immutable audit + legal-owner sign-off (M9) | OPEN | HIGH |
| **G-ISOLATION** | No tenant/store isolation (`tenant-1` hardcoded) | Platform | M5/M7 | Cross-tenant + cross-store denial tests per domain | Tenant A cannot access B; store role cannot escape store | OPEN | BLOCKER |
| **G-INVENTORY-CONSISTENCY** | POS (M7b) may claim stock decrement before durable inventory (M7c) exists | Domain (POS/Inventory) | M7b→M7c (blocking dependency) | **No production sale may claim inventory was decremented unless a durable, transactional, idempotent inventory mutation OR obligation (outbox) exists and reconciliation passes.** The obligation is written **in the same transaction as sale completion**, with a **unique sale/idempotency key**, **durable obligation states** (pending/applied/failed), **replay-safe** M7c reconciliation, and **alerting + a production block on unresolved obligations**. Pre-M7c inventory-tracked sale completion is feature-disabled / non-inventory mode and **no decrement is exposed to the user until M7c reconciliation passes** | Inventory-tracked production POS blocked until M7c consumes the obligation and reconciliation passes; unresolved-obligation alert clean; no silent divergence | OPEN | BLOCKER |
| **G-APICOMPLETE** | UI actions have no backend | API | M3/M7 | UI→API→DB parity map; every user action hits an authorized API | No production UI action relies on mock/in-memory state | OPEN | BLOCKER |
| **G-DBCONSTRAINTS** | Missing FKs/uniqueness/indexes on business data | DBA | M7 | Schema with tenant/store keys, FKs, natural-key uniqueness, indexes | Constraints present & tested per domain | OPEN | HIGH |
| **G-DBROLE** | App connects as owner, bypasses RLS | DBA | M3/M5 | Scoped app role + RLS policies; migration/owner role separated. **Database-owner provisioning belongs to this gate, not to the migration:** the tenant runtime database must have its database-level `TEMPORARY` and `CREATE` grants to `PUBLIC` closed by the **database owner**, because PostgreSQL grants `TEMPORARY` to `PUBLIC` by default, `PUBLIC` reaches every role regardless of that role's own attributes, and a `REVOKE` issued by a non-owner migration principal is a silent no-op that emits only a warning. **M3 S2 evidence:** migration 005 creates the NOLOGIN privilege roles `tmpos_app` / `tmpos_audit_writer`, marks each with an exact migration-ownership comment, and **fails closed** (`insufficient_privilege`) unless the effective database `TEMPORARY` **and** `CREATE` privilege is false for both, checked with `has_database_privilege` so every grant path (direct, inherited, `PUBLIC`) is seen at once — proved against disposable PostgreSQL in both directions, including the RED case where the owner step has not run. Its rollback refuses to remove a role that does not carry that marker, and restores no database-level privilege. **Caller cutover must repeat the same effective-privilege check against the real persistent LOGIN role, and must go further than the migration can:** a direct or inherited grant to that role is invisible to a check that inspects only the privilege roles (proved by mutation, both paths), and on PostgreSQL 16 a membership granted `WITH INHERIT FALSE, SET TRUE` makes `has_database_privilege` report **false** for the member while `SET ROLE` still reaches the privilege (proved live). The NOLOGIN privilege roles hold no membership at all, so migration 005's own gate is sound; a LOGIN principal that can `SET ROLE` is not covered by a single `has_database_privilege` reading and must additionally be shown to hold no SET-able membership. | Tenant paths run under RLS as a non-owner role; **and** the runtime LOGIN role holds no effective database-level `TEMPORARY` or `CREATE` — through a direct grant, a role membership, or `PUBLIC` | **OPEN — M3 S2 delivered the migration-side foundation ONLY: no caller converted, no persistent LOGIN role provisioned, no owner-authorized apply to a managed or non-production database, and no runtime evidence. Owner-side database-ACL provisioning remains a later G-DBROLE operator action; only disposable PostgreSQL has been contacted.** | HIGH |
| **G-MIGRATE** | Migrations never auto-applied; drift risk | Infra | M3 | Migration runner + applied/rolled-back tests in CI | Runner applies/rolls back; no manual-only application | OPEN | HIGH |
| **G-AUDIT** | No durable audit; owner "audit log" forgeable | Security | **M3/M4 (minimal writer) → M6 (full)** | `audit_event` writer wired into every authz/mutation path; **minimal writer live before M5 provisioning/grant changes** | Durable append-only audit on all decisions/mutations; provisioning/IAM changes audited from M5 | OPEN | BLOCKER |
| **G-IDEMPOT** | Idempotency/rate-limit in-memory, process-local; money double-apply on retry | Platform | M6 | Durable idempotency store + distributed rate limits | Retried writes don't double-apply; limits survive restart/multi-instance | **OPEN — M6-RATE-P1 (committed in `dd89ff9`, accepted as a code-only foundation) delivers the rate-limit contract, and M6-IDEMPOT-P2 the idempotency contract below, both in code only; nothing is live. The client address groups IPv4 per address and IPv6 per /64; only IPv4-mapped IPv6 counts as IPv4, and NAT64 and 6to4 addresses are IPv6 clients (corrected in M6-IDEMPOT-P2):**
- **The port.** `DistributedRateLimiter` (`server/runtime/rateLimit.ts`), provider-independent:
  - A bucket is (namespace `runtime`/`tenant`/`admin`/`tenant-login`/`admin-login`, dimension `client`/`account` with `tenant`/`store` reserved, pseudonymous key, limit, window).
  - Its fixed window never extends. A consume is atomic, and one that is refused changes nothing.
  - Answers are explicit: allowed (with the remaining allowance), limited (with a bounded `retryAfterMs`) or unavailable, plus a readiness `probe`.
  - The runtime obeys only a plain record whose outcome and fields are in contract; other fields are ignored. Anything else, a throw, or an overrun of min(port deadline, 500 ms) is a bounded 503 (`rate_limit_unavailable`, `rate_limit_timeout`, `rate_limit_outcome_invalid`).
- **Keys.** Each key is HMAC-SHA256 under `RATE_LIMIT_KEY` over the namespace, dimension and subject: an IPv4 address, an IPv6 /64, or a verified principal's digest. No raw address, UID, token or cookie crosses the port.
- **Limits.** Each request is limited at 300 per 60 s per client per namespace. The login client limit is 10 per 60 s, spent before verification. The login account limit is 5 per 5 min, spent after verification and the evidence policy and before admission.
- **Conformance.** An executable suite (`assertRateLimiterContract`, `server/runtime/rateLimiter.testkit.ts`) covers atomicity across two instances, mixed costs, the fixed window, aborts, and outage with recovery. Positive controls show it fails a non-atomic, per-instance-locked, window-extending, spend-on-refusal, policy-blind, abort-ignoring, fail-open or state-losing adapter.
- **No per-process path.** No per-process limiter remains in the production graph, and no fallback exists. The in-memory limiter is test support outside the artifact.
- **Probes and readiness.** The probes never touch the limiter. Readiness reports its store, with one shared probe per second per instance.

**M6-IDEMPOT-P2 delivers the durable idempotency contract, in code only:**
- **Route policy.** Every route declares `idempotency` (`none` or `required`), and startup refuses a missing or unknown value. `required` registers only on a state-changing route with a verified principal and a declared platform-scope authorization; a tenant- or store-scoped route may not require it until M5 supplies server-derived tenant and store context to bind. Its operation is a `perform` handed only its IdempotentContext. Every current route is `none`, and `required` exists only on synthetic test routes.
- **The step.** It runs after the client limit, CSRF and origin, authentication, authorization and the bounded body read, and just before the operation. A missing, repeated or malformed `Idempotency-Key` (one UUID version 4) is a 400. Another request under the key is a 422 `idempotency_key_reused`, identical whatever differs. One still in progress is a 409 `request_in_progress`. A store timeout, outage or malformed answer, an operation overrun, or a completion the store cannot confirm is a 503; a malformed outcome is a 500.
- **Identity.** A dedicated `IDEMPOTENCY_KEY` keys domain-separated HMACs; the composition root refuses one equal to `RATE_LIMIT_KEY`, however padded. The record is bound to the client key and the principal, so each principal has its own key namespace; the request is bound to its method, route, session boundary, reserved tenant and store slots, and body.
- **Port and replay.** `DurableIdempotencyStore` (`server/runtime/idempotency.ts`) offers acquire, a lease-checked complete and a readiness probe, and every answer is strictly validated. A recorded response is sealed (AES-256-GCM, bound to its scope and fingerprint): 200/201 or the approved 409/422 rejections, JSON only, `Location` only, at most 16 KiB. Set-Cookie, Authorization, WWW-Authenticate, CSRF and tracing material are never recorded, and each replay carries its own request ID. Crashes, timeouts and malformed outcomes are never recorded.
- **Conformance.** `assertIdempotencyStoreContract` runs over two instances sharing synthetic state, with 22 positive controls. Static containment keeps the in-process store out of the production graph.

**Still OPEN:**
- No shared store is approved, and no adapter is composed. The composition root refuses with `rate_limit_store_unavailable` and, when `IDEMPOTENCY_KEY` is configured, `idempotency_store_unavailable`. Limits therefore survive neither restart nor multiple instances anywhere, and no production route may require idempotency.
- **The crash window.** The reservation, the business write and the completion are separate steps. An instance that dies, or an operation that commits and then fails, leaves an applied operation that a retry reclaims and runs again once its 60 s lease expires. The contract guarantees one holder of an unexpired lease and the replay of a completed operation, never exactly-once execution. Closing the window needs the business mutation, the lease-checked completion, the audit record and the transactional-outbox record committed in one database transaction; the outbox is the next M6 slice.
- A future limiter adapter must pass its conformance suite, honour the abort signal, keep no offline queue, time out below 500 ms and reclaim expired buckets. A future idempotency adapter must pass `assertIdempotencyStoreContract`, time out below 1 s and use the store's own clock.
- The deployment must hold one `RATE_LIMIT_KEY` and one `IDEMPOTENCY_KEY` on every instance, with configuration-skew detection to be specified in the adapter stage. Rotating `IDEMPOTENCY_KEY` or changing the contract version starts every record afresh, so either needs a drain of one retention period (24 h); a retry after the retention is a new operation.
- The deployment half of the proxy contract must present IPv4 clients as IPv4 (or IPv4-mapped) addresses. Under the /64 rule, an ingress that translates them into the NAT64 prefix would put every such client in one bucket (`64:ff9b::/96` lies inside one /64). | **BLOCKER** |
| **G-SSRF** | ~~Open SSRF (`label-proxy` fetches arbitrary URL)~~ — **M3 half DONE** | Security | **M3 (eliminate DEV sidecar) ✅ → M7e/M8 (egress boundary)** | Egress boundary; host allowlist; no request-controlled URL | M3 evidence: `label-proxy` + entire Shipping sidecar DELETED; `server/**` contains zero outbound `fetch(`; containment regression suite green. **Still OPEN** for the M7e/M8 provider egress boundary. | **PARTIAL** | BLOCKER |
| **G-UNAUTH** | Unauthenticated endpoints (~~Shipping API~~ and ~~identity routes~~ eliminated M3); protection re-implemented/omitted per route (folds GAP-25) | Security | **M3** (+M4/M5/M8) | Every endpoint authenticated + authorized; **shared enforced middleware (auth/CSRF/rate-limit/security-headers), not per-route**; unauth write removed | M3 evidence: all 22 unauthenticated Shipping routes eliminated; the unauthenticated identity routes (`POST /identity/resolve`, `GET /identity/by-uid`) DELETED, each now a bounded JSON 404 (`server/platform-identity/identityUnauthenticatedSurfaceElimination.test.ts`); **the M3 shared-middleware slice is delivered** — the M3 runtime admits requests only through ONE enforced chain: a closed central route table in which each route declares `public` or `authenticated` + its authorization requirement (startup fails closed otherwise, and an unauthenticated state-changing route cannot register), then a fixed order of rate limit → exact method/path admission → CSRF/exact-origin (unsafe methods) → authentication → authorization → handler, with a closed security-header policy on every application-layer response — proven over real sockets (`server/runtime/enforcement.test.ts`). **The M3 request-body/media-type slice is delivered:** every route declares `none` or bounded JSON (a source-defined cap of at most 1 MiB, required or optional); a body is judged from headers alone (400/413/415; an Expect on HTTP/1.1 → 417) before CSRF, authentication and authorization, read only after them under a cap on the bytes actually streamed, and parsed as strict UTF-8 JSON (`server/runtime/bodyPolicy.test.ts`). **The M4 login/session foundation is delivered in the runtime (committed in `dd89ff9`; `server/runtime/sessions.test.ts`):** two separate session boundaries — tenant/store `/api/v1/*` and administrative `/admin/v1/*` — each with its own `__Host-` cookie (Secure, HttpOnly, SameSite=Strict, Max-Age equal to the absolute lifetime), trusted origins (never shared between the two), audience, admission, authorizer, store and login limits, exchanged at `POST …/session/login`, `GET …/session` and `POST …/session/logout`; opaque 256-bit identifiers held server-side only as digests, rotated at login, revoked at logout, with absolute and idle expiry; session-bound CSRF (derived from the session, compared in fixed time) on every unsafe session request after the session authenticates and before authorization; a login client limit before verification and an account limit keyed only by the verified principal; and the authenticator and authorizer are handed frozen header-only and route views, never the request. The existing Firebase adapter is adapted to the runtime's authenticator port (`createRuntimeIdentityVerifier`, on its own Admin app over exactly the validated configuration) but is not composed into any deployable entry, and that adaptation fails closed without its configuration. The production entry still serves only the public operational routes: **the deployable artifact composes no authenticator, admission source or session store.** **M4 session-lifecycle hardening (committed in `dd89ff9`) adds:** every port call bounded (3 s by default) and handed a cancellation signal, an outage a bounded 503 distinct from a 401; admission revalidation with a security-version witness, a denial revoking every session of the principal in that boundary; idle refresh only by a successful authorized request; admin MFA and recent authentication; opaque provider UIDs, digested wherever a storage key is needed; host-distinct origins; and a provider-aware composition root outside the runtime that composes all or nothing and today refuses (`server/composition/productionSessions.test.ts`). **Still OPEN for:** production wiring — the composition root exists outside the runtime, but an owner-approved deployable build of it is still required; a durable, shared session store (the in-memory one is test support outside the artifact; a Postgres-backed store needs a session schema and the migration 005 runtime role); a production admin admission adapter (the existing canonical platform authorization and its `system_owner`/`manage` floor read through the owner database connection — G-DBROLE — and the pilot guard itself hard-denies in production); tenant/store admission and scope authorization (the tenant plane — migration 005 — and M5); a production admission source to revalidate against (the runtime re-checks admission every 1 min admin / 5 min tenant and revokes on a denial or a changed security version, but no production admission adapter exists; a provider-side disablement reaches a live session only through that admission); a durable audit record of logins and logouts (G-AUDIT — the live writer needs migration 005); the deployment half of the trusted-proxy contract — the contract and its real-socket suite are delivered in code (M6-RATE-P1, `server/runtime/clientAddress.ts`: the socket peer is the client unless it lies in an explicit trusted CIDR; only then is one `X-Forwarded-For` line walked right to left to the first untrusted hop; universal and near-universal ranges, overlaps, more than 32 ranges or two /8s of IPv4, zones, ports, hostnames and overlong chains are refused), but the production `TRUSTED_PROXY_CIDRS` (the composition root refuses it missing, invalid, or `none` under `NODE_ENV=production`), trusted ranges that hold proxy egress addresses only, proxies that append to or overwrite `X-Forwarded-For` and are the runtime's only way in, a trusted set covering every hop, and an end-to-end check through the real edge are deployment work — and HSTS at TLS termination; a composed distributed limiter (G-IDEMPOT, M6 — no per-process limiter remains in the runtime, so the composition root refuses without an approved one); step-up for sensitive actions and provisioning the admin hostname (G-CPLOGIN — admin MFA and recent authentication are enforced at login, and host distinctness at composition); the legacy DEV identity/BCP Express app, which is not behind the shared chain; and the pass rule — every deployed endpoint authenticated + authorized (M5/M8). The forward obligation stays tracked as `TMPOS-UNAUTH-IDENTITY-RESOLVE` in `m2-security-gate-manifest.json`. | **PARTIAL** | BLOCKER |
| **G-SECRETS** | ~~Live carrier keys in unauthenticated process memory~~ — **M3 half DONE** | Security | M3 ✅ / M6/M8 (encryption + rotation) | Encrypted secret store; **rotation policy + KMS/envelope-key management + compromise-response rotation**; no unauth credential endpoint | M3 evidence: `credential-store.ts` DELETED with its routes — no in-memory unauthenticated credential store remains and no credential is transmitted or stored anywhere. **Still OPEN** for encrypted-at-rest + rotatable durable secrets (M6/M8). | **PARTIAL** | BLOCKER |
| **G-PROVIDER** | Provider integration not production-secure | Security | M7e/M8 | Allowlisted egress + credential encryption **+ rotation** + outbox | Provider calls via egress boundary with stored, rotatable creds | OPEN | HIGH |
| **G-WEBHOOK** | Webhook ingest/replay unverified (unauthenticated state-injection) | Security | M8 (design M7e) | Provider signature verification on ingest + replay | No unverified webhook is processed | OPEN | **BLOCKER** |
| **G-HARDCRED** | Hardcoded PIN `1234` in **every** authorization path (`AccessContext.tsx:420`, `POS.tsx:617`) + mock operator PINs | Domain (POS) | M5/M7b | Server-side refund/operator authorization; **all** hardcoded PIN instances removed | No hardcoded credential anywhere in the authorization path | OPEN | **BLOCKER** |
| **G-APPSEC** | No SAST/DAST/dependency (SCA) scanning; Semgrep dropped. **Includes lockfile integrity:** an SCA audit reads `package-lock.json`, so if the lockfile is not the artifact that actually installs, the audit scans a fiction and this gate's own "scans green" pass rule is vacuous — the realised failure was exactly that, a green CI install that silently omitted `@types/react` | Security/QA | M2 | Dependency-audit + SAST on every PR; DAST against staging. **Mandatory subcriterion — lockfile portability:** every `resolved` URL an HTTPS `registry.npmjs.org` tarball on the default port with `integrity`, bound to its own name+version; no internal/private/loopback/link-local/`.local`/alternate-registry host; bundled entries validated through an integrity-pinned parent that declares them. The checker runs **before** `npm ci` in every installing job, mandatory (no `--if-present`, no `\|\| true`), Node built-ins only. Lockfile changes reproduced by a clean-cache public-registry `npm ci`. Per [07 §3a](./07-quality-and-test-strategy.md) | Scans green (no unremediated High/Critical); SAST/SCA block merge; **and** the pre-install portability checker green (internal/non-HTTPS/unapproved-host counts all 0) with an empty-cache `npm ci` installing the expected tree — an SCA result over an unportable lockfile does not count as evidence | **PARTIAL — SAST (Semgrep, digest-pinned, tokenless) + secret scan (Gitleaks) + SCA (npm audit) operational; lockfile portability recovered and verified locally — 688/688 internal URLs normalized to the public registry (each re-fetched and SHA-512-matched against the committed integrity before transformation), tree otherwise unchanged (0 version/integrity/dependency/flag drift), + 6 npm-completed bundled metadata entries; empty-cache `npm ci` exit 0 with `@types/react` present and the typecheck contract PASS; 52/52 portability gate tests; itemized expiring gate manifest (`m2-security-gate-manifest.json`) reconciles known findings and blocks new/changed Critical/High. DAST is M8. **Prod SCA residual at M2 (verified against the recovered lockfile): 0 Critical / 4 High transitive — `@grpc/grpc-js`, `path-to-regexp`, `picomatch`, `ws`; none was resolved by the lockfile recovery, and all four were remediated later by package overrides in `0af5744`.** The advisory set is byte-for-byte identical to the pre-recovery baseline (22 advisories before and after; 0 added, 0 removed), so the recovery neither introduced nor silently suppressed a finding. `npm audit signatures` now verifies 1002 registry signatures — only possible against the public registry. Prod SCA now (2026-09-13, P4-STAB-DEP-R1, `npm audit --omit=dev` of the committed lockfile): 0 Critical / 0 High — the High advisories that later reached `browserslist`, `fast-uri`, `nanoid`, `postcss` and `react-router` (and, by propagation, `ajv` and `react-router-dom`) were cleared by in-range lockfile updates plus the `fast-uri@3` override moving to 3.1.7, and the three manifest entries that recorded the postcss, react-router and react-router-dom Highs were removed. The full (dev) tree still holds 4 High packages: `brace-expansion`, `js-yaml` and `tar` under `firebase-tools`, and `undici` under `firebase-tools` and `jsdom`. The reconciler counts them but fails the full tree only on a Critical, so this gate's pass rule (no unremediated High) is not yet met. They are recorded non-production maintenance debt (M6-IDEMPOT-P2): each is reached only through development and test tooling, npm reports a fix for each that needs no `--force`, and none blocks a development stage unless new evidence shows production reachability, credential exposure, data-loss risk or a broken required gate. Until they are remediated or waived under the security-HIGH waiver rule, this gate cannot pass and go-live cannot proceed. CI runs the SAST, secret-scan and reconciled SCA gates on every push and pull request to `main` (`.github/workflows/ci.yml`).** | HIGH |
| **G-WEBHARDEN** | No web/transport hardening (CSP, security headers, HSTS, clickjacking, XSS-as-session) | Frontend/Security | M4 (admin) / M8 (both) | CSP + `frame-ancestors`/X-Frame-Options + HSTS + output-encoding/DOM-XSS controls on both SPAs | Headers/CSP enforced; admin CP clickjacking-protected; XSS cannot ride the session cookie | **PARTIAL — the admin half exists in code only (M4-ADMIN-UI-P2, committed in `dd89ff9`; nothing deploys it):**
- The admin web policy (`server/runtime/adminWeb.ts`). The document CSP is exactly `default-src 'none'; script-src 'self' https://apis.google.com; style-src 'self'; img-src 'self'; connect-src 'self' https://identitytoolkit.googleapis.com; frame-src https://<authDomain>; base-uri 'none'; form-action 'none'; frame-ancestors 'none'`: no `unsafe-inline`, `unsafe-eval` or wildcard; `apis.google.com` and `frame-src` exist only for the Google popup resolver; `<authDomain>` is accepted only as a bare hostname. With it: `X-Frame-Options: DENY`, `nosniff`, `Referrer-Policy: no-referrer`, a deny-list Permissions-Policy, CORP `same-origin`, `no-store` on the document and immutable caching on hashed assets; HSTS (`max-age=31536000; includeSubDomains`) only at the HTTPS boundary.
- A production-intended listener (`createAdminWebListener`, `server/runtime/adminWebServer.ts`): it serves a build preloaded at startup, with no per-request filesystem access, and hands an API path to the runtime or refuses it with the bounded JSON 404, never the document.
- `/admin/v1/*` and `/api/v1/*` are never answered with the SPA — by the listener, the Vite dev/preview guard or the console (one canonical rule, [03](./03-backend-control-plane-login-session-blueprint.md) §2a); the admin origin fetches no web font; no reCAPTCHA host is admitted (phone MFA is removed from the console); the credential inputs are uncontrolled; a headless-Chromium proof on the production build (DA-7, DA-8).

**Still OPEN for:** an admin-host deployment (none exists — `.replit` deploys the tenant surface as a static site; see the G-CPLOGIN deployment invariants); the tenant SPA's CSP, headers and Google Fonts (M8); a live-provider check of Google popup sign-in under this CSP (DA-8); and the pass rule on both SPAs. The dev/Preview server deliberately sends no framing header, because the Replit Preview embeds it in an iframe. | HIGH |
| **G-COVERAGE** | Critical surfaces unverified | QA | M2/M7 | Test pyramid per [07](./07-quality-and-test-strategy.md); coverage gates incl. **RLS negative (cross-tenant denial) tests** | No Critical/High gap closed without a passing test | **PARTIAL — M2 test framework/baseline established (Vitest/jsdom/Testing-Library render harness + deterministic runner + emulator suite); initial render coverage (AccessGuard) only, NOT whole-platform** | HIGH |
| **G-OBSERVE** | No observability/alerting | SRE | M8 | Metrics, redacted logs, traces, SLOs, alerts | Golden-signal dashboards + alerts live | OPEN | HIGH |
| **G-BACKUP** | No backup/restore | SRE/DBA | M8 | Scheduled backups + PITR + restore rehearsal | Restore rehearsal passes | OPEN | BLOCKER |
| **G-INCIDENT** | No incident response (payment/PII platform) | SRE/Security | M8 | Runbooks + on-call + break-glass governance | IR plan exercised (game day) | OPEN | HIGH |
| **G-PERF** | Performance unvalidated | Perf | M8 | Load/latency tests vs targets | Meets latency/throughput targets under load | OPEN | MED |
| **G-A11Y** | Accessibility unvalidated | Frontend | M8 | WCAG audit on both SPAs | No blocking a11y violations | OPEN | MED |
| **G-UAT** | No user acceptance | Product/Owner | M9 | UAT sign-off on end-to-end flows | Owner/operator acceptance recorded | OPEN | BLOCKER |
| **G-REHEARSAL** | No migration rehearsal | Infra/DBA | M9 | Full migration rehearsal on staging | Rehearsal completes with reconciliation | OPEN | BLOCKER |
| **G-GOLIVE** | Go-live not authorized | Owner | after M9 | All blocking gates PASS; go-live package | Explicit written owner authorization | OPEN | BLOCKER |

## Deferred Assurance Register (pre-launch; not product blockers)

Assurance-only items carried to the M8/M9 pre-launch review. None is a gate and none blocks product development; each is re-examined at go-live readiness.

| # | Item | Source | Disposition |
|---|---|---|---|
| DA-1 | The original C2B-M005 C01–C16 mutation definitions and raw V2 results are unavailable; their RED status rests on the V2 report, the unchanged migration child and the unchanged behavioural witnesses | C2B-M005 closure (2026-09-10) | Pre-launch review |
| DA-2 | Some historical npm activity cannot be reconstructed (npm rotates its debug logs) | C2B-M005 closure | Pre-launch review |
| DA-3 | One agent-tool installation path was outside ambient monitoring | C2B-M005 closure | Pre-launch review |
| DA-4 | The mutation fixture `tests/quality/fixtures/managed-m005-status-mutations.json` remains uncommitted | C2B-M005 closure | Pre-launch review |
| DA-5 | Temporary environment-snapshot hashes may remain in session scratch (V2-R5 manifests) | C2B-M005 closure | Pre-launch review |
| DA-6 | Secret scanning (Gitleaks) and the dependency audit were not run in the Phase 4.0 M3/M4 slices, which prohibited network access; offline Semgrep ran with one known pre-existing finding (`POS.tsx:626`) | Phase 4.0 M3/M4 slices | Run in CI / pre-launch |
| DA-7 | The admin-console browser evidence (the M4-ADMIN-UI-P2 header/CSP proof on the production build included) comes from headless Chromium driven over the DevTools pipe with every request synthesized (the Firebase Identity Toolkit included) and home-grown contrast/landmark checks. No real-provider sign-in, assistive-technology pass or third-party accessibility scanner was run. | M4-ADMIN-UI-P1, M4-ADMIN-UI-P2 | Pre-launch review (with G-A11Y) |
| DA-8 | Google popup sign-in and gapi loading under the admin CSP were not exercised against the live provider (no network was allowed). Carried with it: `script-src https://apis.google.com` trusts that host origin-wide; gapi may set an inline `style` attribute, which `style-src 'self'` would block; and narrowing to path sources, or a nonce/`strict-dynamic` policy, needs live evidence | M4-ADMIN-UI-P2 | Pre-launch review |
| DA-9 | The trusted-proxy contract and the limiter port are proven with synthetic data over IPv4 loopback only. This sandbox has no IPv6 stack, so IPv6 clients were exercised as forwarded entries from a trusted IPv4 peer; no real edge proxy sat in front of the runtime; and no shared store has run the conformance suite | M6-RATE-P1 | Pre-launch review, with the adapter stage's end-to-end edge check |
| DA-10 | Shutdown drains in-flight requests only within the 10 s force timeout. A request whose chain makes several slow port calls (up to 3.75 s each, M4) can outlast it and be cut; a limiter call adds at most 500 ms | M6-RATE-P1 review (a pre-existing M4 property) | Pre-launch review |
| DA-11 | The durable idempotency contract is proven with synthetic data only: an in-process store, two in-process adapter instances over one state, synthetic principals and keys, and loopback sockets. No durable store has run the conformance suite, rotating `IDEMPOTENCY_KEY` is designed no further than a drain of one retention period, and the 60 s lease against the operation's port deadline has not met a real business write | M6-IDEMPOT-P2 | Pre-launch review, with the store-adapter and transactional-outbox stages |

## Summary

- **Total gates:** 37. **BLOCKER:** 21 · **HIGH:** 14 · **MED:** 2. — unchanged since M1. **Lockfile portability is a mandatory subcriterion of G-APPSEC, not a new gate:** G-APPSEC already owns the dependency (SCA) audit, and that audit reads `package-lock.json`, so lock fidelity is a precondition of G-APPSEC's own pass rule rather than a separate risk. No existing gate had claimed lock integrity, clean installation, or supply-chain reproducibility, and none needed to be stretched to accept it. The pre-install checker is enforced regardless of this accounting; only the governance ownership is consolidated. Its non-waivability is a **rule**, not an assertion — see the non-waivable carve-out below.
- **Current state:** all `OPEN` at M1 (M1 is planning; it opens the register, it does not close gates). M2 moves G-EMU, G-APPSEC and G-COVERAGE to `PARTIAL`. **Phase 4.0 M3 (Shipping sidecar elimination) additionally moves G-SSRF and G-SECRETS to `PARTIAL`** — each had a distinct M3 half (eliminate the request-controlled fetch; remove the in-memory unauthenticated credential store) that is now done with regression evidence, while the remainder (M7e/M8 egress boundary; M6/M8 encrypted rotatable secrets) stays open. **Phase 4.0 M3 (shared enforced runtime middleware) moves G-UNAUTH to `PARTIAL`**: the Shipping and identity routes are eliminated and the M3 runtime admits requests only through one shared enforced chain. **The M3 body/media-type policy and the M4 login/session foundation (both committed in `dd89ff9`) keep G-UNAUTH `PARTIAL` and leave G-CPLOGIN `OPEN`:** the body policy is delivered, and the runtime now carries both session boundaries, session-bound CSRF, login limits and the Firebase adapter adapted to the authenticator's port shape (composed only by the provider-aware composition root, which refuses) — but the deployable artifact composes no authenticator, admission source or session store, so production wiring (a deployable build of the composition root, a durable session store, a production admin admission adapter — G-DBROLE — and tenant/store admission — migration 005 + M5), a trusted-proxy contract and HSTS, distributed rate limits (M6), step-up and admin-hostname provisioning, the legacy DEV identity/BCP app and the deployed-endpoint pass rule (M5/M8) stay open. **M4 session-lifecycle hardening (committed in `dd89ff9`) keeps G-UNAUTH `PARTIAL` and G-CPLOGIN `OPEN`:** the provider-aware composition root, bounded port calls, admission revalidation, per-principal revocation, admin MFA and recent authentication, and host-distinct origins are delivered in code, but production login is not active — the composition root refuses until a durable session store and the admission and authorizer adapters exist (G-DBROLE; migration 005 unexecuted; M5). **M4-ADMIN-UI-P1 (committed in `dd89ff9`) keeps G-CPLOGIN `OPEN` and G-UNAUTH `PARTIAL`:** the admin sign-in and read-only control-panel shell are delivered in the browser, but production login is not active and no endpoint was added; the concurrent admission-revalidation requirement (G-CPLOGIN row) now blocks every sensitive admin action and production session activation. **M4-ADMIN-UI-P2 (committed in `dd89ff9`) moves G-WEBHARDEN to `PARTIAL` (admin half):** the admin header policy, the listener and the routing boundary exist in code, but no admin-host deployment exists and the tenant SPA is M8. It keeps G-CPLOGIN `OPEN` and G-UNAUTH `PARTIAL` — it adds no unauthenticated route, and the read-only Command Center route is session-authenticated, authorized and composed nowhere — and leaves G-DBROLE and G-MIGRATE `OPEN`, with migration 005 unexecuted and not modified by this stage. **M6-RATE-P1 (committed in `dd89ff9`) keeps G-IDEMPOT `OPEN` and G-UNAUTH `PARTIAL`:** the trusted-proxy contract and the distributed limiter port, its conformance suite and the runtime integration exist in code, but no shared store is approved or composed, the deployment half of the proxy contract is not done, and durable idempotency is not started. M4's production blockers (the G-CPLOGIN admission race, G-DBROLE, migration 005) are separate and unchanged. **The gate total and severity mix are unchanged (37 / 21 BLOCKER / 14 HIGH / 2 MED) — no gate was added, removed, downgraded, or closed.** **No gate is `PASS`.** **Phase 4.0 M3 S2/S2.2 (database principal separation) leaves G-DBROLE `OPEN`, deliberately:** it added the NOLOGIN privilege roles, the tenant/store RLS policies, the least-privilege grant matrix, migration-owned role lifecycle, and a fail-closed effective-privilege gate — but it converted **no** caller, provisioned **no** LOGIN role, and applied to **no** managed database, so the gate's own pass rule (tenant paths actually running under RLS as a non-owner role) is untouched by it.
- **M0 carry-forward gates still open:** G-EMU, G-HIST, G-4USER.
- **M2's lockfile recovery is now proven on a CI runner.** Every push to `main` runs the lockfile-portability gate before a public-registry `npm ci`, then the typecheck contract and the typecheck ratchet (`ci.yml` quality job); all were green on `8883473d` (run 34779503875), the runner whose missing internal mirror caused the original failure. No M2 gate changes state here: moving G-EMU, G-APPSEC or G-COVERAGE is the owner's decision against each gate's own pass rule.
- **Go-live rule:** every `BLOCKER` must be `PASS` and every `HIGH` must be `PASS` or explicitly owner-waived (with documented risk) before G-GOLIVE can be requested.
- **Security-HIGH waiver rule:** waiving any **security** HIGH gate (e.g. G-APPSEC, G-WEBHARDEN, G-PROVIDER) requires, in addition to owner sign-off, (a) **Security sign-off**, (b) a **documented compensating control**, and (c) a **time-boxed remediation date**. This is why the previously-waivable security-integrity gates (webhook signature verification, the hardcoded PIN, the RLS-bypassing DB role, money-path idempotency) were **elevated to BLOCKER** rather than left owner-waivable.
- **Non-waivable carve-out — G-APPSEC's lockfile-portability subcriterion.** A G-APPSEC waiver may cover SAST/DAST/SCA **findings** (e.g. an open transitive High). It may **never** waive the pre-install lockfile-portability checker, which is **non-waivable and blocks merge independently of G-APPSEC's disposition**. Without this carve-out a subcriterion silently inherits its parent's waivability, and a waiver granted over an unrelated advisory would dissolve supply-chain lock integrity along with it — which nobody granting such a waiver intends. The carve-out is required because the usual "it would fail anyway" reasoning is **false here**: an unportable lockfile does **not** reliably break the build — npm *skips* an unfetchable `optional`/`devOptional` package and `npm ci` still exits 0. That silent path is the realised failure this gate exists to prevent (a green install that omitted `@types/react`), so the control cannot rest on the build failing loudly. Per the precedent above, a security-integrity control that must not be waivable is treated as blocking regardless of its host gate's severity.
- **Legal-HIGH waiver rule:** **G-PRIVACY is a statutory gate** (GDPR Arts 15–22 DSAR, CCPA/CPRA) — its absence at go-live is a legal violation, not a discretionary risk. Waiving G-PRIVACY (or its core export/deletion sub-scope) requires, beyond owner sign-off, (a) **legal-owner sign-off**, (b) a **documented lawful basis**, and (c) a **time-boxed remediation date** — an owner alone may not waive it. The core DSAR-fulfilment sub-scope is treated as effectively blocking for any deployment processing EU/CA-resident PII.
- **Severity semantics:** a gate's blocking severity may **exceed** the defect severity of its source gap (defect severity ≠ go-live-blocking severity). Examples: GAP-18 (defect High) → G-EMU (BLOCKER); GAP-24 (defect Medium) → G-HIST (HIGH). This is intentional, not an error.
- **Operational residual — carrier-credential rotation (owner action, not a code gate).** The eliminated Shipping sidecar accepted carrier credentials over unauthenticated routes and held them in plaintext process memory, and the preview surface that reached it was network-reachable. **This repository holds no evidence that any real carrier credential was ever entered, and none is asserted.** The residual is strictly conditional: **if** a real carrier credential was entered into the provider-settings surface at any time while the legacy preview/sidecar was reachable, **then** before production the store owner must (a) rotate that credential at the carrier/aggregator, and (b) review that provider account's billing and activity for the exposure window. The condition is **not decidable from repository evidence** — absence of historical misuse cannot be proven from this codebase, because the sidecar persisted no credential audit trail and its process memory is not recoverable. Accordingly this is an **owner operational production gate**, discharged by owner attestation, not by any test, scan, or code artifact in this repository; no local gate can close it and none claims to. **No email address, provider account, key fragment, credential identifier, or secret is recorded here, and none may be added.** Related code-side state: G-SECRETS is `PARTIAL` (the in-memory store is gone; encrypted-at-rest and rotatable durable secrets remain M6/M8).
- **Owner decisions RESOLVED (no open M1 owner decision remains):** **G-PCI — integrated payment capability is IN SCOPE** (semi-integrated/tokenized/provider-hosted; no raw cardholder data in TM POS2026; provider selected in M7b; PCI category validated before production). **G-PRIVACY — privacy & DSAR foundation is IN SCOPE** (implement, not exclude; distributed M1/M5/M6/M7/M8/M9). Both directions are **DECIDED**; both remain **evidence-based production gates** until their evidence passes.
