Inventory & Supply Chain Improvements — Narrow QA Correction Pass 5

Read `replit.md` and `Ref.txt` first and treat them as source-of-truth constraints before making any changes.

Also re-read the Inventory & Supply Chain reference links already provided for this workstream before implementing these corrections. Use them as behavioral reference, but do not copy them blindly. Improve beyond them while preserving our current architecture, design baseline, RBAC model, store scoping, and completed workstreams.

This is NOT a redesign and NOT a new workstream.
This is a narrow correction pass based only on the remaining QA failures and partial failures from Inventory & Supply Chain Improvements.

You must implement exactly what is requested below. Do not give shallow approximations. Do not partially satisfy the intent. Do not replace popup/modal behavior with inline panels or sheets unless explicitly allowed. Do not leave fields technically present but operationally unusable.

Expert-role stack
- Principal SaaS Product Architect for multi-tenant repair/POS workflows
- Staff React 19 + TypeScript + Vite engineer
- Firebase / Firestore data-model and state-flow architect
- Auth / RBAC / lifecycle gating specialist
- Inventory and supply-chain workflow architect
- Device/browser capability integration engineer
- QA-driven refactor auditor
- Side auditor and systems thinker at all times

Operating posture
- Act as a side auditor and systems thinker throughout.
- Audit ripple effects before editing.
- Fix only the failed or partial QA items deeply and coherently.
- Preserve current design baseline exactly.
- Preserve auth, preview mode, RBAC, lifecycle gating, and platform/store hierarchy.
- Preserve tenant isolation and store scoping.
- Reuse existing modal/drawer/table/toast/permission/sub-permission patterns.
- Do not reopen completed workstreams except for narrow compatibility-safe fixes required by this pass.
- Do not make changes to folder `Z`.
- Do not make changes to file `Y`.

Remaining corrections to implement

A) Transfer full edit mode before received
Current issue:
- the transfer edit popup exists, but only Notes can be changed
- this is not acceptable

Required behavior:
- as long as a transfer is NOT yet Received/finalized, the transfer must support FULL edit mode
- full edit mode must allow editing:
  - From location
  - To location
  - notes
  - transfer line items
  - quantities
  - item removal
  - item addition where appropriate within the existing architecture
- this must be done inside a proper popup/modal/drawer-based edit experience
- do not leave From/To locked unless there is a clearly justified lifecycle rule
- if a transfer is already Sent or In Transit, define explicitly which fields remain editable and which become locked, but do not silently restrict editing without UX explanation
- if changing From/To impacts line/item validity, enforce that coherently with warnings/confirmation

Required UX behavior:
- Transfer # opens detail popup/modal
- detail popup for editable transfers must provide a clear Edit action
- Edit opens a true edit modal or edit state inside the popup that allows all intended fields
- Save persists all changed values correctly
- reopening the transfer must show the updated values

B) Completed inventory count close-out and re-adjust flow
Current issue:
- completed count closure improved, but user still needs the ability to re-adjust stock count if a mistake happened before final close-out

Required behavior:
- once a count reaches Completed but is still in practical review/closure scope, authorized users must have a controlled way to perform a re-adjustment/correction
- this should not mean reopening the whole count in a broken way
- add a clean close-out model such as:
  - Count Completed
  - Review / Adjustment Summary
  - Close Count
- before final close, allow a controlled re-adjust action for mistakes discovered in the count result
- after final close, the count should become read-only except for whatever audit/history visibility fits the current architecture

Required UX behavior:
- completed count detail modal should clearly show:
  - discrepancy lines
  - what adjustments were applied
  - whether the count is still closable or already finally closed
- provide a clear Re-Adjust / Correct action before final close if allowed
- provide a final Close Count action to finalize the count lifecycle

C) Trade-in detail, view, edit, and camera capture
1. Trade-in detail/view mode
Current issue:
- trade-in edit flow still needs improvement
- there should be a proper view option too

Required behavior:
- each trade-in record must support:
  - View Details
  - Edit
- both should use popup/modal/drawer patterns, not messy inline editing
- View should show all relevant trade-in details clearly
- Edit should open a proper editable popup/modal with all relevant fields

2. Camera capture behavior
Current issue:
- Camera action still behaves like file upload
- this is not acceptable

Required behavior:
- implement real camera-first behavior when the device/browser supports it
- the Camera action must explicitly attempt to invoke device camera capture flow
- do not let the Camera button simply behave like the normal upload button

Technical implementation expectations:
- mobile-capable browsers should use device camera capture intent where supported
- desktop should fall back gracefully when direct camera capture is unavailable
- if the browser/device does not support true direct capture:
  - show a clear fallback path
  - do not silently pretend camera capture happened
- keep Upload as a separate button/path from Camera
- keep preview behavior working cleanly after both camera capture and file upload

Required UX behavior:
- separate buttons:
  - Camera
  - Upload
- captured/uploaded ID must show a clear preview
- edit flow must preserve existing captured/uploaded image unless replaced or removed intentionally

D) Dashboard low-stock quick actions must use popup window and same inventory experience
Current issue:
- low-stock interaction is still not correct enough
- user wants a popup window, not an action sheet
- user also wants View Stock Item and Adjust Stock to use the same inventory detail experience as Inventory Management

Required behavior:
- clicking a low-stock item on Dashboard must open a proper popup window, not a sheet
- inside that popup, use the same inventory item detail experience as Inventory Management
- do not create a weaker or separate mini-view
- use the same detail UI structure already proven in Inventory Management

Required UX model:
Option A preferred:
- click low-stock item
- open popup window directly showing the same inventory detail window/content used in Inventory Management
- within that popup, if authorized, user can move to the same Edit/Adjust actions already available in Inventory
- if unauthorized, show the same read-only state used in Inventory

If your architecture requires a first-step popup with actions, then:
- it must still immediately open the same Inventory Management detail modal/window experience, not a custom simplified sheet
- do not stack broken overlays
- do not redirect away from Dashboard
- keep modal layering clean and stable

Permission-aware behavior:
- authorized users see the same allowed controls as in Inventory Management
- unauthorized users see the same read-only/blocked behavior as in Inventory Management
- the dashboard popup must not diverge from Inventory Management rules

E) Documentation
Update `replit.md` so it accurately reflects:
- transfer full edit mode before received
- completed count re-adjust and final close model
- trade-in View/Edit modal behavior
- camera-first capture implementation and Upload fallback
- dashboard low-stock popup using the same inventory detail experience as Inventory Management

Important implementation rules
- do not implement approximate behavior and call it done
- do not use inline-under-list editing for these corrections
- do not make Camera and Upload effectively identical
- do not create a separate weaker dashboard item detail UI than Inventory Management
- keep permissions and handler guards aligned with existing checkSubPermission patterns
- keep popup/modal behavior stable and non-overlapping

Required design and behavior rules
- Preserve the current premium glassmorphism design baseline exactly
- Maintain keyboard accessibility and clear blocked/save/error states
- Keep state paths unified
- Avoid duplicate parallel systems
- Ensure corrected flows work in preview/demo mode
- Maintain store scoping and tenant safety
- Keep type safety strong

Definition of done
This correction pass is done only if:
- transfers support full edit mode before received where appropriate
- completed counts support practical re-adjust-before-close behavior and final close-out
- trade-ins support proper View and Edit popup/modal flows
- Camera is a real camera-first action when supported, with proper fallback
- Upload remains separate from Camera
- dashboard low-stock click opens a popup window using the same inventory detail experience as Inventory Management
- permission behavior in dashboard matches inventory detail behavior exactly
- no regressions are introduced in inventory, supply chain, repairs, customers, invoices, POS, services, or warranty
- `replit.md` is updated to reflect all corrections

Required final response format
Return your completion report in exactly this structure:

1. Files Changed
2. Corrections Implemented
3. Inventory / Supply Chain Workflow Decisions
4. Permission / Access Enforcement Fixes
5. Camera Capture and Trade-In Detail Design
6. Dashboard Low-Stock Popup Design
7. Non-Regressions Confirmed
8. Known Deferred Items
9. Manual QA Retest Areas