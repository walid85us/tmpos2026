Milestone 4 is approved.

The Platform Settings Change Review / Impact / Audit UX is acceptable and properly scoped.

Confirmed acceptable:

1. Review-before-save is approved.

* “Review & Save (N)” is the correct pattern.
* The review modal showing old → new diff, risk, enforcement, impact, and truth label is accepted.
* Using the unfiltered change set is important and approved, so hidden search/filter changes cannot be missed.

2. High-risk confirmation is approved.

* Red warning banner + required acknowledgment checkbox is correct.
* Defensive re-check inside the save handler is required and accepted.

3. Reset-to-default behavior is approved.

* Reset should stage the default into the draft only.
* It should then flow through the same Review & Save path.
* One platform_setting_updated audit row per confirmed group save is acceptable.
* No separate reset audit row is needed in this phase.

4. Search/group navigation and unsaved state are approved.

* Search, group pills, dirty dots, unsaved badges, row highlight, discard group, and beforeunload guard are all acceptable.

5. Permission gating is approved.

* All mutation entrypoints self-gate on canEdit.
* Read-only behavior without edit_platform_settings is correct.

6. Scope discipline is approved.

* No runtime enforcement.
* No notifications.
* No SSO/SCIM.
* No provider calls.
* No backend config service.
* No server RBAC / PIM / PAM.
* Domains, Command Center, replit.md, and docs were not touched in this milestone.

Please proceed to Milestone 5 — Cross-surface integration + documentation + final non-regression verification.

For Milestone 5:

* Think step by step before implementing.
* Keep scope limited to read-only cross-surface signals, documentation, and final non-regression verification.
* Do not reopen Milestones 1–4 unless directly required for documentation consistency or a small integration issue.
* Confirm Command Center domain readiness/domain issue signals remain accurate after the Domains maturity work.
* Confirm any platform-settings risk signal is read-only, truthful, and not fake runtime enforcement.
* Confirm Audit & Security still shows domain/config/platform-setting audit rows where already emitted.
* Do not add real DNS lookup, SSL automation, provider integrations, notifications, SSO/SCIM, server RBAC, PIM/PAM, access reviews, or Phase 1.3+ behavior.
* Keep replit.md concise.
* Add detailed Phase 1.2 history to docs/platform-operations-security-history.md.
* Set the next phase after Phase 1.2 acceptance as Phase 1.3 — Platform Team Governance.
* Preserve all locked accepted areas:

  * Command Center Intelligence
  * Support Queue / SLA / Macro Maturity
  * Audit Investigation Center
  * Escalation operating model
  * Permission dependency auto-sync
  * Add-on Governance
  * Shipping
  * Store Permissions Matrix
  * Tenant provisioning
  * paid override invoice workflow
  * server PII logging rules

After Milestone 5, provide the final Phase 1.2 report in the required final report format before manual QA.
