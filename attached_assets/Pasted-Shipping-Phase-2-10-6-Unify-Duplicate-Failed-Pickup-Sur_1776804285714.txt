Shipping Phase 2.10.6 — Unify Duplicate Failed Pickup Surfaces Into One Canonical Recovery Surface

Project:
Multi-Tenant Repair / POS SaaS

Scope:
This is a strict isolated bug-fix pass.
Do not redesign the app.
Do not broaden scope.
Do not reopen unrelated shipping workstreams.
Fix only the duplicate failed-state surface problem in failed/unconfirmed pickup recovery.

Goal:
QA proves the recovery actions are still not usable because the same failed pickup state is being rendered in two separate panels at once.

Current runtime problem:
- a persisted orphan/partial_failed banner is shown
- a second pickupAttemptResult failure panel is also shown
- the editable pickup schedule form appears between them
- each surface shows its own recovery actions
- result: Edit, Retry, and Cancel are still confusing in practice

This pass must collapse that into one canonical failed-state recovery surface.

Role:
Act as:
- Principal SaaS application architect
- Senior React/TypeScript engineer
- Senior Node/Express carrier-integration engineer
- Enterprise shipping systems architect
- Root-cause debugging specialist
- QA gatekeeper who must ensure only one recovery surface is shown for one failed pickup state
- Systems thinker

Read first:
- Ref.txt
- replit.md

Preserve without breaking:
- USPS working behavior
- truthful FedEx success/failure behavior
- UPS unsupported-service early blocking
- current pickup forecast + fee-class model
- current exact pickup-rate selection flow
- current no-rates vs partial_failed truthfulness
- current label-lock and pickup-override behavior
- current two-stage address readiness model
- current shipment-cancel dependency rule
- current provider-truthfulness messaging

==================================================
CURRENT QA SOURCE OF TRUTH
==================================================

Observed runtime issue:
- the failed pickup state is rendered twice:
  1) upper orphan/partial_failed recovery surface
  2) lower attempt-result / buy-failed surface
- the schedule editor is rendered in between them
- operator sees duplicate recovery actions
- Edit / Retry / Cancel remain confusing because there is no single canonical recovery panel

This pass must fix that exact issue.

==================================================
NON-NEGOTIABLE REQUIRED FIXES
==================================================

1) ONE FAILED PICKUP STATE = ONE RECOVERY SURFACE
Required:
- when a pickup is in failed/unconfirmed/orphan recovery state, render exactly one canonical recovery surface
- do not render both:
  - persisted partial_failed banner
  - and transient attempt-result failure panel
for the same active failed pickup state

2) CANONICAL SURFACE MUST OWN THE SCHEDULE EDITOR
Required:
- the editable pickup date/window area must belong clearly to the same recovery surface
- the operator must visually understand:
  - this is the failure
  - this is where I edit
  - these are the actions for this same failure
- do not sandwich the schedule editor between two separate failure blocks

3) EDIT / RETRY / CANCEL MUST ALL BELONG TO THE SAME STATE
Required:
- the canonical recovery surface must be the only place that owns:
  - Edit Date / Window
  - Retry Booking
  - Cancel Orphan Pickup
- do not show a second recovery action set elsewhere for the same failed pickup

4) KEEP TRUTHFUL DETAILS, BUT DEDUPE THEM
Required:
- preserve the useful provider details:
  - provider reason
  - code chip
  - stage chip
  - http status
  - provider pickup id
- but show them only once in the canonical surface
- do not duplicate the same failure details in two places

5) ADD A TEMPORARY UNITY PROOF MARKER
Required:
- add a small visible marker such as:
  unified-recovery: 2.10.6
- render it only on the canonical failed recovery surface
- QA should be able to confirm there is now one unified recovery panel

6) DO NOT REOPEN OTHER ISSUES
Required:
- do not broaden into UPS DAP policy
- do not broaden into FedEx date-policy work
- do not alter truthful provider messaging already achieved
- do not redesign the shipping module broadly

==================================================
ACCEPTANCE CRITERIA
==================================================

Acceptance is literal:
- one failed pickup state renders as one recovery surface
- one schedule editor is clearly associated with that surface
- one action set exists for edit/retry/cancel
- provider truthfulness is preserved without duplication

==================================================
IMPLEMENTATION DISCIPLINE
==================================================

Follow this exact order:

1) Identify why both failure surfaces are rendering simultaneously
2) Choose one canonical failed recovery surface for runtime
3) Bind the schedule editor to that canonical surface
4) Remove/hide the duplicate surface and duplicate actions
5) Add the temporary unified-recovery proof marker
6) Re-test USPS/FedEx/UPS non-regression
7) Update documentation

==================================================
DOCUMENTATION REQUIREMENTS
==================================================

Update replit.md with:
- the root cause of duplicate failed-surface rendering
- the chosen canonical failed recovery surface model
- how the schedule editor is now attached to that single surface
- the temporary unified-recovery proof marker
- confirmation that USPS/FedEx/UPS truthfulness was preserved

==================================================
OUTPUT FORMAT REQUIRED
==================================================

Reply ONLY in this structure:

1. Duplicate-Surface Root-Cause Findings
2. Canonical Failed Recovery Surface Fix
3. Schedule Editor Unification Fix
4. Recovery Action Deduplication Fix
5. Temporary Proof Marker Added
6. Files Changed
7. Documentation Updated
8. Non-Regression Notes