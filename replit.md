# Overview

This project is a multi-tenant SaaS frontend platform designed to provide small to medium-sized businesses with a comprehensive suite of tools for Point-of-Sale (POS), employee and customer relationship management (CRM), inventory control, reporting, and marketing. Its primary purpose is to streamline business operations, offer actionable insights, and automate processes through a scalable, customizable, and feature-rich architecture. The platform aims to enhance efficiency and decision-making for businesses.

# User Preferences

I prefer detailed explanations.
I want iterative development.
Ask before making major changes.
Do not make changes to the folder `Z`.
Do not make changes to the file `Y`.

# Current Project State

-   **Current accepted checkpoint**: **Phase 1.4 M0–M6 — Backend & Persistence Readiness are COMPLETE / ACCEPTED / BACKED UP** at GitHub checkpoint `19e32f8d9f35478c40bd54208ac590491bac7604` (M0 at `bb8a735`; M1 `af3c6ec`; M2 `2b9d8b8`; M3 `ed86283`; M4 `ab8cbde`; M5 `cdd1c03`; M6 `19e32f8`). **Product Owner Input Collection (provider/auth gate run with product-owner inputs) is COMPLETE / ACCEPTED / BACKED UP** at GitHub checkpoint `ac94358c7e567560e53fea47730a10fa2783c02b`. **Phase 1.5 Planning Pass (read-only) is accepted; Phase 1.5 M0 — Implementation Kickoff Evidence Pack is COMPLETE / ACCEPTED / BACKED UP** at GitHub checkpoint `b9124b8d38093756a112652a976115f4ed7ac9c2`. **Phase 1.5 M1 — Thin Server/API + Platform Identity Foundation is IMPLEMENTED, pending review / manual QA (not committed/pushed/backed up):** first behavior-changing backend slice — an isolated identity API (own port, separate from the shipping sidecar) + a minimal `platform_identity` table mapping the Firebase UID to an app-owned `internal_user_id`, behind feature flag `ENABLE_SUPABASE_PLATFORM_IDENTITY` (default OFF). **Firebase Auth unchanged; Supabase Auth NOT implemented; no tenant business data; default app behavior unchanged; migration applied by the owner in the Replit shell.** **Gate result (direction only, NOT implemented):** **Supabase** ratified as the **final working/default production provider direction** (managed PostgreSQL, US region; tier/PITR/region at implementation) and **Supabase Auth** as the **future production auth direction** (migrate before production users; **Firebase Auth for testing only**, not permanent); **Neon/Railway = acceptable fallbacks**; **Hostinger-VPS PostgreSQL = not preferred** for production financial/POS/audit data (managed backups required); **Hostinger = static frontend/marketing/demo only**; **Replit = dev-only**; **Stripe + Square = retained payment options, no card data in app DB, integration deferred**. **No implementation started; the first slice + detailed backup plan + secret manager + residency confirmation remain pending.** **Phase 1.3 — Platform Team Governance remains COMPLETE / ACCEPTED / BACKED UP** (all milestones M0–M5; see the Current phase entry below). **Phase 1.4 (Backend & Persistence Readiness) is the active docs/architecture-only readiness phase; no runtime/implementation phase has started and no source behavior has changed.** **Phase 1.2 is finalized/accepted.** Phase 1.2F **Tenant Web Address** — the strategic replacement of the user-facing "Domains" module — is **accepted** (including its final minimal-table correction and Manage-drawer product-language cleanup), and the **Platform Settings Control Center is accepted as-is for now**.
-   **Phase 1.6 M14 — Dormant Server-Authz Shadow FEED Helper (DEV-Flag-Gated, Imported-by-Nothing, Invoked-by-Nothing, No Live Read) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-4d feed groundwork (M14 planning's Option C / Approach X), over the Phase 1.6 M13 base (`f6bcf8f`). Adds a **dormant** feed helper (`src/auth/serverAuthzShadowFeed.ts` + types) exporting `runServerAuthzShadowFeed({ signal? })` (and `isServerAuthzShadowFeedEnabled()`) that — **only if a future approved caller invokes it** — would acquire a Supabase token via the **M11 bridge** (`withSupabaseAccessToken`), SHADOW-call `/auth/session/resolve` (**`Authorization: Bearer` header + exactly-`{}` body**, content-type `application/json`, base from `VITE_IDENTITY_API_BASE`), extract **only** the response `authorization` object, pass it **transiently** to the **M13** helper (`compareServerAuthzShadow`), and return a **non-secret** result (`ok/status/phase/serverAuthzPresent/comparison/message`; `phase` ∈ disabled/token_bridge_disabled/no_session/no_token/cancelled/route_disabled/denied/unreachable/server_error/malformed/server_authz_unavailable/compared). `authorization: null` ⇒ `server_authz_unavailable` (legacy engine authoritative; **not deny, not fail-open, not enforceable**); non-null ⇒ `compared` (**comparable only**). **Approach X — M12 is deliberately NOT imported** (M12 discards `authorization`); the feed does its own authorization-extracting read mirroring M12's token/route discipline. Imports the **M11 token bridge + M13 comparison helper (+ M13/own types) only** (not M12, `@supabase/supabase-js`, React, Firebase, AccessContext/Login/AccessGuard/App/`main.tsx`/pilot, or `server/**`). The token is used **only** as the Bearer header (never body/URL/log/store/return); only `authorization` is read (never raw body, identity, scope/tenant/store, roles, status, userType); the result carries **no** token/JWT, raw body, raw authorization DTO, identity, `tenantId`/`storeId`, role/plan/permission-LEVEL values. New DEV-only **default-OFF** master flag `VITE_ENABLE_SERVER_AUTHZ_SHADOW_FEED`; enablement = DEV ∧ token-bridge-enabled ∧ `VITE_ENABLE_SESSION_RESOLVE_SHADOW` ∧ server-authz-shadow-enabled ∧ feed flag (no single flag implies feed behavior; M11/M13 flags via imported enable-checks, not duplicated literals). **In M14 the helper is invoked by nothing and `/auth/session/resolve` is never called** (no live call, no DB, no SQL, no audit write, no live diagnostic) — **dormant**, imported by nothing active → tree-shaken from production (proven). One new offline diagnostic (**89 checks**); controlled token-bridge `49b` allowlist (feed as 2nd importer) + new `49f–49h` (feed dormancy); controlled comparison `80/81` allowlist (feed as sole importer/caller) + new `80b` (feed dormancy); additive bundle-scan `2h` (feed identifiers + flag absent); **owner-authorized** controlled single-file allowlists for the feed in the route guards `diagnostics-frontend-auth-provider-inventory-check.ts` (2c) + `diagnostics-frontend-supabase-auth-readiness-check.ts` (3c) — feed references the explicit route literal, still FAILS for any other non-pilot reference — and for the flag (feed's `…_FEED` flag prefixes the M13 flag) in inventory (4e) + readiness (5a) + observer private-surface (42). **No AccessContext/M8-observer/M9-lock/M11-bridge/M12-shadow-client/M13-helper-logic/Login/AccessGuard/App-routing/`main.tsx`/`src/pilot/**`/M5–M7/backend/package/migration/seed/SQL/DB/Supabase-MCP/production change; `.replit` untouched (pre-existing out-of-scope `packages=["amp"]` modification + pre-existing out-of-scope `goose-*.tar.bz2` untracked artifact left as-is, neither staged nor committed); pilot `authorization: null` pin untouched.** QA (non-live): feed **89/89**, token-bridge **81/81**, comparison **95/95**, M12 shadow-client **92/92**, inventory (2c/4e) **21/21**, readiness (3c/5a) **20/20**, observer private-surface (42) **46/46**, adoption **22/22**, full M1–M13 regression green, offline `vite build` OK + bundle secret-scan **14/14** (feed + flag absent from `dist/**`), `tsc` 12 pre-existing errors / **0 new / 0 in any M14 file**. Next: M15 = owner-approved guarded live one-shot feed invocation under identity/audit guardrails (or deferred AccessContext-wiring / pilot-read options). See [`docs/phase-1.6-milestone-14-server-authz-shadow-feed-plan.md`](docs/phase-1.6-milestone-14-server-authz-shadow-feed-plan.md).
-   **Phase 1.6 M13 — Dormant Server-Authz Shadow COMPARISON Helper (DEV-Flag-Gated, Synthetic-Input-Only, Imported-by-Nothing, Invoked-by-Nothing, No Live Read) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-4c comparison groundwork (M13 planning's Option D), over the Phase 1.6 M12 base (`ce8c767`). Adds a **dormant**, pure/synchronous/no-throw helper (`src/auth/serverAuthzShadowComparison.ts` + types) exporting `compareServerAuthzShadow(input: ServerDerivedAuthorizationLike | null)` (and `isServerAuthzShadowEnabled()`) that — **only if a future approved caller invokes it** — structurally compares a **SYNTHETIC / caller-provided** server-derived authorization DTO against the FRONTEND permission vocabulary on **KEY-SPACES ONLY** (permissions/subPermissions/entitlements key NAMES + counts + safe mismatch key names), normalizing the entitlement alias `supply_chain`→`supply-chain`. `authorization: null` ⇒ phase `server_authz_unavailable` (legacy engine authoritative; **not deny, not fail-open, not enforceable**); non-null ⇒ phase `compared` (**comparable only, not authoritative, not enforceable**); default ⇒ phase `disabled`. Reads **synthetic input only** — **no `/auth/session/resolve`, no fetch/XHR/sendBeacon, no token, no M12 shadow client, no M11 token bridge, no live `authorization` read**; reads only object KEYS (never values/levels/decisions); invokes **no** AccessContext function (`hasPermission`/`checkSubPermission`/`canAccess`/`getPermissionLevel`/`isStoreActivated`/`resolveLandingRoute`). The non-secret result carries only phase/presence/parity booleans/per-key-space comparisons (safe key names + counts)/aggregate counts/phase-derived message — **no** token/JWT/provider token, identity (`internalUserId`/`authProvider`/`authProviderUid`/`email`/`displayName`/`identity`), `tenantId`/`storeId`, role/plan/permission-LEVEL values, or raw DTO. New DEV-only **default-OFF** flag `VITE_ENABLE_SERVER_AUTHZ_SHADOW` (separate flag; enablement = DEV && flag==='true'; no M11/M12/route-helper flag referenced; no other new flag). Imports the frontend permission vocabulary (`accessConfig` + `platformPermissionsConfig`) + own types **only** (not `@supabase/supabase-js`, React, Firebase, AccessContext/Login/AccessGuard/App/`main.tsx`/pilot, the server permission catalog, or any `server/**`). **In M13 the helper is invoked by nothing and nothing live is read** — **dormant**, imported by nothing active → tree-shaken from production (proven). One new offline diagnostic (**94 assertions**, labels 1–83e); additive bundle-scan `2g` (comparison identifiers + flag absent from `dist/**`); **owner-authorized** controlled single-file exception scoping `VITE_ENABLE_SERVER_AUTHZ_SHADOW` to exactly `src/auth/serverAuthzShadowComparison.ts` in `diagnostics-frontend-auth-provider-inventory-check.ts` (4e), `diagnostics-frontend-supabase-auth-readiness-check.ts` (5a), **and** `diagnostics-accesscontext-supabase-observer-private-surface-check.ts` (42) — still FAILS for any other `src/**` reference. **No AccessContext/M8-observer/M9-lock/M11-bridge/M12-shadow-client/Login/AccessGuard/App-routing/`main.tsx`/`src/pilot/**`/M5–M7/backend/package/migration/seed/SQL/DB/Supabase-MCP/production change; `.replit` untouched (a pre-existing out-of-scope `packages=["amp"]` modification + a pre-existing out-of-scope `goose-*.tar.bz2` untracked artifact were left as-is, neither staged nor committed); pilot `authorization: null` pin untouched.** QA (non-live): comparison-helper **94/94**, inventory (4e) **21/21**, readiness (5a) **20/20**, private-surface (42) **46/46**, adoption **22/22**, token-bridge **78/78** + shadow-client **92/92**, full M1–M12 regression green, offline `vite build` OK + bundle secret-scan **13/13** (comparison + flag absent from `dist/**`), `tsc` 12 pre-existing errors / **0 new / 0 in any M13 file**. Next: M14 = live read-only shadow under separate owner approval + identity/audit guardrails (or deferred level/decision/AccessContext-wiring options). See [`docs/phase-1.6-milestone-13-server-authz-shadow-comparison-plan.md`](docs/phase-1.6-milestone-13-server-authz-shadow-comparison-plan.md).
-   **Phase 1.6 M12 — Dormant Session-Resolve Shadow Route-Call Helper (DEV-Flag-Gated, Imported-by-Nothing, Invoked-by-Nothing, No Live Call) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-4b route-call groundwork (M12 planning's Option C), over the Phase 1.6 M11 base (`a4b1151`). Adds a **dormant** shadow client (`src/auth/sessionResolveShadowClient.ts` + types) exporting `runSessionResolveShadowCheck({ signal? })` that — **only if a future approved caller invokes it** — would call the backend `/auth/session/resolve` via the M11 bridge (`withSupabaseAccessToken`) with **`Authorization: Bearer` header + exactly-`{}` body**, classify the response into a **non-secret** status record (`ok/status/phase/requestId/authState/decision/reasonCode/sourceOfTruth/message`; `phase` ∈ disabled/token_bridge_disabled/no_session/no_token/cancelled/route_disabled/denied/resolved/unreachable/server_error/malformed), and is cancellation-safe + no-throw. The token is used **only** as the Bearer header (never body/URL/log/store/return); the result carries **no** token, raw response body, server `authorization`, permissions, or any identity field (`internalUserId`/`authProvider`/`authProviderUid`/`email`/`displayName`/`identity`); messages are phase-derived. New DEV-only **default-OFF** flag `VITE_ENABLE_SESSION_RESOLVE_SHADOW` (separate flag; **also requires** the M11 token bridge enabled; no `VITE_ENABLE_SESSION_RESOLVE_ROUTE_HELPER` introduced; `VITE_ENABLE_SERVER_AUTHZ_SHADOW` reserved for M13, not referenced). Imports the M11 token bridge + own types **only** (not the M5 foundation directly, `@supabase/supabase-js`, React, Firebase, AccessContext, M6 bootstrap, M7 helper, pilot/`pilotEnv`/`sessionResolvePilotClient`, or server); base URL read locally from `VITE_IDENTITY_API_BASE` (`/__identity` default). **In M12 the helper is invoked by nothing and `/auth/session/resolve` is never called** (no live call, no DB, no SQL, no audit write, no live diagnostic) — **dormant**, imported by nothing active → tree-shaken from production (proven). One new offline diagnostic (**92 checks**); controlled token-bridge allowlist (49b allows the shadow client as sole importer + new 49c–49e shadow-dormancy); additive bundle-scan `2f` (shadow identifiers + flag absent); **owner-authorized** controlled single-file allowlist for the non-pilot `/auth/session/resolve` guard in `diagnostics-frontend-auth-provider-inventory-check.ts` (2c) **and** `diagnostics-frontend-supabase-auth-readiness-check.ts` (3c) — route literal kept explicit/auditable, still FAILS for any other non-pilot reference. **No AccessContext/M8-observer/M9-lock/M11-bridge/Login/AccessGuard/App-routing/`main.tsx`/`src/pilot/**`/M5–M7/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): shadow-client **92/92**, token-bridge (updated) **78/78**, inventory (2c) **21/21**, readiness (3c) **20/20**, foundation **37/37** + bootstrap **44/44** + awareness **55/55** + observer-wiring **45/45** + private-surface **46/46** + authority **36/36** + adoption **22/22**, full M1–M11 regression green, protected-action (DB-free) **8/8**, offline `vite build` OK + bundle secret-scan **12/12** (shadow + flag absent from `dist/**`), `tsc` 12 pre-existing errors / **0 new / 0 in any M12 file**. Next: M13 = private server-authz shadow comparison (structural, no enforcement); live route call still requires separate owner approval + identity/audit guardrails. See [`docs/phase-1.6-milestone-12-session-resolve-shadow-client-plan.md`](docs/phase-1.6-milestone-12-session-resolve-shadow-client-plan.md).
-   **Phase 1.6 M11 — Dormant Supabase Token Bridge Helper (DEV-Flag-Gated, Immediate-Use, No Route Call) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-4 token-safety groundwork (M10 planning's Option C), over the Phase 1.6 M9 base (`3ed357d`). Adds a **dormant** token bridge (`src/auth/supabaseTokenBridge.ts` + types) that yields a Supabase access token to an **immediate-use callback** only — `withSupabaseAccessToken<T>(use, { signal? })` — obtaining it via the M5 foundation's browser client `getSession()`. The raw token is **never** returned to a storable caller, stored in module scope / React state / ref / context / provider value, written to `localStorage`/`sessionStorage`/IndexedDB/`window`/`globalThis`, logged, serialized, or placed in a URL/query/body; there is **no** `getAccessToken()`/`getToken()` raw-token getter. The non-secret result carries only the callback's return + a status (`disabled`/`foundation_unavailable`/`no_session`/`no_token`/`cancelled`/`callback_error`/`success`); cancellation-safe, no-throw. New DEV-only **default-OFF** flag `VITE_ENABLE_SUPABASE_TOKEN_BRIDGE` (separate from foundation/bootstrap/awareness/diagnostic-surface/session-resolve-shadow/server-authz-shadow flags; **also requires** the foundation enabled). Imports the M5 foundation **only** (not `@supabase/supabase-js`, React, Firebase, AccessContext, M6 bootstrap, M7 helper, pilot, or server); **no `/auth/session/resolve` call, no network, no server-authz read, no AccessContext wiring**. **Dormant** — imported by nothing active → tree-shaken from production (proven). Owner-approved controlled allowlist update (mirroring M6 precedent) lets the foundation accept the token bridge as a 2nd importer in **both** `diagnostics-supabase-auth-foundation-dormant-check.ts` (8b) **and** `diagnostics-frontend-auth-provider-inventory-check.ts` (2g); one new offline diagnostic (**75 checks**); additive bundle-scan `2e` (token-bridge identifiers + flag absent). **No AccessContext/M8-observer/M9-lock/Login/AccessGuard/App-routing/`main.tsx`/`src/pilot/**`/M6-bootstrap/M7-helper/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`​.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): token-bridge **75/75**, foundation **37/37** + inventory **21/21** (updated allowlists), bootstrap **44/44** + awareness **55/55** + observer-wiring **45/45** + private-surface **46/46** + authority **36/36** + readiness **20/20** + adoption **22/22**, full M1–M9 regression green, protected-action (DB-free) **8/8**, offline `vite build` OK + bundle secret-scan **11/11** (token-bridge + flag absent from `dist/**`), `tsc` 12 pre-existing errors / **0 new / 0 in any M11 file**. Next: M12 = dormant route-call shadow helper (no live call without owner approval; route upserts identity + audits when enabled). See [`docs/phase-1.6-milestone-11-supabase-token-bridge-plan.md`](docs/phase-1.6-milestone-11-supabase-token-bridge-plan.md).
-   **Phase 1.6 M9 — AccessContext Supabase Observer Private-Surface Lock (Docs + Diagnostics Only) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-3d resolved as a **no-surfacing lock**, over the Phase 1.6 M8 base (`09c0be1`). **No `src/**` runtime change.** Adds one offline, text-only, self-inert diagnostic (`scripts/diagnostics-accesscontext-supabase-observer-private-surface-check.ts`, **46 checks**) that permanently LOCKS the M8 private observer: `supabaseAwarenessRef` stays a private, **write-only** (read-by-nothing) `useRef`, never in the provider value / `AccessContextType` / a `useAccess` getter, confined to AccessContext (the `AccessAwarenessRecord` type is referenced only by the helper, its types, and AccessContext's type-only import), and is **never surfaced** through a `window.__TM_POS_SUPABASE_AWARENESS__`/global hook, DOM `CustomEvent`/`dispatchEvent`, `console` log, `localStorage`/`sessionStorage`/IndexedDB, or `fetch`/`XHR`/`sendBeacon`; no `/auth/session/resolve`, no server-derived authorization, no `VITE_ENABLE_SERVER_AUTHZ_SHADOW`, no token/role/tenant/plan/permission surfacing; M7 helper still imported only by AccessContext via the DEV+awareness-flag-gated dynamic import. The reserved future flag `VITE_ENABLE_ACCESSCONTEXT_SUPABASE_DIAGNOSTIC_SURFACE` is **documented only** (absent from `src/**`). **Firebase remains the sole active/default/authoritative session source; AccessContext + M8 observer wiring byte-for-byte unchanged; no UI/window/event/console/persistence/network surfacing added.** Additive bundle-scan check `2d` (reserved flag + window-hook absent from `dist/**`; no existing check weakened). **No `src/**`/Login/AccessGuard/App-routing/`main.tsx`/`src/pilot/**`/M5-foundation/M6-bootstrap/M7-helper/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`​.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): private-surface **46/46**, M8 wiring **45/45**, authority **36/36**, observational **55/55**, adoption-readiness **22/22**, foundation **37/37** + bootstrap **44/44** + inventory **21/21** + readiness **20/20**, full M1–M8 regression green, protected-action (DB-free) **8/8**, offline `vite build` OK + bundle secret-scan **10/10**, `tsc` 12 pre-existing errors / **0 new / 0 in any M9 file**. Next: M10 should plan token→`/auth/session/resolve` / server-authz **shadow** (observational, default OFF). See [`docs/phase-1.6-milestone-9-observer-diagnostic-surfacing-plan.md`](docs/phase-1.6-milestone-9-observer-diagnostic-surfacing-plan.md).
-   **Phase 1.6 M8 — AccessContext One-Shot Supabase Observer Wiring (DEV-Flag-Gated, Private, Non-Authoritative) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-3c — the **first milestone to modify the live authority component**, over the Phase 1.6 M7 base (`8b7fd21`). Wires the proven M7 awareness helper into `src/context/AccessContext.tsx` as a **private, one-shot OBSERVER**: one **type-only** record import, one private `useRef`, and one **isolated** `useEffect` (separate from the unchanged Firebase listener) that — only when `import.meta.env.DEV` **and** `VITE_ENABLE_ACCESSCONTEXT_SUPABASE_AWARENESS === 'true'`, and only **after** Firebase `loading` is false — performs a **DYNAMIC** `import('../auth/supabaseAccessAwareness')` and calls `runAccessContextSupabaseAwarenessObservation({ signal })`, storing the non-secret record **only in the private ref**. The ref is **write-only** (read by nothing): never in the provider value / context type, never used by session/tenant/role/plan/permissions/subPermissions/`hasPermission`/`checkSubPermission`/`canAccess`/`getPermissionLevel`/`isStoreActivated`/`resolveLandingRoute`/AccessGuard/routing/loading/`authError`; never logged; no `/auth/session/resolve`, no server-derived authorization, no shadow flag, no token read. **StrictMode-safe** via per-run `AbortController` cleanup. The DEV guard makes the branch **dead-code-eliminated in production**, so the helper/bootstrap/foundation and the awareness flag are **tree-shaken out of the production bundle** (proven). **Firebase remains the sole active/default/authoritative session source; existing session/tenant/role/plan/permissions/AccessGuard/routing/Login/logout/loading behavior unchanged; provider value byte-for-byte unchanged.** Owner-approved controlled updates: the M7 observational diagnostic now allows the single dynamic AccessContext importer (no static import; DEV+flag-gated); the M6→M8 authority diagnostic is hardened with an observer-lock section; one new offline observer-wiring diagnostic (**45 checks**); bundle-scan `2c` label updated (assertion unchanged). **No Login/AccessGuard/App-routing/`main.tsx`/`src/pilot/**`/M5-foundation/M6-bootstrap/M7-helper/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`​.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): observer-wiring **45/45**, hardened authority **36/36**, updated observational **55/55**, adoption-readiness **22/22**, foundation **37/37** + bootstrap **44/44** + inventory **21/21** + readiness **20/20**, full M1–M7 regression green, protected-action (DB-free) **8/8**, **HARD GATE** offline `vite build` OK + bundle secret-scan **9/9** (foundation + bootstrap + helper + flag absent from `dist/**`, independently grep-verified 0 occurrences), `tsc` 12 pre-existing errors / **0 new / 0 in any M8 file**. See [`docs/phase-1.6-milestone-8-accesscontext-one-shot-supabase-observer-plan.md`](docs/phase-1.6-milestone-8-accesscontext-one-shot-supabase-observer-plan.md).
-   **Phase 1.6 M7 — AccessContext Supabase Awareness Helper (Dormant, DEV-Flag-Gated) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-3b groundwork over the Phase 1.6 M6 base (`1918b7e`). Adds a clean **dormant** AccessContext Supabase **awareness helper** (`src/auth/supabaseAccessAwareness.ts` + types) — the **first and only** permitted importer of the M6 bootstrap — that encapsulates a **one-shot, cancellation-safe, no-throw** observation lifecycle (`runAccessContextSupabaseAwarenessObservation({ signal? })`) for a FUTURE M8 AccessContext wiring. New DEV-only **default-OFF** flag `VITE_ENABLE_ACCESSCONTEXT_SUPABASE_AWARENESS` (separate from the foundation/bootstrap/pilot/shadow flags; **also requires** the bootstrap to be enabled). It imports the bootstrap **only** (not `@supabase/supabase-js`, React, Firebase, AccessContext, or the M5 foundation directly), has **no import-time side effects / no top-level await / no call site**, returns a **non-secret** record (status/enabled/cancelled/hasSession/redacted-email/source/message), and **never** exposes access/refresh tokens, role/tenant/plan/permissions/subPermissions, mutates session/tenant/role/plan/permission/loading/routing state, calls the backend session-resolve route, or reads server-derived authorization. **AccessContext is byte-for-byte unchanged; the M6 authority diagnostic stays unchanged and green; Firebase remains the sole authoritative session source; no UX change.** Safety is by **non-importation** — Login/AccessContext/AccessGuard/App-routing/`main.tsx` do not import it, so it (and the bootstrap/foundation it reaches) is **tree-shaken out of production** (verified). Owner-approved controlled update limited to M6 bootstrap dormancy `check 10b` (bootstrap may now be imported by exactly the one helper file; entrypoint/token/mutation/secret checks intact); one new offline diagnostic (53-check observational proof); additive bundle-scan helper-absence check. **No AccessContext/Login/AccessGuard/App-routing/M5-foundation/M6-bootstrap/`src/pilot/**`/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`​.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): awareness **53/53**, updated bootstrap dormancy **44/44**, unchanged authority **23/23**, foundation **37/37** + inventory **21/21** + readiness **20/20**, full M1–M6 regression green, offline `vite build` OK + bundle secret-scan **9/9** (foundation + bootstrap + helper absent), `tsc` 12 pre-existing errors / **0 new / 0 in any M7 file**. See [`docs/phase-1.6-milestone-7-accesscontext-supabase-awareness-plan.md`](docs/phase-1.6-milestone-7-accesscontext-supabase-awareness-plan.md).
-   **Phase 1.6 M6 — Dual-Provider Session Bootstrap (Dormant, DEV-Flag-Gated) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** Stage-3 groundwork over the Phase 1.6 M5 base (`2624403`). Adds a clean **dormant** Supabase **session-bootstrap** module (`src/auth/supabaseSessionBootstrap.ts` + types) — the **first and only** permitted importer of the M5 foundation — that, when explicitly invoked, reads a **non-secret observational** snapshot of Supabase session presence **through** the foundation. New DEV-only **default-OFF** flag `VITE_ENABLE_SUPABASE_SESSION_BOOTSTRAP` (separate from the foundation/pilot/shadow flags). It imports the foundation **only** (not `@supabase/supabase-js` directly — SDK stays confined to foundation + `src/pilot/**`), uses a **lazy async reader** (no client/session read at import, **no top-level await, no call site added**), and **never** exposes access/refresh tokens, mutates session/tenant/role/plan/permission state, calls the backend session-resolve route, reads server-derived authorization, or touches AccessContext. Safety is by **non-importation** — Login/AccessContext/AccessGuard/App-routing/`main.tsx` do not import it, so it (and the foundation it imports) is **tree-shaken out of production** (verified). **Firebase remains the sole active/default/authoritative session source; AccessContext untouched; no UX change.** Owner-approved controlled allowlist updates limited to M5 dormancy `check 8b` + inventory `check 2g` (foundation may now be imported by exactly the one bootstrap file; entrypoint + secret-safety checks intact); two new offline diagnostics (bootstrap-dormancy proof; AccessContext Firebase-authority lock); additive bundle-scan bootstrap-absence check. **No Login/AccessContext/AccessGuard/App-routing/M5-foundation/`src/pilot/**`/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`​.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): bootstrap-dormancy **44/44**, AccessContext-authority **23/23**, updated dormancy **37/37** + inventory **21/21**, readiness **20/20**, full M1–M5 regression green, offline `vite build` OK + bundle secret-scan **8/8** (foundation + bootstrap absent), `tsc` 12 pre-existing errors / **0 new / 0 in any M6 file**. See [`docs/phase-1.6-milestone-6-dual-provider-session-bootstrap-plan.md`](docs/phase-1.6-milestone-6-dual-provider-session-bootstrap-plan.md).
-   **Phase 1.6 M5 — Supabase Auth Frontend Foundation (Dormant, DEV-Flag-Gated) (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** first Phase 1.6 slice to add new `src/**` files, over the Phase 1.6 M4 base (`b621b39`). Adds a clean **app-level, dormant-by-default** Supabase browser-auth foundation (`src/auth/supabaseAuthFoundation.ts` + types) that adapts the proven `src/pilot/**` patterns **without importing or modifying the pilot**: public `VITE_` env only (`VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` + new DEV-only **default-OFF** flag `VITE_ENABLE_SUPABASE_AUTH_FOUNDATION`), a **lazy factory** (no client built at import, **no call site added**), no-throw disabled state, and **no service-role key / DB URL / Firebase / server / pilot / session-resolve / protected-API** reference. Safety is by **non-importation** — Login/AccessContext/AccessGuard/App-routing/`main.tsx` do not import it, so it is **tree-shaken out of the production bundle** (verified). **Firebase remains the sole active/default session authority; no UX change.** Controlled (owner-approved) update to the M4 inventory diagnostic `check 2a` (SDK allowlist = `src/pilot/**` **+** the one foundation file) plus compensating dormancy assertions; two new offline diagnostics (dormancy proof; emitted-bundle secret scan). **No Login/AccessContext/AccessGuard/App-routing/`src/pilot/**`/backend/package/migration/seed/SQL/DB/Supabase-MCP/production/`​.replit` change; pilot `authorization: null` pin untouched.** QA (non-live): dormancy **37/37**, inventory **21/21**, readiness **20/20**, full M1/M2/M3/M4 regression green, offline `vite build` OK + bundle secret-scan **7/7**, `tsc` 12 pre-existing errors / **0 new / 0 in any M5 file**. See [`docs/phase-1.6-milestone-5-supabase-auth-frontend-foundation-plan.md`](docs/phase-1.6-milestone-5-supabase-auth-frontend-foundation-plan.md).
-   **Phase 1.6 M4 — Supabase Auth Frontend Migration Readiness (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** docs + offline static-diagnostics slice over the Phase 1.6 M3 base (`cb9c9b7`). Codifies the migration path from the current **Firebase-derived** frontend session model toward a future **Supabase-token-compatible** model that can consume server-derived authorization, and **locks the frontend secret-safety boundary** with machine checks. Documents the provider mismatch (production app = Firebase `onAuthStateChanged` + Firestore role doc; server authz = Supabase bearer token), confirms the proven dev-only Supabase auth path already exists isolated under `src/pilot/**` (so **no new client and no package change** — `@supabase/supabase-js` already installed), and lays out the staged sequence (foundation → dual-provider bootstrap → session-resolve integration → AccessContext adapter/shadow `VITE_ENABLE_SERVER_AUTHZ_SHADOW` → enforcement → Firebase retirement) with binding `authorization: null` = fall-back-to-legacy-engine (never fail-open/deny/blank) semantics. Two new offline static diagnostics inventory Firebase/Supabase usage (pilot-scoped; main app imports no pilot client) and assert **no service-role key / DB URL reaches the frontend** (only public `VITE_` names read client-side; anon key only). **No `src/**`/AccessContext/Login/AccessGuard/App-routing/backend-route/migration/seed/SQL/DB/Supabase-MCP/package/production change.** QA (non-live): inventory **18/18**, readiness **20/20**, full M1/M2/M3 regression green, `tsc` 12 pre-existing errors / **0 in M4 files**. See [`docs/phase-1.6-milestone-4-supabase-auth-frontend-migration-plan.md`](docs/phase-1.6-milestone-4-supabase-auth-frontend-migration-plan.md).
-   **Phase 1.6 M3 — Route Contract Hardening & Frontend Adoption Planning (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** backend-only route-contract hardening + static-readiness + docs over the Phase 1.6 M2 base (`0878296`). Reconciles the stale `sessionResolveContract.ts` invariant that claimed `authorization` is "ALWAYS null" to the true M11.5 behavior — **`null` by default**, non-null **only** under the AND-ed DEV-only gates (`ENABLE_SUPABASE_PLATFORM_IDENTITY` + `ENABLE_SESSION_RESOLVE` + `ENABLE_LIVE_SESSION_AUTHORIZATION` + non-production + resolver `allow` + durable audit success) — via comments/types only (added the declarative `SESSION_RESOLVE_AUTHORIZATION_PRESENCE`; **no DTO/`authz.v1`/wire-field change, no route behavior change**). Adds two offline/static diagnostics: a route-contract stability check and an AccessContext adoption-readiness check proving **exact bidirectional key-space parity** (domains 21, tenant subs 74, platform features 11, platform subs 78; gate keys ⊆ planFeatures) so a future adapter mapping is total and the frontend needs no server import. Documents the Firebase-vs-Supabase provider mismatch, the deferred adapter/shadow strategy (`VITE_ENABLE_SERVER_AUTHZ_SHADOW`), and the binding `authorization: null` = fall-back-to-legacy-engine (never deny/fail-open) semantics. **No frontend/AccessContext/protected-API/control-plane/DB-console/migration/seed/SQL/DB/Supabase-MCP/production change; default `/auth/session/resolve` still returns `authorization: null` (proven statically, no live route call).** QA (non-live): contract **23/23**, adoption-readiness **22/22**, full M1/M2 regression green, `tsc` 12 pre-existing errors / **0 in M3 files**. See [`docs/phase-1.6-milestone-3-route-contract-frontend-adoption-plan.md`](docs/phase-1.6-milestone-3-route-contract-frontend-adoption-plan.md).
-   **Phase 1.6 M2 — Platform Role Vocabulary & Feature-Key Reconciliation (IMPLEMENTED, pending owner review / manual QA; not committed/pushed/backed up):** backend-only catalog/documentation/diagnostic reconciliation over the Phase 1.6 M1 base (`a4e3041`). Corrects the resolver's stale platform-role vocabulary header to the **post-003 canonical** vocabulary (`system_owner | support_admin | billing_admin | operations_admin | security_admin`; migration 003 already aligned DEV durable roles — production untouched), reframes `PLATFORM_ROLE_COMPAT_MAP` as a **legacy-compat fallback**, and confirms `billing_admin`/`security_admin` are first-class canonical while `platform_admin`/`platform_readonly` stay **fail-closed by design**. Formalizes the `supply_chain ↔ supply-chain` naming split into an explicit, deterministic, idempotent, many-to-one `normalizeFeatureKey` (cap-only; unknown keys fail closed). **No migration/seed/SQL/DB connection/Supabase MCP/production/frontend/AccessContext/protected-API/contract-shape change; `/auth/session/resolve` still returns `authorization: null`.** QA (non-live): catalog static **48/48**, materialization **43/43**, resolver **27/27**, new platform-role-vocab **21/21**, new feature-key **21/21**, contract **12/12**, `tsc` 12 pre-existing errors / **0 in M2 files**. See [`docs/phase-1.6-milestone-2-platform-role-feature-key-reconciliation.md`](docs/phase-1.6-milestone-2-platform-role-feature-key-reconciliation.md).
-   **Phase 1.5 M2 — Server-Side Request Context + Protected Diagnostic Enforcement (IMPLEMENTED, pending review / manual QA; not committed/pushed/backed up):** the first runtime realization of a *subset* of the accepted Phase 1.4 M3 contract — a thin, **dev-only** enforcement spine (request context → dev/test auth-adapter boundary → permission decision → protected-action wrapper → **advisory** audit envelope) proven against **one harmless diagnostic endpoint** (`POST /diagnostics/echo-decision`) inside the existing isolated identity API. **Default behavior unchanged:** gated by BOTH `ENABLE_SUPABASE_PLATFORM_IDENTITY=true` (default OFF) AND `PLATFORM_IDENTITY_DEV_DIAGNOSTICS=true` (default OFF), and never served when `NODE_ENV==='production'`. **No real Firebase token verification** (verifier stubbed → `firebase_verification_not_implemented`); permission snapshot is **dev-asserted** (`source: dev_asserted_snapshot`); audit is log-only (`evidenceLevel: dev_sidecar_log_advisory`, not compliance evidence). **No Supabase Auth; no Firebase replacement; no durable audit table; no RLS/schema/migration change; no tenant/business persistence; no `src/` / shipping-sidecar change.** Mirrors (does not fork or edit) the frozen `accessConfig.ts`/`platformPermissionsConfig.ts`/`checkSubPermission` semantics — see [`docs/phase-1.5-milestone-2-request-context-protected-enforcement.md`](docs/phase-1.5-milestone-2-request-context-protected-enforcement.md).
-   **Phase 1.5 M3-Revised — Supabase Auth Verified Actor Diagnostic (CORRECTED — fail-closed identity; pending review / manual QA; not committed/pushed/backed up):** first runtime slice performing **real server-side Supabase Auth token verification** — a dev-only `POST /diagnostics/supabase-whoami` that verifies a Supabase access token via **asymmetric JWKS** (`jose`; **no `SUPABASE_JWT_SECRET`, no service-role key, no Firebase Admin SDK**), derives the trusted actor from verified claims, and resolves/creates the app-owned `internal_user_id` (`auth_provider='supabase'`) via the M1 mapping. **FAIL-CLOSED identity (corrected):** a verified token is NOT a fully authenticated app actor until it resolves to a non-null `internal_user_id` — the endpoint returns `200`/`authState='authenticated'` **only** when verification AND identity resolution both succeed; any resolution failure (DB unavailable/unconfigured, upsert fails, or missing id) returns **`503 identity_resolution_error`** with `authState='unauthenticated'`, never `authenticated`, never an authenticated actor with a null `actorId` (no schema change). **Default behavior unchanged:** gated by BOTH `ENABLE_SUPABASE_PLATFORM_IDENTITY=true` (default OFF) AND the new `PLATFORM_IDENTITY_VERIFIED_DIAGNOSTICS=true` (default OFF), and never served when `NODE_ENV==='production'`. **Supersedes the chat-only Firebase-verifier M3** (no Firebase token verification added). **Frontend login unchanged; the M2 dev-asserted `/diagnostics/echo-decision` path is unchanged; no durable roles, no business APIs, no schema/RLS change, no shipping-sidecar change.** Audit is log-only/advisory (`sourceOfTruth: supabase_verified_token`, `evidenceLevel: dev_sidecar_log_advisory`, NOT compliance evidence). QA (Claude-run): M3 harness **23/23** (incl. fail-closed T8/T9/T10 + audit A1/A2 + request-context B1 `token-verified`) + M2 regression **8/8** + `tsc` clean for all M3 files (12 pre-existing unrelated errors only). **Semgrep PostToolUse hook temporarily disabled (no `SEMGREP_APP_TOKEN`); security review / Semgrep scan deferred to a later explicit checkpoint.** See [`docs/phase-1.5-milestone-3-revised-supabase-verified-actor-diagnostic.md`](docs/phase-1.5-milestone-3-revised-supabase-verified-actor-diagnostic.md).
-   **Phase 1.5 M4 — Supabase Auth Frontend Login Pilot (IMPLEMENTED, pending review / manual QA; not committed/pushed/backed up):** an **isolated, dev-only, feature-flagged** frontend pilot proving Supabase **email/password** sign-in/sign-out + a **safe** session view, with an optional manual call to the **unchanged** M3 `POST /diagnostics/supabase-whoami` using **only** `Authorization: Bearer <token>`. **Identity proof only — never authorization:** no client-asserted role/tenant/store/permission is sent or trusted; the raw access token is never rendered, logged, persisted, or filed. Lives only under `src/pilot/**` at route `/dev/supabase-pilot`, gated by BOTH `import.meta.env.DEV` AND `VITE_ENABLE_SUPABASE_PILOT==='true'` (default OFF), **outside** the `/` and `/owner` trees and **not** wrapped by `AccessGuard`; code-split into its own lazy chunk so production builds never activate it. Public client-safe vars only (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_ENABLE_SUPABASE_PILOT`, `VITE_IDENTITY_API_BASE`); **no service-role key / DB URL / JWT secret / token in the frontend** (bundle + source scans CLEAN). A **dev-only Vite proxy** `'/__identity' → http://localhost:5002` (prefix stripped) lets the pilot reach the diagnostic **without any backend CORS change** — `server/platform-identity/**` and `server/index.ts` are untouched. **Firebase login, AccessContext, AccessGuard, `Login.tsx`, Dev Session, Firestore provisioning, routing for normal users, and all business modules are unchanged.** Adds `@supabase/supabase-js` (frontend only); **no schema/RLS/migration, no Supabase MCP, no Hostinger deploy.** QA (Claude-run): M3 **23/23** + M2 **8/8** + `tsc` **12 baseline errors, 0 in M4 files** + pilot-disabled/enabled builds succeed + bundle/source secret scans CLEAN + whoami contract probe (401 `denied_unauthenticated` / 404 `FEATURE_DISABLED`) + Vite proxy round-trip verified. See [`docs/phase-1.5-milestone-4-supabase-auth-frontend-login-pilot.md`](docs/phase-1.5-milestone-4-supabase-auth-frontend-login-pilot.md).
-   **Phase 1 — Consolidated Architecture Direction Alignment (DOCS-ONLY; pending review):** Phase 1 architecture direction aligned — **Supabase Auth/Postgres = selected production direction**; **Firebase Auth = testing-only / to be retired**; **Hostinger = possible deployment/hosting layer only, NOT production DB**; **Replit = dev-only**; the **previous Firebase-verifier M3 plan is superseded** (chat-only, never implemented); **M3-Revised proceeds toward a Supabase Auth verified-actor diagnostic**. Early Phase 1 (foundational build, 1.1.x, 1.2/1.2F) was UI/client/advisory and needs no code revision or reopening; `platform_identity` is provider-agnostic (no schema change); the M2 spine is vendor-neutral. Full corrected milestone map + deployment-portability/env/CORS/QA-ownership rules — see [`docs/phase-1-architecture-direction-alignment.md`](docs/phase-1-architecture-direction-alignment.md).
-   **Phase 1.4 — Backend & Persistence Readiness (active, docs/architecture only)**: **M0** (Enforcement Boundary + Persistence Inventory, Firebase Coupling Inventory, Data Domain Persistence Map, DB Portability/Migration Map, Audit-Truthfulness Labels, Request-Context contract, Protected-Action catalog) is **complete / accepted / backed up** (`bb8a735`). **M1** (Auth/Repository Boundary Plan) is **complete / accepted / backed up**. **M2** (Durable Data-Shape & Domain Model Documentation — conceptual PostgreSQL-ready, tenant/store/platform-scoped, audit-aware domain map) is **complete / accepted / backed up**. **M3** (Request Context + Protected Action Contract + Audit Decision Boundary — a future provider-agnostic enforcement contract: request context, protected-action catalog, authorization-decision shape, audit truthfulness labels; no middleware/guards/authorization logic) is **complete / accepted / backed up** (`ed86283`). **M4** (Provider/Auth Decision Criteria Resolution — consolidated qualitative evaluation of Supabase / Neon / Railway / Hostinger-VPS PostgreSQL + Firebase-Auth-keep/replace + Supabase-Auth, against cost/region/RPO-RTO/payments-PCI/local-dev/schema-tooling/scale criteria; recommended working decision + ratified-vs-provisional split; no implementation) is **complete / accepted / backed up** (`ab8cbde`). **M5** (Provider Finalization Gate + Production Readiness Inputs — the decision gate that must be satisfied before any backend/provider/auth implementation begins: product-owner input checklist, per-dimension gates (backup/RPO-RTO, region/residency, cost, scale, payments/PCI, local-dev, schema-tooling, security/secrets, preview/demo, deployment), per-provider and per-auth go/no-go criteria, minimum evidence package, start conditions, blocked-work list, and a decision-intake template; finalizes nothing by assumption) is **complete / accepted / backed up** (`cdd1c03`). **M6** (Backend & Persistence Readiness Closeout Package — the authoritative Phase 1.4 handoff that consolidates M0–M5: per-milestone artifact summaries, consolidated ratified/provisional/blocked lists, current truthfulness statement, per-area readiness summaries, provider decision status, product-owner input gate, restated minimum-evidence package, recommended next step = Product Owner Input Collection, and a copy-ready future-session handoff; finalizes nothing) is **complete / accepted / backed up** (`19e32f8`). **Ratified working directions:** (1) **PostgreSQL** is the working production database direction; (2) a **server/API tier, managed API layer, or equivalent backend enforcement layer** is required for any future real server-side enforcement. **M4 working decision (not a final lock):** **Supabase** is confirmed the **PREFERRED default working provider candidate (design target)**; **Firebase Auth remains for testing, untouched.** **Still PROVISIONAL:** the **final** Postgres provider (**Neon / Railway / Hostinger-VPS PostgreSQL** = valid alternatives/fallbacks; VPS not preferred for financial data) and the **final auth provider** (**Supabase Auth** to be planned before production users if Supabase is finalized; **Firebase Auth** may remain viable if Neon/Railway/VPS is finalized with correct token verification — kept provider-agnostic via the M1 auth boundary) — pending product-owner inputs (budget band, region/residency, RPO/RTO, scale envelope) + a provider backup/restore plan. **M5** consolidated those into a **finalization gate**: final provider/auth stay **PROVISIONAL until the M5 gate is satisfied**, and **no backend/provider/auth implementation may begin before the gate inputs are answered** (product-owner input checklist, per-dimension + per-provider + per-auth go/no-go criteria, minimum evidence package, start conditions, blocked-work list, decision-intake template). **M6** is the **Phase 1.4 closeout package** (complete/accepted/backed up, `19e32f8`) and named **Product Owner Input Collection** as the recommended next step. **Product Owner Input Collection (complete/accepted/backed up, `ac94358`)** then ran the M5 gate against the owner's answers — see [`docs/phase-1.4-product-owner-input-collection.md`](docs/phase-1.4-product-owner-input-collection.md). **Phase 1.5 Planning Pass (read-only, accepted)** recommended the first behavior-changing slice; **Phase 1.5 M0 — Implementation Kickoff Evidence Pack (complete/accepted/backed up, `b9124b8`)** assembled the final pre-implementation evidence — see [`docs/phase-1.5-milestone-0-implementation-kickoff-evidence-pack.md`](docs/phase-1.5-milestone-0-implementation-kickoff-evidence-pack.md). **Phase 1.5 M1 — Thin Server/API + Platform Identity Foundation (IMPLEMENTED, pending review / manual QA; not committed/pushed/backed up)** added an isolated identity API + a minimal `platform_identity` table (Firebase UID → app-owned `internal_user_id`) behind feature flag `ENABLE_SUPABASE_PLATFORM_IDENTITY` (default OFF); Firebase Auth unchanged; Supabase Auth not implemented; no tenant business data; default behavior unchanged — see [`docs/phase-1.5-milestone-1-thin-api-platform-identity.md`](docs/phase-1.5-milestone-1-thin-api-platform-identity.md). **Gate result (direction only, NOT implemented):** **Supabase = GO → final working/default production provider direction** (managed PostgreSQL, US region; paid tier with PITR/region/add-ons selected at implementation); **Supabase Auth = GO → future production auth direction** (migrate before production users; **Firebase Auth for testing only, not permanent**); **Neon/Railway = acceptable fallbacks**; **Hostinger-VPS PostgreSQL = NO-GO/not preferred** for production financial/POS/audit data (managed backups required); **Hostinger = static frontend/marketing/demo only**, **Replit = dev-only**; **Stripe + Square = retained payment options, no card data in app DB, integration deferred**. **Accepted targets:** managed backups + PITR, RPO ≤15min, RTO ≤4h, 30-day operational + 7-year financial/audit retention; ~$200/mo budget (flexible); local dev = Supabase CLI; schema tooling = SQL-first/Supabase migrations (ORM deferred). **No implementation authorized:** first implementation slice, detailed backup/restore plan, concrete secret manager, and pre-go-live residency confirmation remain pending. **No runtime implementation has started; no source behavior has changed; behavior-changing server enforcement, Firestore rules, and database migration remain deferred.** Artifacts: [`docs/phase-1.4-product-owner-input-collection.md`](docs/phase-1.4-product-owner-input-collection.md), [`docs/phase-1.4-milestone-0-backend-persistence-readiness.md`](docs/phase-1.4-milestone-0-backend-persistence-readiness.md), [`docs/phase-1.4-decision-record-production-database.md`](docs/phase-1.4-decision-record-production-database.md), [`docs/phase-1.4-decision-record-deployment-topology.md`](docs/phase-1.4-decision-record-deployment-topology.md), [`docs/phase-1.4-milestone-1-auth-repository-boundary-plan.md`](docs/phase-1.4-milestone-1-auth-repository-boundary-plan.md), [`docs/phase-1.4-milestone-2-durable-data-shape-domain-model.md`](docs/phase-1.4-milestone-2-durable-data-shape-domain-model.md), [`docs/phase-1.4-domain-model-index.md`](docs/phase-1.4-domain-model-index.md), [`docs/phase-1.4-milestone-3-request-context-protected-action-contract.md`](docs/phase-1.4-milestone-3-request-context-protected-action-contract.md), [`docs/phase-1.4-milestone-4-provider-auth-decision-criteria.md`](docs/phase-1.4-milestone-4-provider-auth-decision-criteria.md), [`docs/phase-1.4-milestone-5-provider-finalization-gate.md`](docs/phase-1.4-milestone-5-provider-finalization-gate.md), [`docs/phase-1.4-milestone-6-backend-persistence-readiness-closeout.md`](docs/phase-1.4-milestone-6-backend-persistence-readiness-closeout.md).
-   **What "Tenant Web Address" is**: the System Owner module is repositioned away from a registrar/DNS "Domains" control panel toward honestly **managing each tenant's platform web address** (`{tenant.subdomain}.repairplatform.com`). It is presentation/positioning over a new pure helper (`src/owner/tenantWebAddress.ts`) and is **non-destructive over the M0 domain object model** — the `tenant_domains_v1` store and the dormant DNS/SSL/registrar helpers (`platformOpsDomainModel.ts`/`platformOpsDomains.ts`) are retained untouched for future custom-domain work. `WEB_ADDRESS_LIVE_HOSTING=false` (Copy always allowed; Open disabled/Future). No real DNS/SSL/registrar/hosting. No-drift: one `webAddresses` array + one `matchesWebAddressFilter` predicate drive all cards/saved-view counts. Permission resolver/ids/gating, audit, the Add flow, and `?domain=`/`?status=` deep-linking are unchanged.
-   **Superseded**: the earlier Phase 1.2F domain-control-panel milestones (M0 Domain Object Model, M1 Portfolio Dashboard, M2 Overview Workspace) and the **rejected Phase 1.2E control-panel direction** were superseded by this Strategic Replacement. The M0 model itself remains in place underneath.
-   **Platform Settings**: the governance work (registry + change-review UX) is **accepted as-is for now** — truthful (nothing enforced at runtime), correctly gated, and audited. Deeper implementation (backend config service, runtime enforcement, SSO/SCIM, notifications, provider integrations, approval-workflow engine, compliance-evidence automation, server-side policy enforcement) is **deferred to future platform operations/security phases**.
-   **Current phase**: Phase 1.3 — Platform Team Governance (server-side RBAC / PIM / PAM) is **COMPLETE / ACCEPTED / BACKED UP**. **All milestones are complete, accepted, and backed up:** Milestone 0 (Current Access Inventory), Milestone 1 (advisory governance model), Milestone 2 (Platform Team Directory + Role Matrix UI), Milestone 3 (Temporary Access / PIM Foundation), Milestone 4 (Access Review + Sensitive Action Reason Capture), and Milestone 5 (Command Center + Audit Integration + Documentation Closeout — a read-only **Platform Team Governance** signals section in the Command Center and an additive **Governance Activity (Advisory)** lens in the Audit Investigation Center, plus a correction pass that integrated all M3–M5 governance capabilities into the **Platform Global Permissions Matrix** via seven additive sub-permissions — `view_/manage_temporary_access`, `view_/manage_access_reviews`, `capture_sensitive_action_reasons` (team_management), `view_governance_signals` (command_center), `view_governance_audit_lens` (audit_security) — with dependency entries, so the System Owner controls who can view/manage governance per role; default least-privilege posture: only System Owner + Security Admin can manage, others view-only). The latest backed-up GitHub checkpoint is `7ad9ed2595ff10f008414190542fdb4cfcf60bbe`. **Current enforcement remains UI/client-gated and local/advisory where applicable** — the governance matrix permissions control who can view/manage the advisory governance surfaces, but no governance record changes a real role/permission, the permission resolver is unchanged, and System Owner stays fully allowed/protected. **Server-side enforcement, Firestore rules, SSO/SCIM, production PAM, scheduler-based revocation, and compliance-evidence automation remain future/deferred; these signals are not production compliance evidence.** **No new phase has started.** Authoritative artifacts: Milestone 0 inventory [`docs/phase-1.3-platform-access-inventory.md`](docs/phase-1.3-platform-access-inventory.md), Milestone 1 model [`docs/phase-1.3-platform-team-governance-model.md`](docs/phase-1.3-platform-team-governance-model.md), Milestone 2 UI [`docs/phase-1.3-milestone-2-team-directory-role-matrix-ui.md`](docs/phase-1.3-milestone-2-team-directory-role-matrix-ui.md), Milestone 3 foundation [`docs/phase-1.3-milestone-3-temporary-access-pim-foundation.md`](docs/phase-1.3-milestone-3-temporary-access-pim-foundation.md), Milestone 4 access review + reason capture [`docs/phase-1.3-milestone-4-access-review-sensitive-action-reason-capture.md`](docs/phase-1.3-milestone-4-access-review-sensitive-action-reason-capture.md), Milestone 5 command center + audit closeout [`docs/phase-1.3-milestone-5-command-center-audit-closeout.md`](docs/phase-1.3-milestone-5-command-center-audit-closeout.md).
-   **Detailed history**: full long-form implementation notes and the complete correction sequences (including every Tenant Web Address table/drawer correction) live in [`docs/platform-operations-security-history.md`](docs/platform-operations-security-history.md). `replit.md` keeps only the high-level overview, locked rules, and roadmap.
-   **GitHub backup**: after each accepted milestone/phase, run `npm run backup:github` before starting the next phase. It safe-pushes `main` to GitHub (fast-forward only; never force pushes; refuses on a dirty tree or diverged remote).

# System Architecture

## Frontend

The frontend is built using React 19, TypeScript, Vite 6, and Tailwind CSS v4.

## UI/UX Decisions

-   **Design System**: Features rounded cards, a glass effect, a consistent primary color theme, and a `ghost-border` utility. Typography uses `font-black uppercase tracking-widest` for labels.
-   **Animations**: Utilizes Framer Motion for smooth UI transitions and consistent modal patterns.
-   **Notifications**: Implements toast notifications for user feedback.
-   **Accessibility**: Prioritizes semantic accessibility for forms, tables, and modals.

## Technical Implementations & Feature Specifications

-   **Authentication & Access Control**: Firebase Authentication and a 7-level hierarchical permission model. Permission **enforcement is UI/client-gated** (no server-side or Firestore-rule enforcement today).
-   **Tenant Management**: Full lifecycle management including provisioning, subscriptions, feature overrides, billing, and audit logging.
-   **Business Management Modules**:
    -   **Point of Sale (POS)**: Cart management, diverse payment options, tax/discount calculations, customer management, repair intake, and robust refund/warranty workflows.
    -   **Employee Management**: Manages employees, roles, time tracking, and payroll with granular permissions.
    -   **CRM (Customers)**: Enhanced CRM with multi-field search, loyalty programs, duplicate detection, and integrated order/invoice history.
    -   **Invoices**: Supports full invoice lifecycle with dynamic line items, searchable catalog, payment processing, and configurable document templates.
    -   **Services**: Manages a service catalog with CRUD operations and warranty configuration.
    -   **Repairs Module**: Manages repair ticket lifecycle, parts tracking, technician assignment, and financial summaries.
    -   **Inventory Management**: Comprehensive system for managing various `StockItem` types, including UPC/manufacturer/location tracking, min/max stock levels, serial number tracking, stock adjustments, inventory transfers, and trade-in management, integrated with RBAC.
    -   **Supply Chain Management**: Manages suppliers, purchase orders, Goods Received Notes (GRN), and Return Merchandise Authorization (RMA) with tracking and financial recording, gated by RBAC.
    -   **Shipping Center**: Centralized fulfillment module for managing shipments across all source documents, supporting various types, status transitions, tracking, carrier configuration, cost tracking, and a provider adapter layer.
    -   **Returns Portal / Reverse Logistics**: Manages returns with a structured lifecycle, reasons, resolutions, item disposition, intake/inspection, and integration with return shipments.
-   **Reporting**: Features a dashboard with data visualizations.
-   **Onboarding**: A multi-step process for new tenants covering plan selection, pre-configured settings, and an activation checklist.
-   **Document Template Editor**: A structured editor for 5 document types, offering deterministic HTML generation, branding, and visual preview.

## System Design Choices

-   **Routing**: Utilizes React Router v7.
-   **State Management**: Primarily relies on React Context API and local component state.
-   **Firebase Integration**: Firebase **Authentication** for sign-in plus a **single `users/{uid}` Firestore read** to resolve the signed-in user's role/name. Firestore is **not** currently used for broad backend data persistence — there are no Firestore writes and no other collections read; application data is currently mock/in-memory/browser storage (see [`docs/phase-1.4-milestone-0-backend-persistence-readiness.md`](docs/phase-1.4-milestone-0-backend-persistence-readiness.md)).
-   **AI Integration**: `@google/genai` is present as **scaffold/unused** — there are currently no Gemini call sites in the codebase. Treat as a future capability, not an active integration.
-   **Server-Side Shipping API**: A **dev-only** Express server (`server/index.ts`, not part of the `static` production deploy) handles shipping provider operations. Provider credentials are held **in-memory (ephemeral, lost on restart), masked in responses, and redacted in logs** — this is not durable/production-grade secret storage. The server has no user/tenant/auth context.
-   **Commercial Controls**: Implements a system for managing add-ons and feature entitlements with governance, tenant overrides, pricing rules, and a detailed audit trail, including an internal invoice workflow and activation modes for feature grants.
-   **Add-on Governance**: Features three governance states (`active`, `disabled`, `archived`) and a separate PM `lifecycle`. Each add-on has a `readinessStatus` and runtime backing checklist to gate tenant grants and provide an implementation brief generator.
-   **Production Logging & PII Redaction**: Server-side logs are restricted to operational metadata, with sensitive data redacted using `server/safe-log.ts` and `sanitizeError(err)`.
-   **Platform Operations & Security**: A suite of System Owner surfaces (Command Center, Audit & Security, Support Tools, Platform Settings, Domains, Team Management) for governance and operational control, including a deterministic rule-based intelligence layer. Full implementation detail in [`docs/platform-operations-security-history.md`](docs/platform-operations-security-history.md).

# Locked Product Rules

These are accepted, no-regression rules. Do not change behavior without explicit instruction. Deep detail for each lives in [`docs/platform-operations-security-history.md`](docs/platform-operations-security-history.md).

## Platform Permissions

-   The **Global Permissions Matrix** (`platformPermissionsConfig.ts`) is the single **platform** source of truth for sidebar visibility, page access, and action gating.
-   The **Store Permissions Matrix** remains **tenant/store-only** and is separate from platform permissions.
-   Permission levels are fixed and rank-ordered: **None / View Only / Create / Edit / Approve / Manage / Full Access**.
-   **Parent/child behavior**: sidebar/page visibility uses effective access (parent level OR any child sub-permission >= view); section/action gating uses the exact child sub-permission.
-   **Explicit child None** denies that child even when the parent grants access.
-   **Parent View + child Full Access** allows the child's action (child grant is honored independently of parent default).
-   **Permission dependency auto-sync** (write-time, direction-aware, least-privilege preserving):
    -   Granting/raising a dependent action auto-raises each denied prerequisite to the minimum level that satisfies its own threshold (never beyond).
    -   Lowering a prerequisite auto-caps (writes explicit `none` on) any dependent that would otherwise resolve to `denied_prerequisite`.
    -   Lowering a dependent action never lowers its prerequisite.
    -   System Owner is locked and never reconciled.
-   Permission enforcement is **UI/client-gated** (local/advisory where applicable). Phase 1.3 delivered the local/advisory Platform Team Governance foundation (now controlled through the Global Permissions Matrix); **real server-side RBAC/PIM/PAM enforcement, Firestore rules, SSO/SCIM, production PAM, scheduler-based revocation, and compliance-evidence automation remain future/deferred**.

## Escalation Operating Model

-   **Assignment, escalation, acknowledgement, escalation resolution, and case closure are distinct lifecycle actions** — each gated and audited independently.
-   **Request De-escalation pending-review lifecycle**: front-line operators who cannot de-escalate but can add notes may submit a request; it persists a pending status, posts an internal note, and writes an audit row without mutating escalation state.
-   **No duplicate active request** per case (blocked at handler level).
-   **Reviewer approval/rejection**: approvers de-escalate through the standard confirmation modal (with confirm-time re-check); rejection requires a reason and never mutates escalation status.
-   **Auditability**: every escalation lifecycle transition and de-escalation request action emits an audit row.
-   **Active escalation badge consistency**: `isActiveEscalation` (wrapping `effectiveEscalationStatus(c).active`) is the single truth source for every escalation count, list, banner, and badge. A case shows exactly **one** active-escalation badge — never duplicated, never missing on an active case, never present on terminal (`deescalated` / `resolved`) cases.

## Command Center Intelligence

-   **Deterministic, rule-based intelligence only** — no AI/ML.
-   **No fake AI**, **no live infrastructure/uptime**, **no real DNS/SSL/SSO/SCIM**, **no external notifications**, **no Firestore realtime** claims. Operational signals refresh from current app/session state; live-backend listeners are future work.
-   **Drilldowns must show the records behind every count** — count and list are derived from one source and cannot drift.
-   **No invisible filters** — any active filter must show a visible chip, the matching items, or a truthful empty state.
-   **Source / confidence / truth labels are required** on every intelligence surface.

## Audit Investigation Center

-   **Deterministic, rule-based only** — no AI/ML, SIEM, realtime listeners, prediction, immutable/legal-grade/compliance-certified claims, external notification, or server-side RBAC/PIM/PAM. Derivations live in `platformOpsInvestigation.ts` (separate from the locked `platformOpsDerive.ts`) and reuse `deriveHighRiskFlag`.
-   **Audit rows are never mutated.** Review status (`needs_review`/`reviewed`/`dismissed`) and investigation notes are an append-only session **overlay** keyed by event id, separate from both audit rows and global `SecurityNote`s.
-   **No new permission keys** — review-status + note-add reuse `add_security_note`; note-delete reuses `delete_security_note`. `view_restricted_audit_details` still gates restricted detail (redacted with an explicit placeholder, never silently dropped).
-   **No drift / no invisible filters** — lens counts and the visible list derive from one predicate over one event set; every active filter and the active lens render as visible, clearable chips with truthful empty states.
-   **Date-granular truth** — audit rows have no sub-day timestamp, recorded actor role, or recorded source surface; correlation windows are whole-day, and `sourceSurface` is labeled derived.
-   **No audit spam** — only real review-status transitions emit an audit row; handlers read fresh persisted state before writing. Create-case-from-audit is duplicate-safe (fresh storage read + re-entrancy lock).
-   The **evidence summary is internal, copy-only**, and explicitly not legal/compliance-certified.

## Domains

-   **Deterministic, rule-based only** — no real DNS lookups, SSL issuance/automation, or provider/registrar integrations. DNS/SSL readiness is derived guidance with manual operator verification, clearly truth-labeled.
-   Domain derivations live in `platformOpsDomains.ts` (separate from `platformOpsDerive.ts`). The raw `status`/`ssl`/`kind` fields on `TenantDomainRecord` are the source of truth; `DomainLifecycleStatus` is a **derived presentation layer** and must not replace them.
-   **Single domain store** — Domains page, Command Center, Support Tools, and Dashboard all read `tenant_domains_v1`; counts/signals cannot diverge.
-   Lifecycle actions (create / status change / SSL state / disable / re-enable / delete) are **explicit, confirmed, and audited** (`domain_*` actions, category `domains`). Destructive transitions never go through a silent `<select>`. Page visibility uses `view_domains`; mutation uses `manage_domain_lifecycle`.

## Platform Settings

-   **Registry is the single source of truth** — `platformOpsSettings.ts` (`SETTINGS_REGISTRY`, one entry per `platformDefaults` field) drives every label, default, risk, enforcement, owner, impact, and truth label; posture cards, rows, review diffs, and the audit note all derive from it (no drift).
-   **Nothing is enforced at runtime** — the `enforced` enforcement value exists in the type for future use but is assigned to nothing today (branding/support `display_only`, maintenance `advisory`, security `documentation_only`). Standing truth labels say so.
-   **Review before save** — per-group "Review & Save" shows the old→new diff, impact, and risk for the **unfiltered** change set (search/filters can never hide a pending change). High-risk batches require an acknowledgment checkbox, re-checked inside the save handler. Reset-to-default stages the default into the draft and flows through the same review/save path (no separate reset audit row).
-   **Persistence + audit unchanged** — `platform_settings_v1` localStorage key; exactly one `platform_setting_updated` row (category `configuration`) per confirmed group save (severity `warning` if any high-risk, else `notice`). Page visibility uses `hasEffectiveFeatureAccess('platform_settings')`; editing uses `edit_platform_settings`. All mutation entrypoints self-gate on `canEdit`.

## Non-Regression Locked Areas

Do not change behavior in these areas as a side effect of other work:

-   Shipping Module
-   Add-on Governance
-   Store Permissions Matrix
-   Tenant Features
-   Tenant provisioning
-   Paid override invoice workflow
-   Server PII logging rules
-   Domains (lifecycle model + shared store)
-   Platform Settings (governance registry + change-review UX)

# Active Roadmap

-   **Phase 1.1.3C** — Support Queue / SLA / Macro Maturity **(accepted)**
-   **Phase 1.1.3D** — Audit Investigation Center **(accepted)**
-   **Phase 1.2** — Domains + Platform Settings Maturity **(finalized/accepted — Tenant Web Address is the accepted replacement for Domains; Platform Settings accepted as-is for now, deeper implementation deferred)**
-   **Phase 1.2E** — Domains Control Center UX Maturity **(direction REJECTED by user — superseded by Phase 1.2F)**
-   **Phase 1.2F** — Strategic Replacement ("Domains" → "Tenant Web Address") **(accepted, incl. final minimal-table correction + Manage-drawer product-language cleanup)** — built non-destructively over the earlier (now superseded) M0/M1/M2 domain-control-panel milestones; full detail in the history doc.
-   **Phase 1.3** — Platform Team Governance (server-side RBAC / PIM / PAM) **(COMPLETE / ACCEPTED / BACKED UP — M0–M5 all complete, accepted & backed up; M3–M5 governance features controlled through the Platform Global Permissions Matrix; latest GitHub checkpoint `7ad9ed2595ff10f008414190542fdb4cfcf60bbe`; current enforcement UI/client-gated and local/advisory; server-side enforcement deferred; no new phase started)**
-   **Phase 1.4** — **Backend & Persistence Readiness** (docs/architecture/pure-helper readiness only; **M0–M6 complete/accepted/backed up (`19e32f8`)**). **Product Owner Input Collection** (provider/auth gate run with product-owner inputs) is **complete / accepted / backed up** (`ac94358`). **Phase 1.5 Planning Pass (read-only) is accepted; Phase 1.5 M0 — Implementation Kickoff Evidence Pack is complete/accepted/backed up (`b9124b8`). Phase 1.5 M1 — Thin Server/API + Platform Identity Foundation is IMPLEMENTED, pending review / manual QA (not committed/pushed/backed up):** first behavior-changing backend slice — isolated identity API + minimal `platform_identity` table (Firebase UID → app-owned `internal_user_id`) behind feature flag `ENABLE_SUPABASE_PLATFORM_IDENTITY` (default OFF); Firebase Auth unchanged; Supabase Auth not implemented; no tenant business data; default behavior unchanged. Ratified working directions: **PostgreSQL** as the production database direction and a **server/API tier (managed API layer / serverless / VPS) required for future real enforcement**. **Gate result (direction only, NOT implemented):** **Supabase = final working/default production provider direction** (managed PostgreSQL, US region; tier/PITR/region at implementation); **Supabase Auth = future production auth direction** (migrate before production users; **Firebase Auth for testing only, not permanent**); **Neon/Railway = acceptable fallbacks**; **Hostinger-VPS PostgreSQL = not preferred** for production financial/POS/audit data (managed backups required); **Hostinger = static frontend/marketing/demo only; Replit = dev-only**; **Stripe + Square = retained payment options, no card data in app DB, integration deferred**. **No backend implementation authorized** — first implementation slice, detailed backup/restore plan, concrete secret manager, and pre-go-live residency confirmation remain pending; **behavior-changing server-side enforcement, Firestore rules, and database migration remain deferred** until a behavior-changing milestone is explicitly accepted. Artifacts: [`docs/phase-1.4-product-owner-input-collection.md`](docs/phase-1.4-product-owner-input-collection.md), [`docs/phase-1.4-milestone-0-backend-persistence-readiness.md`](docs/phase-1.4-milestone-0-backend-persistence-readiness.md), [`docs/phase-1.4-milestone-1-auth-repository-boundary-plan.md`](docs/phase-1.4-milestone-1-auth-repository-boundary-plan.md), [`docs/phase-1.4-milestone-2-durable-data-shape-domain-model.md`](docs/phase-1.4-milestone-2-durable-data-shape-domain-model.md), [`docs/phase-1.4-milestone-3-request-context-protected-action-contract.md`](docs/phase-1.4-milestone-3-request-context-protected-action-contract.md), [`docs/phase-1.4-milestone-4-provider-auth-decision-criteria.md`](docs/phase-1.4-milestone-4-provider-auth-decision-criteria.md), [`docs/phase-1.4-milestone-5-provider-finalization-gate.md`](docs/phase-1.4-milestone-5-provider-finalization-gate.md), [`docs/phase-1.4-milestone-6-backend-persistence-readiness-closeout.md`](docs/phase-1.4-milestone-6-backend-persistence-readiness-closeout.md), [`docs/phase-1.4-decision-record-production-database.md`](docs/phase-1.4-decision-record-production-database.md), [`docs/phase-1.4-decision-record-deployment-topology.md`](docs/phase-1.4-decision-record-deployment-topology.md).
-   **Phase 2** — Real Integrations (incl. Automation + Alerts, re-sequenced from the former Phase 1.4)
-   **Phase 3** — Compliance + Evidence
-   **Phase 4** — Predictive / AI Operations

# Documentation Links

-   [`docs/platform-operations-security-history.md`](docs/platform-operations-security-history.md) — detailed long-form implementation history for all completed Platform Operations & Security phases (Phase 1.1 through 1.1.3D, Phase 1.2 — Domains + Platform Settings Maturity, Phase 1.2E, and Phase 1.2F — Tenant Web Address, including every table/drawer correction pass).

# External Dependencies

-   **Firebase**: **Authentication** + a **single `users/{uid}` Firestore read** (no Firestore writes / no broad persistence today).
-   **Google AI Studio / Gemini API**: `@google/genai` dependency present but **scaffold/unused** (no call sites today).
-   **Recharts**: Used for charts and data visualizations.
-   **Framer Motion**: Employed for UI animations and transitions.
-   **EasyPost**: Shipping API for label generation and tracking.
-   **Shippo**: Shipping API for label generation and tracking.
-   **ShipStation**: Shipping API for label generation and tracking.
