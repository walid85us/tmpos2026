# Overview

This project is a multi-tenant SaaS frontend platform for small to medium-sized businesses. It provides a unified suite of tools including Point-of-Sale (POS), employee and customer relationship management (CRM), inventory control, reporting, and marketing. The platform aims to streamline operations, offer actionable insights, and automate business processes through a scalable, customizable, and feature-rich architecture.

# User Preferences

I prefer detailed explanations.
I want iterative development.
Ask before making major changes.
Do not make changes to the folder `Z`.
Do not make changes to the file `Y`.

# System Architecture

## Frontend

The frontend is built using React 19, TypeScript, Vite 6, and Tailwind CSS v4.

## UI/UX Decisions

-   **Design System**: Features rounded cards, a glass effect (`bg-white/80 backdrop-blur-xl`), a consistent primary color theme, and a `ghost-border` utility.
-   **Typography**: Uses `font-black uppercase tracking-widest` for labels.
-   **Animations**: Employs Framer Motion for smooth UI transitions and consistent modal patterns.
-   **Notifications**: Utilizes toast notifications for user feedback.
-   **Accessibility**: Prioritizes semantic accessibility for forms, tables, and modals.

## Technical Implementations & Feature Specifications

-   **Authentication & Access Control**: Implemented with Firebase Authentication and a 7-level hierarchical permission model.
-   **Tenant Management**: Provides full lifecycle management, including provisioning, subscriptions, feature overrides, billing, and audit logging.
-   **Business Management Modules**:
    -   **Point of Sale (POS)**: Comprehensive cart management, diverse payment options, tax/discount calculations, customer management, repair intake, held orders, quick add stock, operator switching with RBAC, refund workflows, and warranty claims.
    -   **Employee Management**: Manages employees, roles, time tracking, and payroll with granular permissions.
    -   **CRM (Customers)**: Enhanced CRM with multi-field search, loyalty programs, duplicate detection, and integrated order/invoice history.
    -   **Invoices**: Supports full invoice lifecycle with dynamic line items, searchable catalog, payment processing, and configurable document templates.
    -   **Services**: Manages a service catalog with CRUD operations, warranty configuration, and bulk price editing.
    -   **Repairs Module**: Manages the full repair ticket lifecycle, including structured workflow, parts tracking, technician assignment, activity timelines, and financial summaries.
    -   **Inventory Management**: Comprehensive system for managing various `StockItem` types (serialized, non-serialized, handset). Features include UPC/manufacturer/location tracking, min/max stock levels, serial number tracking, advanced filtering, stock adjustments, global movements log, inventory transfer lifecycle, stock counts, and trade-in management, all integrated with RBAC.
    -   **Supply Chain Management**: Manages suppliers, purchase orders, Goods Received Notes (GRN), and Return Merchandise Authorization (RMA) with detailed tracking and financial recording, gated by RBAC.
    -   **Shipping Center**: A centralized fulfillment module for managing shipments across all source documents, supporting various shipment types. Includes status transition workflows, tracking event timelines, carrier/service level configuration, cost tracking, a provider adapter layer for external shipping carriers, service points, pickup requests, and address validation. Features a unified, idempotent webhook/event processing pipeline, and tools for bulk tracking synchronization.
    -   **Returns Portal / Reverse Logistics**: A first-class module for managing returns with a structured lifecycle (Draft → Requested → Approved → ... → Completed), return reasons, requested/final resolutions, item disposition, intake/inspection workflow, and integration with return shipments.
-   **Reporting**: Features a dashboard with data visualizations.
-   **Onboarding**: A multi-step process for new tenants, covering plan selection, pre-configured settings, and an activation checklist.
-   **Document Template Editor**: A structured editor for 5 document types, offering deterministic HTML generation, branding, and visual preview.
-   **Carrier Analytics Foundation (Phase 2 + correction pass)**: Provides deterministic, truthful shipping metrics inside Shipping Center → Analytics tab. Implementation in `src/components/shipping/CarrierAnalytics.tsx`.
    -   **Mode derivation**: The Mode filter uses a `deriveMode(shipment)` helper that mirrors the operational `getShipmentMode()` in ShippingCenter (provider when `selectedRate` exists; manual when `carrier && serviceLevel` are populated without a live rate; provider otherwise). The stale UI-only `shipmentMode` field is intentionally NOT consulted, so analytics cannot drift from operational behavior.
    -   **Status-to-bucket mapping**: Dispatched and In Transit are SEPARATE buckets. `dispatchedCount = shipments where status === 'Dispatched'`. `inTransitCount = shipments where status === 'In Transit'`. We never collapse Dispatched into In Transit. The volume card explicitly renders both with a "Dispatched ≠ In Transit" affordance, plus Delivered, Exceptions, and Cancelled cards.
    -   **Pickup Analytics gating**: The pickup-analytics block is gated by BOTH the `pickup_requests` plan feature AND the `view_pickup_analytics` sub-permission (separate from operational `request_carrier_pickup` so visibility can be granted without the ability to schedule or cancel pickups). When the plan excludes pickups, a coherent "not included" state renders. When the role lacks the sub-permission, a coherent "not enabled for your role" state renders. Local-only intents are NEVER inflated into provider-confirmed counts (provider-confirmed strictly requires `status in {confirmed, completed} AND source === 'live_provider'`).
    -   **Provider-connected analytics**: When an active aggregator provider is configured (`activeProviderId !== null`), an additional read-only block surfaces provider-backed sub-counts (provider shipments / labels / delivered / dispatched / in-transit / provider-confirmed pickups) derived from the SAME already-recorded fields. We do NOT poll a live carrier dashboard, do NOT fabricate SLA grades, and do NOT imply parity across unconfigured providers. An explicit coverage chip and amber caveat call out partial coverage truthfully. The block hides entirely when no provider is configured.
    -   **Cost metrics, lifecycle timing, distributions, coverage chips**: As before — cost gated by `view_shipping_costs`, lifecycle timing requires ≥3 paired timestamps (otherwise `insufficient-data: N/3` badge — never extrapolated), distributions surface `Unknown` rows in amber, coverage chips colored green ≥90% / amber ≥50% / rose <50%.
    -   **Plan + permission gating (overall surface)**: The Analytics tab requires `carrier_analytics` plan feature (Growth + Advanced) AND `view_carrier_analytics` sub-permission. The component re-validates both internally so a stale activeTab cannot bypass either gate. `view_carrier_analytics` has `defaultLevel: 'manage'` so the schema-level fallback cannot silently grant analytics to shipping viewers; technician + sales_associate role templates also explicitly seed `false`.
-   **Carrier Locators / Shipping Provider plan & permission treatment (Phase 2 correction)**:
    -   **Plan features**: `service_points` (Carrier Locators) and the new `shipping_providers` (Shipping Provider Configuration) are independently controlled in System Owner Plans & Features (`src/owner/mockData.ts` featureMatrix). Both ship enabled for Growth + Advanced and disabled for Essential.
    -   **Settings tab gating in Shipping Center**: When `shipping_providers` is excluded from the plan, the ShippingProvidersPage block is replaced by a "not included in your current plan" state. When `service_points` is excluded, the Carrier Locators block is replaced with a similar state. Aggregator provider configuration (EasyPost / Shippo / ShipStation) and per-carrier locator adapters (USPS / FedEx / UPS / DHL / GLS) are conceptually separate and gate independently.
    -   **Sub-permissions**: `manage_shipping_settings` controls aggregator provider configuration. The new `manage_carrier_locator_settings` (`defaultLevel: 'manage'`, granted to manager / store_owner only) controls per-store locator adapter configuration. The Carrier Locators block hides with a coherent "not enabled for your role" state when the operator lacks this sub-permission. The new `view_pickup_analytics` sub-permission controls pickup-analytics visibility independently from operational pickup actions.
-   **Customs Docs and Insurance Rules — queued, not deferred**: These are NOT implemented in the Phase 2 correction pass, but per the correction-pass guidance they are no longer treated as indefinitely deferred. They are the next queued shipping improvements after this correction pass closes. When implemented they MUST be added coherently to BOTH System Owner Plans & Features (as plan-gated features) and Store Owner Permissions Matrix (as sub-permissions), matching the gating discipline used here for Carrier Analytics, Pickup Requests, Service Points / Carrier Locators, and Shipping Provider Configuration.
-   **Out of scope for Phase 2 correction pass**: The broader Addon governance / archived-addon / tenant Features tab paid-override redesign is intentionally NOT in this correction pass. It belongs to a separate commercial-platform workstream.
-   **Phase 2 — Plan-to-Permission Propagation + Provider Hard-Disable (general rule)**: A generalized rule has been formalized: plan decides whether a feature exists; role decides who uses it; role can NEVER resurrect a plan-disabled feature. This is now enforced at three layers — (1) `FEATURE_PERMISSION_DEPENDENCIES` map in `accessConfig.ts` links feature ids (`shipping_providers`, `pickup_requests`, `service_points`, `carrier_analytics`) to dependent sub-permissions, with helpers `isSubPermissionPlanAvailable`, `isPlanFeatureLiveFor`, `getFeatureGatesForSubPermission`; (2) `AccessContext.checkSubPermission` runs the plan-availability check BEFORE the system_owner/store_owner blanket allow so no role can exercise a plan-disabled capability; (3) `Employees.tsx` Permissions Matrix renders Plan-Locked rows with a truthful subtext (which feature/module is missing) and overrides the Store Owner column to Plan-Locked instead of Allowed. `planFeatures` for Growth/Advanced now includes `carrier_analytics` so the static-fallback path is consistent with the override path.
-   **Phase 2 Final Settings Scoping + Dynamic Sub-Permission Count correction**: Three architectural fixes that close the last QA gaps from the entitlement-integrity work. (1) **Parent tab vs subsection scoping** — the Shipping Center Settings tab is a CONTAINER for multiple independent subsections (Shipping Provider configuration, Carrier Locators, general shipping). Tab visibility is now gated on having ANY of `manage_shipping_settings` (general), `manage_carrier_locator_settings`, or `configure_shipping_provider` — NOT on the provider permission alone. Disabling the Shipping Provider Configuration plan feature no longer collapses the entire Settings tab; it only hides/replaces the Shipping Provider configuration subsection inside. (2) **Truthful unavailable-for-plan rendering at the right scope** — the Shipping Provider configuration subsection inside the now-preserved Settings tab continues to render its truthful "Shipping Provider configuration is not included in your current plan" card when `planAllowsShippingProviders` is false, instead of disappearing into a missing-tab condition. The state model stays distinct: enabled-and-configured / enabled-unconfigured / plan-disabled / permission-denied / subsection-unavailable-because-plan-disabled. (3) **Dynamic sub-permission counts** — the "N Sub-Perms" badge in the Store Permissions Matrix and the create-role modal now derives its count from the plan-filtered sub-permission list (`getSubPermissionsForDomain(d).filter(sub => isSubPermissionPlanAvailable(sub, tenant.plan))`), not the static master array. When a linked sub-permission disappears due to a plan flip, the count decreases; when the plan re-enables the feature, it recovers. The count always matches what the operator can actually expand and see. The general rule "linked feature permissions disappear from the matrix when plan-disabled" remains intact and is reinforced by counting from the same filtered list. Customs Docs and Insurance Rules remain queued as the next shipping improvements after this correction closes.
-   **Phase 2 Final Entitlement Integrity correction — linked feature permissions DISAPPEAR (general rule)**: Refined the prior plan-to-permission propagation rule. Previously plan-disabled sub-permissions rendered as "Plan-Locked" in the Store Permissions Matrix; the final rule is that they are OMITTED entirely. `Employees.tsx` now does `if (!isSubPermissionPlanAvailable(sub, tenant.plan)) return null;` — no Plan-Locked chrome, no disabled row. The matrix only displays capabilities that actually exist for the tenant's plan. The `FEATURE_PERMISSION_DEPENDENCIES` map remains the single architectural source of truth, so future feature/plan additions inherit disappearance semantics with no UI changes needed. The runtime evaluator `checkSubPermission` still short-circuits to denied for plan-locked sub-permissions, so even a stale role grant cannot resurrect a plan-disabled capability.
-   **Explicit Shipping Provider configuration permission**: Added new sub-permission `configure_shipping_provider` (defaultLevel: 'manage', store_owner / manager only). It is the dedicated permission for connecting / configuring / switching environments for / removing aggregator shipping providers (EasyPost / Shippo / ShipStation). It is plan-gated by `shipping_providers` via `FEATURE_PERMISSION_DEPENDENCIES` and is independent of (a) general `manage_shipping_settings` (origin defaults / package presets / manual carrier list / label preferences) and (b) per-action provider permissions (`fetch_shipping_rates` / `purchase_shipping_label` / `validate_shipping_address` / `sync_shipping_tracking`). `ShippingCenter.canManageProviderSettings` now reads `configure_shipping_provider`. `manage_shipping_settings` is no longer a member of the `shipping_providers` feature dependency list and remains visible/assignable independent of provider plan gating.
-   **Shipping Provider FULL deactivation on plan disable**: When `shipping_providers` is no longer live in the tenant's plan, a single plan-aware effect in `ShippingCenter.tsx` performs hard deactivation: (1) calls `shippingApi.setActiveProvider(null)` to clear the server's active-provider pointer, (2) enumerates all configured providers via `getProvidersStatus` and calls `removeProviderCredentials(id)` for each, and (3) clears every local operational state slot (active provider id, environment, provider statuses, settings panel visibility, available rates, rates panel visibility). The effect uses a `cancelled` token so any in-flight load cannot land after the plan flips and resurrect stale state. The on-mount load is also folded into the same effect so it only runs when the plan ALLOWS providers — eliminating the resurrection race entirely. If the server purge partially fails the operator sees a truthful `providerWarning` ("provider deactivated locally; some server-side credentials may not have been cleared and will be removed on the next successful sync"). When the plan re-enables the feature later, the load branch re-runs but the server has no active provider and no credentials, so the operator must explicitly reconnect — nothing silently resurrects.
-   **Shipping Provider hard-disable in `ShippingCenter.tsx`**: Renamed underlying state to `activeProviderIdRaw` and introduced a single derived `activeProviderId = planAllowsShippingProviders ? activeProviderIdRaw : null` declared immediately after the state. All existing `if (!activeProviderId)` early-returns therefore auto-inherit plan gating with no per-handler bypass. A defense-in-depth `guardShippingProviderPlan` is invoked at the top of every provider-call handler (handleFetchRates, handlePurchaseLabel, handleValidateAddress(es), handleSyncTracking, handleBulkSync, handleRequestPickup, handleConfirmPickupBuy, handleTestConnection, handleReplayEvent, handleSimulateTrackingEvent) so the operator sees a truthful "not included in your current plan" message instead of the generic "configure a provider" message. Credential store state is preserved across plan changes — only the derived id nulls out at read time. A truthful four-state `providerAvailability` tagged union (`plan_disabled | permission_denied | no_provider | configured`) drives `getProviderPrerequisiteMessage`, surfacing a distinct message per state. `CarrierAnalytics` automatically inherits this gating because it consumes the plan-aware `activeProviderId` prop.

-   **Phase 3 — Shipping Automation Rules + Batch Labels Foundation**: Two new shipping capabilities added, each plan-gated and permission-gated independently and each strictly truthful.
    -   **Plan features**: `shipping_automation_rules` and `batch_labels` added to the System Owner Plans & Features matrix (`src/owner/mockData.ts`). Both ship enabled for Growth and Advanced and disabled for Essential. Each gates a separate top-level Shipping Center tab.
    -   **Sub-permissions**: Four new sub-permissions live in `src/context/accessConfig.ts` — `manage_shipping_automation_rules`, `view_shipping_automation_results`, `manage_batch_labels`, `purchase_batch_labels`. They are wired into `FEATURE_PERMISSION_DEPENDENCIES` so they Plan-Lock when the corresponding plan feature is excluded; `purchase_batch_labels` additionally depends on `shipping_providers` because batch execution iterates the single-shipment label-purchase flow which itself requires the provider feature. Role seeds: store_owner = true, manager = true (already true via blanket), technician/sales = false.
    -   **Automation engine** (`src/shipping/automationEngine.ts`): Pure module with `runAutomation(rules, shipment, trigger)`. Rules can flag, add internal notes, mark-review-needed, mark-ready-for-batch, and set priority. Rules CANNOT purchase labels, change status, cancel shipments, request pickups, or perform any irreversible carrier action — this constraint is enforced by the closed `AutomationActionType` union. Triggers: `shipment_created`, `shipment_updated`, `status_changed`, `label_purchased`, `pickup_*`, `tracking_synced`, `return_shipment_created`. Conditions evaluate against a typed `AutomationConditionField` set including derived fields like `addressValidationState` and `hasLabel`. Every match writes an `AutomationLogEntry` for auditability (capped at 500 in local state).
    -   **State**: `StoreLocalState` exposes `automationRules`, `automationLogs`, `shipmentBatches` and their CRUD/append helpers. The `Shipment` type was extended with optional Phase-3 fields (`flags`, `priority`, `reviewNeeded`, `batchQueueState`, `batchQueueMarkedAt`, `batchQueueRuleId`, `internalNotes`) so action results are first-class on the shipment record.
    -   **Wiring in `ShippingCenter.tsx`**: A `triggerAutomation(shipment, trigger, extraUpdates)` helper is invoked from `handleCreateShipment` (`shipment_created`), `handleConfirmStatusChange` (`status_changed`), and `handlePurchaseLabel` (`label_purchased` then `status_changed`). Each invocation merges aggregate updates with any concurrent operational updates so a single `addShipment`/`updateShipment` call applies both the operator action and rule outcomes.
    -   **Batch Labels** (`src/components/shipping/BatchLabels.tsx`): Truthful per-shipment iteration, NOT a provider-native bulk API. Eligibility for each shipment reuses `getLabelPrerequisites` plus `planAllowsShippingProviders` and `activeProviderId`, surfacing exact reasons when ineligible. Execution iterates `purchaseLabelForBatch(id)` per selected shipment, recording `success | failed | skipped` per item. The batch transitions `processing → completed | completed_with_errors`. The Batch tab is hidden when the plan excludes `batch_labels`; when the plan allows batches but excludes the provider feature, the operator can still assemble a batch but execution is disabled with a truthful banner.
    -   **Automation tab** (`src/components/shipping/AutomationRules.tsx`): Rule editor (name, trigger, conditions, actions, enabled), rule list with run/match counters, and a collapsible auditable Results log. Manage vs view-results are independent sub-permissions so a role can be granted read-only access to results without rule editing.

-   **Phase 3 Correction Pass — Automation Rules Clarity & Execution Transparency**: Strict isolated correction pass on top of the Phase 3 base. No new automation actions, no broadened action surface, no other shipping workstreams touched.
    -   **Typed field model** (`src/shipping/automationEngine.ts`): Each `AutomationConditionField` is classified by `AutomationFieldKind` (`boolean | number | enum | text`) via a `FIELD_DESCRIPTORS` registry that also carries the human-readable field label and, for enum fields, the closed list of valid options (`mode`, `status`, `sourceType`, `addressValidationState`).
    -   **Operator language by field type** (`OPERATORS_BY_KIND`): Underlying op codes (`eq/neq/in/notIn/gt/gte/lt/lte/truthy/falsy/contains/not_contains/starts_with/ends_with`) are now implementation primitives only; the UI never exposes them. Booleans show only `is true / is false`; numbers show `equals / does not equal / greater than / greater than or equal to / less than / less than or equal to`; enums show `is / is not / is one of / is not one of`; text adds `contains / does not contain / starts with / ends with`. Four new ops (`contains`, `not_contains`, `starts_with`, `ends_with`) added to the engine to back the text operators.
    -   **Type-aware rule builder** (`src/components/shipping/AutomationRules.tsx`): The operator dropdown is filtered by the selected field's kind; the value input shape is also kind-driven (no input for boolean ops, number input for numeric, single-select dropdown for enum eq/is, multi-select chips for `in/notIn`, free-text for text). Changing a condition's field resets its op and value to safe defaults so the previous boolean-paired-with-numeric-value footgun (e.g. `Has Label eq 6`) is no longer expressible.
    -   **Plain-language summary** (`describeRule`): Every rule renders a sentence like *"When a carrier label is purchased and Shipping Cost greater than or equal to $6.00, mark the shipment for review."* The editor shows it live as the operator builds the rule; the rule list shows the persisted summary; the execution log shows it next to each entry; the delete confirm shows it for the rule being removed.
    -   **Per-rule last evaluation snapshot**: `AutomationRule.lastEvaluation` records `{ matched, shipmentId, shipmentNumber, trigger, timestamp, failedConditionIndex?, failedConditionDescription? }` from the engine's `evaluations` output. Visible on each rule card so the operator can see whether the most recent evaluation matched and, if not, which condition failed — without bloating the global execution log.
    -   **Execution log = matches-only audit**: The execution log records only matches — entries the engine actually acted on — so the audit trail is truthful. Non-matches are surfaced through each rule's `lastEvaluation` snapshot on its card (matched? / which condition failed) instead of as log rows. Each log entry stores a `ruleSummary` snapshot of the rule's plain-language description at execution time, so historical rows do not drift if the rule is later edited or deleted.
    -   **In-app delete confirmation**: The previous browser `confirm()` for rule deletion is replaced with an in-app modal that names the rule, shows its summary, and offers Cancel + Delete Rule consistent with the rest of the app's modal language. No browser-native dialog is used on this path.
    -   **Out of scope (unchanged)**: Packing Workflows, SLA Optimization, Carrier Scorecards, Customs Docs, and Insurance Rules are not implemented in this pass.
-   **Phase 3 Correction Pass #2 — Automation Action Outcomes & Operator Findability**: Strict isolated correction on top of Correction Pass #1. No new automation actions, no broader workstreams.
    -   **Action-to-outcome contract**: Each existing action now produces a real, durable, operator-visible outcome surfaced in the same place the operator already works (the shipment list and detail view), not only in the execution log. `mark_review_needed` writes `Shipment.reviewNeeded = { ruleId, ruleName, reason, markedAt, resolved:false }` and stays unresolved until an operator clicks Resolve; `mark_ready_for_batch` sets `batchQueueState='ready_for_batch'`; `set_priority` sets `priority`; `add_flag` appends to `flags[]`; `add_internal_note` appends a rule-sourced note. Re-application of `mark_review_needed` by the same rule is suppressed when an unresolved entry from that rule already exists, so duplicate audit-log spam is impossible.
    -   **Review lifecycle** (`StoreLocalState.resolveShipmentReview`): Operators resolve from the shipment detail's Automation Outcomes panel via a confirm modal that captures an optional resolution note. Resolution sets `resolved/resolvedAt/resolvedBy/resolutionNote` on the existing `reviewNeeded` (preserving the original rule source for historical truth) and atomically appends a `system`-sourced internal note ("Review resolved by … — note") so the audit trail is intact. Permission gate: `canManageAutomationRules || edit_shipment_pre_dispatch` — no new sub-permission was introduced.
    -   **Operator findability — list surface** (`src/components/ShippingCenter.tsx`): The shipment list shows per-row badges for Review Needed (amber, unresolved only), Ready for Batch, High/Urgent Priority, and Flag count. A new "Automation Outcomes" filter chip row above the list (visible only when the plan allows automation rules and at least one outcome exists) lets operators narrow to Review Needed / Ready for Batch / High Priority / Flagged with live counts.
    -   **Operator findability — detail surface**: The shipment detail Overview tab leads with an "Automation Outcomes" panel that surfaces the unresolved review (with rule name, reason, marked-at, and a Resolve button), priority chip, batch-queue chip, flag chips, and recent rule-sourced + system-sourced internal notes — all derived from the same fields the engine writes. Resolved reviews stay visible as a green "Review Resolved" entry crediting the resolver and original rule, so the history doesn't disappear.
    -   **Builder helper text** (`src/components/shipping/AutomationRules.tsx`): Each action row in the rule editor shows a one-line plain-English description of where the outcome will appear in the operator workflow (row badge, filter chip, detail panel, internal notes), so authors understand what the action actually does at run time.
    -   **Out of scope (unchanged)**: Packing Workflows, SLA Optimization, Carrier Scorecards, Customs Docs, Insurance Rules, and any broadening of the automation action surface.

-   **Phase 3 Correction Pass #3 — Automation Guardrails, Approval Workflow & Operational Execution History**: Splits the automation surface into two truthful execution modes and tightens the permission/audit story. No other shipping workstreams touched.
    -   **Two rule types** (`src/types.ts`, `src/shipping/automationEngine.ts`): Every `AutomationRule` now carries `ruleType: 'observational' | 'guardrail'` (default `observational`). Observational rules continue to post-fire after operator actions (existing behavior). Guardrail rules pre-fire at decision points and gate the operator action itself. `runAutomation` skips guardrail rules; `applyActionsToShipment` skips guardrail-only actions; `evaluateGuardrails(rules, shipment, trigger, candidatePatch?)` is a pure pre-decision helper returning `{ blockingRules, approvalRules, allClear }` with no mutation and no log writes.
    -   **New trigger & actions** (engine + types): `pre_label_purchase` trigger; `require_approval` and `block_unless_approved` actions (guardrail-only — engine asserts and `actionsForProcessType` filters the dropdown). Trigger/action label maps and `describeAction` extended.
    -   **Expanded review state** (`Shipment.reviewNeeded`): `kind: 'review' | 'approval' | 'block'` (default `review`) plus `state: 'pending' | 'resolved' | 'approved' | 'overridden' | 'dismissed'` (default `pending`), with `approvedBy/approvedAt`, `overriddenBy/overriddenAt`, `dismissedBy/dismissedAt` and a `triggerContext` snapshot (e.g. `selectedRate` at the time of the guardrail decision) for forensic truth. `AutomationLogEntry` gained `ruleType` and `guardrailOutcome` (`approved | overridden | blocked | approval_required`).
    -   **Permissions** (`src/context/accessConfig.ts`): The pre-existing `view_shipping_automation_results` was relabeled "View Automation History" and three new sub-perms were added under the same `shipping_automation_rules` feature: `resolve_shipping_automation_reviews`, `approve_shipping_automation_exceptions`, `override_shipping_automation_guardrails`. Manager seeded `true`; tech/sales seeded `false`.
    -   **Generalized resolution action** (`src/context/StoreLocalState.tsx`): `setReviewOutcome(id, { resolution: 'resolve'|'approve'|'override'|'dismiss', actor, note })` is the single mutation point; the audit-note text varies by resolution and the appropriate state + timestamp + actor fields are set atomically. `resolveShipmentReview` is preserved as a thin wrapper so existing callers keep working.
    -   **Rule builder** (`src/components/shipping/AutomationRules.tsx`): A "Rule Type" segmented selector at the top of the editor switches the trigger and action dropdowns to the type-appropriate list (`triggersForProcessType`/`actionsForProcessType`); changing type resets trigger/actions to safe defaults. Helper text was added for `require_approval` ("Operator must approve before continuing") and `block_unless_approved` ("Blocks the operator action unless an approver clears it"). The rule list shows a `ruleType` badge so authors can scan modes at a glance.
    -   **Execution History clickability + outcome awareness** (`src/components/shipping/AutomationRules.tsx`): Shipment numbers in the Execution History table are now buttons that call a new `onOpenShipment` prop. ShippingCenter wires this to switch to the Shipments tab and select the row. When a log entry has an associated live `reviewNeeded` on its shipment, the row shows the current outcome state (Pending / Approved / Overridden / Resolved / Dismissed) so history reflects truth, not just the moment of fire.
    -   **Pre-label Guardrail Modal** (`src/components/ShippingCenter.tsx`): `handlePurchaseLabel` and `purchaseLabelForBatch` evaluate `pre_label_purchase` guardrails against the shipment plus the selected rate. If matches exist and the call wasn't already cleared (`opts.bypassGuardrails`), the single-shipment path opens a modal listing each rule with its reason and color (rose for blocking, amber for approval-required) and offers Cancel / Choose Other Rate / Request Approval / Approve & Purchase / Override & Purchase. Approve and Override are perm-gated (`approve_shipping_automation_exceptions` and `override_shipping_automation_guardrails`), and on confirm they write the appropriate `reviewNeeded` record + audit log and resume the purchase with `bypassGuardrails: true`. The batch path records a blocked or approval-required `reviewNeeded` on each affected shipment and skips it without ever silently purchasing — the operator must clear the review from the detail panel before the next batch run.
    -   **Resolve modal — kind-aware** (`src/components/ShippingCenter.tsx`): The existing Resolve Review modal now reads `reviewNeeded.kind` and exposes Dismiss / Override / Approve / Resolve Review buttons appropriate to that kind, gated on the new resolution permissions; all paths route through `setReviewOutcome` so audit notes and state stay consistent with the guardrail flow.
    -   **Out of scope (unchanged)**: No changes to non-automation shipping flows (rates, batches, packing, customs), no broadening of the observational action surface, no new triggers beyond `pre_label_purchase`.

-   **Phase 3 Correction Pass #4 — Purpose-Driven Automation Rules, Trigger/Condition/Action Compatibility Matrix & Outcome-vs-History Permission Split**: Replaces the technical Observational/Guardrail rule-type UX with an outcome-first authoring model and prevents matrix-incompatible rule shapes from ever being saved.
    -   **RulePurpose model** (`src/types.ts`, `src/shipping/automationEngine.ts`): `AutomationRule.purpose?: 'flag_note' | 'queue_batch' | 'require_review' | 'require_approval' | 'block_action'` is the new authoring axis. `ruleType` is now derived from purpose+trigger via `effectiveProcessType` (`require_review` becomes a guardrail only on `pre_label_purchase`; otherwise observational; `require_approval` and `block_action` are always guardrail and only valid on pre-action triggers). Legacy rules without `purpose` are migrated at edit time via `getRulePurpose()` which infers via `inferPurposeFromActions` (block > approval > review > queue > flag). New action `require_review_before_action` is the implicit primary for `require_review` on pre-action triggers; `mark_review_needed` remains the implicit primary on observational triggers.
    -   **Compatibility matrix** (`src/shipping/automationEngine.ts`): `PURPOSE_TRIGGER_MATRIX` + `triggersForPurpose` + `purposesForTrigger` define the allowed (purpose, trigger) pairs. `actionsForPurpose(purpose, trigger)` = implicit `primaryActionFor` + `supplementaryActionsFor` (only `flag_note` and `queue_batch` accept supplementary `add_flag`/`add_internal_note`/`set_priority`; pre-action purposes are atomic). `conditionFieldsForTrigger(trigger)` narrows the field dropdown so a condition can never reference a field with no operational meaning at that trigger — pre_label_purchase exposes the new `selectedRateCost` (reads `shipment.selectedRate?.rate`), pickup/tracking/label/return triggers each get their own narrow set, and other triggers default to the full observational catalog.
    -   **Engine review bucket** (`src/shipping/automationEngine.ts`): `evaluateGuardrails` now picks up rules with effective process type guardrail (including `purpose='require_review'` on `pre_label_purchase`) and returns a third `reviewRules` bucket alongside `blockingRules`/`approvalRules`. Blocking and approval guardrail-only actions (and the new `require_review_before_action`) are skipped by `applyActionsToShipment` so they never silently mutate observational state.
    -   **Permissions — outcomes vs history split** (`src/context/accessConfig.ts`): New `view_shipping_automation_outcomes` sub-permission gates per-shipment outcome surfaces (Review-Needed / Approval-Pending / Blocked / Ready-for-Batch / Priority / Flag badges, the matching outcome filter chips, and the Automation Outcomes panel inside the shipment detail) independently of `view_shipping_automation_results` (now strictly the audit history surface). Both are mapped to the `shipping_automation_rules` plan feature; manager seeded `true`, technician/sales seeded `false`. Existing pages can be wired to gate on either or both without changing the rest of the perm model.
    -   **Purpose-first rule builder** (`src/components/shipping/AutomationRules.tsx`): The "Rule Type" radio is replaced by a five-option "Purpose" segmented selector with plain-language descriptions (`PURPOSE_LABELS`/`PURPOSE_DESCRIPTIONS`). The trigger dropdown is filtered by `triggersForPurpose`; the condition-field dropdown by `conditionFieldsForTrigger`; the action UI shows the implicit primary action as a fixed chip (no swap, no remove) and only renders an editable supplementary picker / "+ Add Supplementary Action" affordance when `supplementaryActionsFor` returns a non-empty set. Changing the purpose resets trigger to the first compatible option, drops conditions on now-invalid fields, and rebuilds actions from the new primary + still-allowed supplementary set; changing the trigger does the same drop+rebuild so the rule is always valid. Editing a legacy rule re-derives purpose so the new builder always opens with a concrete value.
    -   **Pre-label modal — review path** (`src/components/ShippingCenter.tsx`): `pendingGuardrail` state and the modal carry a third `reviewRules` bucket. Review-only rules render an indigo "Requires Review" chip and unlock an "Acknowledge & Purchase" button accessible to any operator (no approve/override permission required); on confirm, the modal writes one `reviewNeeded { kind: 'review', state: 'resolved' }` record (resolvedBy/resolvedAt/note) and one `guardrail:acknowledged` audit log entry per fired review rule, then resumes purchase with `bypassGuardrails: true`. Mixed runs (review + approval and/or block) keep the existing approve/override path while still surfacing review reasons. The batch path treats review-only matches like approval — non-interactive, so the row is skipped with a `review_required` `reviewNeeded` and a `guardrail:review_required` audit log entry until an operator clears it.
    -   **Out of scope (unchanged)**: No changes to non-automation shipping flows, no new triggers beyond those already in the matrix, no changes to outcomes/history rendering itself (the new perm only gates whether existing outcome surfaces render — pages that adopt the gate can do so incrementally).

-   **Phase 3 Correction Pass #5 — Guardrail Approval-Request Lifecycle**: Fixes the approval-request loop bug so requester / approver / shipment detail / execution history / purchase flow all agree on the same state machine.
    -   **State model** (`src/types.ts`): `Shipment.reviewNeeded` gains explicit approval-lifecycle fields: `state` adds `'approval_requested'` (the explicit pending state for `kind='approval'`); `requestedBy`, `requestedAt`, `requesterNote`, `approverNote`, `approvalContextKey`, and `selectedRateContext` are persisted so the approver sees who asked, when, why, and what rate they are approving — without losing the requester's note across user/role switches. `Shipment.approvedGuardrailContexts: string[]` lists the approval-context keys that have been cleared on the shipment, so the engine can recognize an already-approved (rule + rate) pair and skip it.
    -   **Approval context key** (`src/shipping/automationEngine.ts`): `buildApprovalContextKey(ruleId, shipmentId, selectedRate)` produces a stable hash from the rule id, shipment id, and a deterministic selected-rate signature (prefers `providerRateRef`, falls back to `carrier|service|rate|currency`). The key is the single source of truth for both duplicate-request prevention and approved-context recognition. Selecting a different rate produces a different key, so prior approval cannot leak to a different rate.
    -   **Engine — already-approved skip**: `evaluateGuardrails` computes the candidate key for each guardrail rule against the candidate (or current) selected rate and skips any rule whose key is already in `shipment.approvedGuardrailContexts`. This is what closes the post-approval re-prompt loop: after Approve & Purchase, the same rule + rate combination is recognized as cleared, so a re-purchase attempt does not re-open the modal.
    -   **Requester Pending Approval view** (`src/components/ShippingCenter.tsx`): When a non-approver clicks Request Approval, `requestApprovalOnly` writes `state='approval_requested'` (kind=approval) or `'pending'` (kind=block) and persists `requestedBy`, `requestedAt`, `requesterNote`, `approvalContextKey`, and `selectedRateContext`. The audit log entry switches from `guardrail:approval_required` to `guardrail:approval_requested` (with `guardrailOutcome='approval_requested'`) so history reflects the requester action, not just the rule fire. The shipment detail's Automation Outcomes panel renders this state as **Pending Approval** (sky styling, `pending` icon) with the rule name, requester + timestamp, selected-rate snapshot, and requester note inline — replacing the generic "Marked for Review" copy that previously hid the request.
    -   **Approver view** (`src/components/ShippingCenter.tsx`): The Resolve Review modal now opens with a dedicated "Approval Request" card that shows requester + timestamp, the snapshotted selected rate (carrier, service, cost), and the requester note before the approver writes their own decision note. The note input is relabeled "Approver Note" for `kind` of `approval`/`block`. Approve/Override actions remain perm-gated. The detail panel's resolve action button text is now kind-aware ("Review Request" for approval, "Resolve Block" for block, "Resolve" for review).
    -   **Duplicate-request prevention** (`src/components/ShippingCenter.tsx`): The pre-label modal computes the same approval-context key the engine uses; if the shipment already has a pending approval/block reviewNeeded with a matching key, the modal's title flips to **Approval Pending** (sky), a "Approval request already submitted" banner is shown with the original requester + note, and the Request Approval button is disabled with the label "Approval Request Submitted" (the handler also short-circuits as defense in depth).
    -   **Approved-context loop prevention** (`src/components/ShippingCenter.tsx`): `recordOutcomeAndPurchase` carries forward the requester metadata onto the resolved record (so history shows both requester and approver), persists `approverNote` separately from any legacy `resolutionNote`, and pushes every key in the current guardrail bundle (`block` + `approval`) into `shipment.approvedGuardrailContexts`. Because the engine now skips rules whose key is in that list, the requester can re-attempt purchase and proceed without re-prompting.
    -   **Different-rate behavior** (`src/components/ShippingCenter.tsx`): `handleSelectRate` checks for a pending approval/block reviewNeeded and, if the new rate's computed key differs from the stored `approvalContextKey`, marks the old request as `state='dismissed'` with a "Superseded by new rate selection (carrier service — $cost)" note and writes a `guardrail:superseded_by_rate_change` audit log entry (`guardrailOutcome='cleared_by_alternate_rate'`). The new rate is then re-evaluated independently — if it does not violate, no approval is required; if it violates differently, a fresh request can be filed because the dedup key has changed.
    -   **Execution history alignment** (`src/components/shipping/AutomationRules.tsx`): The outcome-chip lookup adds `state='approval_requested'` → "Pending Approval" (sky) on the live overlay, plus `guardrailOutcome='approval_requested'` → "Pending Approval", `'review_required'` → "Review Required", `'acknowledged'` → "Acknowledged" on the historical fallback. History never says "Review Needed" when the underlying state is "Approval Requested".
    -   **Permission integrity (preserved)**: Request Approval remains available to any operator (creates the request); Approve & Purchase is gated by `approve_shipping_automation_exceptions`/`override_shipping_automation_guardrails`; Override & Purchase remains gated by `override_shipping_automation_guardrails`; Acknowledge & Purchase (review-only) remains ungated. Resolve modal buttons (Dismiss / Override / Approve / Resolve) remain gated as before. The new outcome surfaces continue to honor `view_shipping_automation_outcomes`.
    -   **Out of scope (unchanged)**: No changes to non-automation shipping flows, no broadening of the automation rule schema beyond the lifecycle fields above, no new triggers, no changes to packing / SLA / scorecards / customs / insurance.

-   **Phase 3 Correction Pass #6 — Approval Status Visibility**: Surfaces the live automation approval/review state on the requester-facing shipment list and detail header, so the requester sees Pending Approval → Approved/Resolved transitions without digging into the Automation Outcomes section or Execution History.
    -   **Canonical badge derivation** (`src/components/ShippingCenter.tsx`): A single pure helper `deriveAutomationBadge(shipment)` reads `reviewNeeded.kind` + `state` + `resolved` and returns a uniform `{label, short, icon, chipClass, bannerWrap, bannerText, bannerSub, description, tone}` object — or `null` when there is nothing to surface (no `reviewNeeded`, or `state==='dismissed'` from a supersede). The same helper drives both the list-row chip and the detail-header chip + summary line, eliminating the possibility of the surfaces disagreeing. Tone mapping: kind=approval+state in (`pending`,`approval_requested`) → "Pending Approval" (sky/`pending`), kind=block+state=`pending` → "Approval Required" (rose/`block`), kind=review+state=`pending` → "Review Needed" (amber/`flag`), state=`approved` → "Approval Approved" (emerald/`verified`), state=`overridden` → "Approval Overridden" (amber/`gpp_maybe`), state=`resolved` → "Review Resolved" (emerald/`check_circle`).
    -   **Shipment list row badge** (`src/components/ShippingCenter.tsx`): The previous always-amber "Review Needed" chip is replaced with the canonical badge, so the requester can scan the list and see which shipments are pending their approver, which are approved/resolved, and which still need review. Superseded (dismissed) requests stop showing immediately. (NOTE: visibility was perm-gated by `view_shipping_automation_outcomes` at this Pass; Pass #8 later removed that visibility gate so the requester always sees the chip.)
    -   **Shipment detail header summary** (`src/components/ShippingCenter.tsx`): A status chip (using the same canonical badge) is rendered right next to the shipment number/status in the detail modal header, plus a one-line plain-language description (e.g. "Automation approval pending — waiting for an authorized approver." → "Automation approval approved — label purchase exception cleared.") under the meta line. Visible to anyone who can view automation outcomes; action buttons in the resolve modal remain permission-gated as before.
    -   **Automation Outcomes terminal banner** (`src/components/ShippingCenter.tsx`): The post-resolution banner inside the Automation Outcomes panel now switches heading/icon/copy by terminal state — "Approval Approved" (emerald/`verified`, shows approver), "Approval Overridden" (amber/`gpp_maybe`, shows overrider), "Review Resolved" (emerald/`check_circle`, shows resolver) — and surfaces both the requester note and approver note distinctly so the audit trail is preserved on a single screen.
    -   **State consistency**: shipment list badge, detail header chip + summary, Automation Outcomes section, and Execution History (Pass #5 chip) all share the same kind+state derivation, so a Pending Approval shows pending everywhere and an Approved/Resolved shows the terminal state everywhere — no stale "Review Needed" remains after approval, and superseded requests are not shown anywhere as pending.
    -   **Permission safety (Pass #6 baseline; superseded by Pass #8)**: at this Pass status visibility was perm-gated by `view_shipping_automation_outcomes`. Pass #8 later removed that gate so the requester always sees the status. Action buttons (Approve / Override / Resolve / Dismiss) continue to be gated by `approve_shipping_automation_exceptions` / `override_shipping_automation_guardrails` / `manage_shipping_automation_rules` as before — Pass #8 did not relax any action gate.
    -   **Out of scope (unchanged)**: no changes to the approval lifecycle, duplicate prevention, approved-context loop prevention, alternate-rate supersede, batch labels, execution history backing data, or non-automation shipping flows.

-   **Phase 3 Correction Pass #7 — Role-Specific Approval Labels**: Same canonical pending state, different copy for the requester vs the authorized actor, so the requester is no longer left guessing whether the request was even submitted.
    -   **Role-aware badge** (`src/components/ShippingCenter.tsx`): `deriveAutomationBadge(shipment, viewerRoles)` now accepts an optional `{canActOnApproval, canActOnReview}` viewer hint. Pending labels: kind=approval (or block) + pending → "Approval Request" / "Approval Request (Blocking)" for users who hold `approve_shipping_automation_exceptions` / `override_shipping_automation_guardrails` (action-needed framing); "Pending Approval" / "Pending Approval (Blocking)" for everyone else, including the requester (waiting framing). Review-pending → "Review Request" for users with `resolve_shipping_automation_reviews`, "Pending Review" otherwise. Terminal states (Approved / Overridden / Resolved / Dismissed) are unchanged for everyone since the outcome is the same regardless of viewer.
    -   **Viewer-roles memo**: `automationViewerRoles = useMemo({canActOnApproval: canApproveAutomationExceptions || canOverrideAutomationGuardrails, canActOnReview: canResolveAutomationReviews}, ...)` is computed once and threaded into every badge call (list row, detail header chip, detail header summary, Automation Outcomes pending tone). The Automation Outcomes panel pending title also derives from the same memo so the panel matches the badge exactly.
    -   **Surfaces updated**: shipment list row chip + tooltip, shipment detail modal header chip + plain-language summary, Automation Outcomes panel pending header — all use the same role-aware label and the same canonical state. Execution History (Pass #5) continues to render an audit-style label ("Pending Approval" / "Approval Required" / "Review Required") since history is a record of what happened, not a call to action.
    -   **Permission safety (preserved)**: visibility is still gated by `view_shipping_automation_outcomes` only; the role hint is a labelling-only signal and never enables an action button. Approve / Override / Resolve / Dismiss buttons remain perm-gated by their own checks (`approve_shipping_automation_exceptions`, `override_shipping_automation_guardrails`, `resolve_shipping_automation_reviews`, `manage_shipping_automation_rules`) — a viewer who sees "Approval Request" copy but lacks permission still gets no actionable controls.
    -   **Acceptance**: requester sees "Pending Approval" / "Pending Review" in list and detail immediately after submitting; approver sees "Approval Request" / "Review Request" in the same surfaces; both see "Approved" / "Resolved" after action; superseded requests disappear from list/header (Pass #6) and render "Request Dismissed" in the panel. Duplicate prevention, approved-context loop prevention, and alternate-rate supersede behavior from Pass #5 remain intact.

-   **Phase 3 Correction Pass #8 — Approval Badge UI Binding (visibility ungated)**: Hard correction. Pass #6/#7 derived the right state and the right copy but kept the badge surfaces gated behind `view_shipping_automation_outcomes`, which the requester typically does not hold — so the requester saw nothing. This pass removes that visibility gate from all status surfaces while keeping every action button perm-gated.
    -   **List row chip ungated** (`src/components/ShippingCenter.tsx`): the shipment list automation badge no longer requires `canViewAutomationOutcomes`. `deriveAutomationBadge(shipment, automationViewerRoles)` is called for every row; when it returns a badge, the chip renders unconditionally for any user who can see the shipment.
    -   **Detail header chip + summary ungated** (`src/components/ShippingCenter.tsx`): the chip beside the shipment number and the plain-language summary line below the meta line are now rendered unconditionally for any user who can open the detail modal. The requester sees Pending Approval / Pending Review immediately above the fold; the approver sees Approval Request / Review Request in the same place.
    -   **Automation Outcomes panel content ungated** (`src/components/ShippingCenter.tsx`): the panel itself no longer early-returns on `!canViewAutomationOutcomes`. Status content (badge title, requester name + note, rule name, selected-rate snapshot, terminal banner) is visible to anyone who can open the shipment. Action buttons inside the panel remain gated by `canResolve` (and the resolve modal still gates Approve / Override / Dismiss by their own permission checks), so a viewer without action perms sees the status without unlocking actions.
    -   **Action buttons remain perm-gated**: Approve & Purchase (`approve_shipping_automation_exceptions` / `override_shipping_automation_guardrails`), Override & Purchase (`override_shipping_automation_guardrails`), Resolve / Dismiss (`resolve_shipping_automation_reviews` / `manage_shipping_automation_rules`) are unchanged. The viewer-roles label hint from Pass #7 is purely cosmetic and never enables a button.
    -   **Debug instrumentation** (`src/components/ShippingCenter.tsx`): a `console.warn('[ShippingCenter] automation state exists but badge not rendered for shipment …', shipmentNumber, reviewNeeded)` fires from the list row whenever a shipment has a non-`dismissed` `reviewNeeded` but `deriveAutomationBadge` returns no badge. The detail header logs `console.debug('[ShippingCenter] detail header badge', shipmentNumber, tone, label)` on each render so a silent UI-binding regression surfaces in the browser console immediately.
    -   **Acceptance**: requester sees Pending Approval / Pending Review in the shipment list and the detail header without holding the outcomes perm; approver sees Approval Request / Review Request in the same surfaces; both see Approved / Resolved after action; superseded requests stop showing as pending. No new action affordance is unlocked by the visibility change.
    -   **Out of scope (unchanged)**: `view_shipping_automation_outcomes` continues to gate the row-level Priority / Ready-for-Batch / Flags chips (those are general automation outcomes, not approval state) and continues to gate other Pass #4 surfaces. Approval lifecycle (Pass #5), state-consistency dismissed parity (Pass #6), role-aware copy (Pass #7) all unchanged.

-   **Phase 3 Correction Pass #9 — Approval Modal Role Copy + Mandatory Cancel Note**: Strict, isolated copy + validation pass on the Resolve Review modal. Pass #6/#7/#8 made status visible to the requester, but opening the modal still showed approver-flavoured copy ("Approver Note (Optional)") — even when the requester was cancelling their own outstanding request — and there was no validation requiring a cancellation reason.
    -   **Role-aware modal title** (`src/components/ShippingCenter.tsx`): The modal computes `isApprover = canApproveAutomationExceptions || canOverrideAutomationGuardrails`. For kind=approval / kind=block, the title is `Review Approval` for the approver (action framing) and `Review Request` for the requester (their outstanding submission). For kind=review the title remains `Resolve Review` (no requester-side concept).
    -   **Role-aware detail-panel CTA** (`src/components/ShippingCenter.tsx`): The button on the Pending Approval / Pending Approval (Blocking) panel now reads `Review Approval` for the approver and `Review Request` for the requester so the user is not surprised by the modal heading after clicking.
    -   **Role-aware requester-meta box**: Inside the modal, the sky requester-context block heads with `Approval Request` for the approver (action context — who is asking) and `Your Request` for the requester (confirmation — your own outstanding submission). The submission line reads "Submitted by …" for the requester and "Requested by …" for the approver. The requester's own note is prefixed `Your note:` so a requester confirming a cancel sees their own original note labelled clearly.
    -   **Role-aware note label + placeholder**: The textarea label is `Approver Note (optional)` for the approver and `Request Note (required to dismiss)` for the requester. Placeholder is "What did you check or change?" for the approver and "Why are you cancelling this request?" for the requester. The kind=review path keeps `Resolution Note (optional)`.
    -   **Mandatory note on requester cancel**: When `isRequesterFlow` (kind=approval/block + viewer is not approver), the Dismiss button is relabelled `Cancel Request` and is disabled (with `disabled:cursor-not-allowed` and a tooltip "Enter a Request Note to cancel this approval request.") whenever the note field is empty. A defensive guard inside the click handler also no-ops on empty note. An inline hint sits beneath the textarea — informational ("Required if you cancel this request — recorded in execution history.") until the user attempts to submit empty, at which point it turns red and reads "A request note is required to cancel your approval request." The textarea border also turns rose to draw the eye.
    -   **Approver flow unchanged**: For the approver, the Dismiss button keeps its original label and remains optional-note (consistent with prior behaviour); Approve / Override buttons remain perm-gated by `approve_shipping_automation_exceptions` / `override_shipping_automation_guardrails`.
    -   **Permission safety preserved**: no new action is unlocked. The requester still cannot Approve or Override (those buttons render only when the viewer holds the corresponding perm). The Dismiss action for the requester continues to require `canResolveAutomationReviews || canManageAutomationRules || canEditPreDispatch` (unchanged); the only added gate is the note-empty check, which restricts requesters further (cannot cancel without a reason), it does not loosen anything.
    -   **Execution history records the cancel note truthfully**: `setReviewOutcome` (`src/context/StoreLocalState.tsx`) already passes the trimmed note through to `resolutionNote` on the dismissed `reviewNeeded` and embeds the note text in the audit `internalNote` (`Review dismissed by Current User — <note text> (rule: …)`), which the Execution History panel renders. No engine change needed; the requester's reason now flows into the same audit pipe the approver's note has always used.
    -   **Acceptance**: requester opening the modal sees title "Review Request", heading "Your Request" with their submission echoed back, label "Request Note (required to dismiss)", and a disabled Cancel Request button until they enter a reason. Approver sees title "Review Approval", heading "Approval Request", label "Approver Note (optional)", and Approve / Override / Dismiss as before. Cancel reason is persisted to `reviewNeeded.resolutionNote` and surfaced in the Request Dismissed banner + Execution History audit note. Lifecycle (Pass #5), badge canonical source (Pass #6), role-aware label hint (Pass #7), and visibility ungating (Pass #8) all unchanged.

-   **Phase 3 Pass #10 — Packing Workflows Foundation**: Adds an explicit, auditable packing lifecycle inside the Shipping Center detail modal — distinct from but aligned with the existing `'Packed'` shipment status. Out of scope: SLA, scorecards, customs, insurance, barcode/WMS.
    -   **Types** (`src/types.ts`): Shipment gains `packingStatus` (`'not_started' | 'in_progress' | 'packed' | 'exception' | 'not_required'`), `packingStartedBy/At`, `packedBy/At`, `packingNotes`, and four append-only collections (`packingExceptions`, `packingItemVerifications`, `packingPackageVerifications`, `packingHistory`). New helper interfaces `PackingException`, `PackingItemVerification`, `PackingPackageVerification`, `PackingHistoryEntry` plus the `PackingExceptionType` union (`missing_item | quantity_mismatch | damaged_item | package_weight_missing | package_dimensions_missing | source_item_mismatch | cannot_verify_contents | other`).
    -   **Permissions** (`src/context/accessConfig.ts`): five new sub-perms gated by `packing_workflows` parent — `view_packing_workflows`, `manage_packing_workflows`, `complete_packing`, `resolve_packing_exceptions`, `override_packing_requirements`. Wired into `FEATURE_PERMISSION_DEPENDENCIES.packing_workflows`. Plan availability: Growth + Advanced (not Starter). Role defaults: Owner/Manager have all five (override too); Technician/Sales have view + manage + complete; resolve and override are manager-only.
    -   **Mutators** (`src/context/StoreLocalState.tsx`): atomic mutators each writing the packing field, a `PackingHistoryEntry`, and a `system`-source `internalNote` audit row in the same `setShipments` transaction — `startPacking`, `recordPackingItemVerification`, `recordPackingPackageVerification`, `addPackingException`, `resolvePackingException`, `completePackingForShipment`, `reopenPacking`, `markPackingNotRequired`. `startPacking`, `addPackingException`, and `completePackingForShipment` return result objects so the UI fires observational automation triggers only when state actually transitioned. `completePackingForShipment` accepts a caller-supplied `expectedSourceItems` array and validates every item's `verifiedQty >= expectedQty` at the data layer; the override path requires a non-empty reason at the mutator layer (defense-in-depth).
    -   **UI** (`src/components/ShippingCenter.tsx`): a new `Packing` sub-tab inside the shipment detail modal (between Packages and Logistics) renders a status header, an item/contents verification checklist (extracted from the underlying invoice/repair/transfer/RMA — falls back to a "source contents not available" notice when the source has no item list), a per-package weight + dimensions verification grid, exception create + resolve, completion (clean path or perm-gated override with required reason), reopen, mark-not-required, and an append-only history. A small status chip ("In Progress" / "Exception" / "Not Required") appears on the shipment list row, suppressed when packing is `'not_started'` or when `packingStatus === 'packed' && status === 'Packed'` to avoid duplicating the lifecycle chip. The Packing surface is rendered only when both the plan and the viewer's `view_packing_workflows` perm are satisfied; action buttons are individually gated by `manage_packing_workflows` (verify + create exception), `resolve_packing_exceptions` (resolve), `complete_packing` (clean completion), and `override_packing_requirements` (override completion + reopen + mark-not-required).
    -   **Automation triggers** (`src/shipping/automationEngine.ts`): three new observational triggers — `packing_started`, `packing_completed`, `packing_exception_created` — added to `OBSERVATIONAL_TRIGGERS`, allowed only for the `flag_note` and `require_review` purposes in `PURPOSE_TRIGGER_MATRIX` (no guardrail/blocking actions, since packing is a workflow signal, not a label-purchase prerequisite). Trigger labels are wired into `TRIGGER_LABELS`, `TRIGGER_LABELS_TITLECASE`, and `AutomationRules.tsx`. The fired-state context (`packingStatus`, `packedBy`, etc.) is passed as `extraUpdates` to `triggerAutomation` so condition matching sees the post-mutation state.
    -   **Lifecycle alignment**: `packingStatus` and `Shipment.status` are intentionally distinct — `packingStatus = 'packed'` does not auto-flip the lifecycle status to `'Packed'`; the operator continues to control shipment lifecycle from the Overview tab. The Packing tab surfaces an inline note pointing this out when both prerequisites are met. `markPackingNotRequired` is permission-gated and refuses while open exceptions exist (UI prevents, server-side guard for safety).
    -   **Acceptance**: a manager can start packing, verify each invoice line item and each package's weight + dimensions, create + resolve exceptions, and mark packed when prerequisites are met; a Sales role can verify and create exceptions but cannot resolve, complete, or override; an in-progress shipment shows a "In Progress" chip on the list and a status header on the Packing tab; a manager-marked-not-required shipment surfaces "Not Required" with the reason recorded in history; reopening a packed shipment clears `packedBy/packedAt`, returns to `in_progress` (or `exception` if open exceptions exist), and writes a history entry; the override path on completion requires the override perm and a reason both at the UI and at the mutator. Audit trail (history + internal notes) is append-only and survives every state change. The UI fires `packing_started` / `packing_completed` / `packing_exception_created` observational triggers only when the corresponding mutator actually changed state.

## System Design Choices

-   **Routing**: Utilizes React Router v7.
-   **State Management**: Primarily relies on React Context API and local component state.
-   **Firebase Integration**: Leverages Firestore for backend data persistence and Firebase Authentication for user management.
-   **AI Integration**: Integrates with the Gemini API for AI functionalities.
-   **Server-Side Shipping API**: An Express server on port 5001 handles all shipping provider operations with secure credential storage.
-   **Configuration**: `firebase-applet-config.json` for Firebase project configuration and `firebase-blueprint.json` for Firestore schema definition.

# External Dependencies

-   **Firebase**: Firestore and Authentication.
-   **Google AI Studio / Gemini API**: Integrated via `@google/genai`.
-   **Recharts**: Used for charts and data visualizations.
-   **Framer Motion**: Employed for UI animations and transitions.
-   **EasyPost**: Shipping API for label generation and tracking.
-   **Shippo**: Shipping API for label generation and tracking.
-   **ShipStation**: Shipping API for label generation and tracking.