# Overview

This project is a multi-tenant SaaS frontend platform offering a comprehensive suite of tools for Point-of-Sale (POS), employee and customer relationship management (CRM), inventory control, reporting, and marketing for small to medium-sized businesses. Its core purpose is to streamline operations, provide actionable insights, and automate business processes through a scalable, customizable, and feature-rich architecture.

# User Preferences

I prefer detailed explanations.
I want iterative development.
Ask before making major changes.
Do not make changes to the folder `Z`.
Do not make changes to the file `Y`.

# System Architecture

## Frontend

The frontend is built using React 19, TypeScript, Vite 6, and Tailwind CSS v4.

## UI/UX Decisions

-   **Design System**: Employs rounded cards, a glass effect, a consistent primary color theme, and a `ghost-border` utility. Typography uses `font-black uppercase tracking-widest` for labels.
-   **Animations**: Utilizes Framer Motion for smooth UI transitions and consistent modal patterns.
-   **Notifications**: Implements toast notifications for user feedback.
-   **Accessibility**: Prioritizes semantic accessibility for forms, tables, and modals.

## Technical Implementations & Feature Specifications

-   **Authentication & Access Control**: Implemented with Firebase Authentication and a 7-level hierarchical permission model.
-   **Tenant Management**: Full lifecycle management including provisioning, subscriptions, feature overrides, billing, and audit logging.
-   **Business Management Modules**:
    -   **Point of Sale (POS)**: Cart management, diverse payment options, tax/discount calculations, customer management, repair intake, and robust refund/warranty workflows.
    -   **Employee Management**: Manages employees, roles, time tracking, and payroll with granular permissions.
    -   **CRM (Customers)**: Enhanced CRM with multi-field search, loyalty programs, duplicate detection, and integrated order/invoice history.
    -   **Invoices**: Supports full invoice lifecycle with dynamic line items, searchable catalog, payment processing, and configurable document templates.
    -   **Services**: Manages a service catalog with CRUD operations and warranty configuration.
    -   **Repairs Module**: Manages repair ticket lifecycle, parts tracking, technician assignment, and financial summaries.
    -   **Inventory Management**: Comprehensive system for managing various `StockItem` types, including UPC/manufacturer/location tracking, min/max stock levels, serial number tracking, stock adjustments, inventory transfers, and trade-in management, all integrated with RBAC.
    -   **Supply Chain Management**: Manages suppliers, purchase orders, Goods Received Notes (GRN), and Return Merchandise Authorization (RMA) with tracking and financial recording, gated by RBAC.
    -   **Shipping Center**: Centralized fulfillment module for managing shipments across all source documents, supporting various types, status transitions, tracking, carrier configuration, cost tracking, and a provider adapter layer.
    -   **Returns Portal / Reverse Logistics**: Manages returns with a structured lifecycle, reasons, resolutions, item disposition, intake/inspection, and integration with return shipments.
-   **Reporting**: Features a dashboard with data visualizations.
-   **Onboarding**: A multi-step process for new tenants covering plan selection, pre-configured settings, and an activation checklist.
-   **Document Template Editor**: A structured editor for 5 document types, offering deterministic HTML generation, branding, and visual preview.

## System Design Choices

-   **Routing**: Utilizes React Router v7.
-   **State Management**: Primarily relies on React Context API and local component state.
-   **Firebase Integration**: Leverages Firestore for backend data persistence and Firebase Authentication for user management.
-   **AI Integration**: Integrates with the Gemini API for AI functionalities.
-   **Server-Side Shipping API**: An Express server handles all shipping provider operations with secure credential storage.
-   **Commercial Controls**: Implements a system for managing add-ons and feature entitlements with governance, tenant overrides, pricing rules, and a detailed audit trail. This includes an internal invoice workflow and activation modes for feature grants.
-   **Add-on Governance**: Features three governance states (`active`, `disabled`, `archived`) and a separate PM `lifecycle`. Add-ons can be linked to existing features or registered as standalone capabilities. Archive and delete operations are blocked by dependencies.
-   **Add-on Implementation Readiness**: Each add-on has a `readinessStatus` (`runtime_backed`, `partially_backed`, `parent_feature_linked`, `implementation_required`, `commercial_placeholder`) and a runtime backing checklist. This gates tenant grants and provides an implementation brief generator for non-`runtime_backed` add-ons.
-   **Production Logging & PII Redaction**: Server-side logs are restricted to operational metadata only, with sensitive customer and credential data strictly redacted using `server/safe-log.ts` and `sanitizeError(err)`.
-   **Platform Operations & Security**: A suite of System Owner surfaces (Audit & Security, Support Tools, Platform Settings, Domains, Team Management) for governance and operational control. These features include mock data for local development, a platform audit helper, and truth labels indicating manual verification where automated systems are not yet implemented.
-   **Command Center**: A central dashboard providing a platform health overview, needs attention queue, tenant risk summary, and workflow health summary, with live updates.
-   **Audit & Security Upgrade**: Includes saved views, CSV export, high-risk flags, linked case badges, and detailed drawers for event analysis, related events, actor profiles, and the ability to create support cases from audit events.
-   **Support Tools Upgrade**: Features SLA timers, saved views, escalation mechanisms, tenant health mini-cards, a note composer with macro inserter, and related entities panels.
-   **Risk Scoring (`deriveTenantRisk`)**: Calculates a per-tenant risk score based on open critical audits, escalated cases, overdue/at-risk SLA cases, and domain verification statuses.
-   **SLA Timer Logic (`deriveSlaStatus`)**: Determines the status of support cases (overdue, at_risk, paused, met) based on predefined deadlines and case status.
-   **High-Risk Audit Flag Logic (`deriveHighRiskFlag`)**: Identifies critical, sensitive, or burst audit events.
-   **Command Center — Escalated Cases in Needs Attention**: Active escalated support cases (`escalated === true` and status not in `resolved`/`closed`) are included in the Command Center "Needs Attention" queue with a deep link to `/owner/support-tools?caseId=<id>`. Resolved or closed cases are excluded. The same case is never queued more than once when it is also urgent or overdue. Command Center additionally listens for a `support_cases:changed` window event (dispatched by Support Tools whenever cases are persisted) so escalations reflect live without a full app reload.
-   **Phase 1.1 QA Patch**: Additive bug-fix on top of the locked Phase 1.1. Fixes the Support Tools case-detail crash (drawer was reading the non-existent `tenantRiskForSelected.reasons` instead of `signals`, and wrong `TenantDomainRecord` field names); wires `?caseId=` deep links from Command Center "Needs Attention" rows and Audit & Security "linked case" badges so they open the matching case drawer (with an in-app "Case not found" empty-state for stale ids); adds `data-testid` attributes on saved-view chips, support-case rows, and attention rows for e2e coverage; replaces the browser `confirm()` for security-note delete with an in-app confirmation modal; and verifies that every support-case mutation writes exactly one audit event (no duplicates) and that the `security_note_deleted` audit fires only on confirm. No public type, audit action id, route, AccessGuard rule, `feature` key, or Domains/Settings/Team/Shipping/Billing/PII surface was changed; the patch only edits `SupportToolsPage.tsx`, `CommandCenterPage.tsx`, and `AuditSecurityPage.tsx`.
-   **Phase 1.1.2 — Competitive Ops Console Maturity**: Strictly additive on top of locked Phase 1.1 + 1.1.1; no real DNS/SSL/SSO/AI/uptime/notifications were added and only meaningful operator actions remain audited. New shared helpers in `platformOpsDerive.ts` only (no signature changes to existing exports): `PHASE_112_TRUTH_LABEL` (single source of truth for the cross-surface "All values are derived from in-session signals — no live infrastructure or AI." line), `countCasesForSupportView(view, cases, now?)` mirroring `predefinedSupportViews` predicates so saved-view chips can render queue counts, a `WidgetId` string-literal union + `WIDGET_META` record (title / one-line helper / truthful empty message / optional `navigateTo` deep link) covering all 8 Operational Widgets, a `bucketNavigateTo(widgetId, bucketKey)` helper that emits per-bucket deep links (severity/SLA/risk/audit-severity/domain-status), an `NbaTier` union ('p1'|'p2'|'p3') with `NBA_TIER_LABEL`/`NBA_TIER_STYLES`/`tierForPriority` deterministically mapping critical→p1, high→p2, medium/low→p3 (existing `NextBestAction.priority` shape unchanged). Mission Control hero: a calm "what needs attention now" rollup line ("Open · Overdue · Escalated · High-risk audits · Domain issues") computed from the existing focused\* slices so it respects focus mode, plus the Phase 1.1.2 truth label moved into a footer ribbon inside the hero (`data-testid="mission-control-truth"`) so it is always visible. Operational Widgets: every distribution + list widget now renders `WIDGET_META[id].helper` as a single-line subtitle, `meta.emptyMessage` as a truthful empty-state when its bucket array is empty, an "Open →" header link when `meta.navigateTo` is defined, and each distribution bucket is wrapped as a `Link` to its filtered surface (e.g. cases-by-severity buckets deep-link to `/owner/support-tools?severity=urgent`, audit-by-severity to `/owner/audit-security?severity=critical`, domain-snapshot to `/owner/domains?status=failed`); list-widget rows were already Links and remain unchanged. Next Best Actions: each row now shows a left-side priority tier pill (`P1`/`P2`/`P3`) with deterministic colors via `NBA_TIER_STYLES`, the section header gains a "Rule-based · not AI" caption, and the empty state explains "No actions surface in <focus> mode" when not Normal. Tenant 360 drawer: existing summary band (name + plan + status + derived risk with reasons) and sub-cards (Open Cases with SLA + escalation badges, SLA Pressure, Recent Audit Events with high-risk flag, Domain Issues) preserved; a new "Quick Links" sub-card was added at the bottom with two deep links — Audit (this tenant) → `/owner/audit-security?tenant=<id>` and Support (this tenant) → `/owner/support-tools?tenant=<id>` — above the existing "Open full tenant page" CTA, and the drawer footer now uses the shared `PHASE_112_TRUTH_LABEL`. Audit & Security drawer: Detail tab gains a top "actor context" block (avatar circle with initials + actor name + recent-actions micro-count from the existing `actorProfile` derivation + a "Profile →" jump to the Actor tab; `data-testid="audit-drawer-actor-context"`); the Related tab now visually groups events under Today / Yesterday / Earlier headers (`data-testid="audit-related-group-<label>"`); existing linked-case chip and CSV export untouched. Support Tools: saved-view chips now render a count badge via `countCasesForSupportView` (`data-testid="support-saved-view-<id>-count"`); the case detail header summary band (`data-testid="support-case-header"`) now surfaces a tenant-risk pill alongside the existing SLA + Escalated + From audit pills (`data-testid="support-case-header-risk"`); `NotesTimeline` now groups notes under Today / Yesterday / explicit-date headings and renders a small material icon per `kind` (status_change → sync, escalation → priority_high, resolution → task_alt, internal → sticky_note_2, reply → reply); macro composer + escalation banner unchanged. Cross-surface: every page imports its labels/styles from `platformOpsDerive` (no new local color maps); the two pre-existing `SEVERITY_STYLES` constants in AuditSecurityPage and SupportToolsPage are kept because they describe different domains (audit normalized severity vs support-case severity) and were already locked in earlier phases. No new audit action ids were registered (count remains the same as Phase 1.1.1) — UI maturity changes do not log on hover, scroll, filter changes, or pure navigation; only meaningful operator actions continue to audit. No public type, route, AccessGuard rule, `feature` key, or Domains/Settings/Team/Shipping/Billing/PII surface changed. Touched files: `platformOpsDerive.ts`, `CommandCenterPage.tsx`, `AuditSecurityPage.tsx`, `SupportToolsPage.tsx`, `replit.md`. `platformOpsAudit.ts` was inspected and intentionally left untouched (no new audit action ids needed for this maturity pass).
-   **Phase 1.1.2 Benchmark Patterns Considered**: Direction was informed from prior knowledge of four named SaaS reference points — Datadog (mission-control hero with state banner + signal rollup), PagerDuty (incident review focus modes + tier-pill priorities for next actions), Zendesk (saved-view queue counts + SLA pill emphasis on case rows), and Atlassian Jira Service Management (Tenant 360-style customer drawer with summary band, sub-cards and quick-links). No live external browsing was performed during this build; the benchmark direction is documented from prior knowledge and the implementation deliberately stays within the existing in-session derivation model (no real DNS/SSL/SSO/AI/uptime/notifications introduced).
-   **Phase 1.1.1 UX Correction Pass**: Strictly additive fix-ups on top of Phase 1.1.1 to address 5 QA gaps without adding any real DNS/SSL/SSO/AI/uptime. (1) Mission Control hero strengthened: state-tinted gradient panel with state-icon, large state label, "what changed" summary line, time-range/focus-mode chips, refresh button (audited), and an Active Focus Mode banner ribbon under the chips when not Normal. (2) Focus mode now visibly changes content: new helpers `applyFocusModeToCases` / `applyFocusModeToAudits` / `applyFocusModeToDomains` in `platformOpsDerive.ts` produce focused snapshots that feed `deriveOperationalPulse`, `deriveWidgetDistributions`, the High-Risk Stream, the Escalated widget, the Tenant Risk widget, and the Needs Attention queue, so Watch and Incident modes alter pulse counts, widget bars and the queue. (3) Operational Pulse items are now real buttons (hover/active/disabled states, ARIA pressed, action hints): a `PULSE_ROUTING` map either applies a `filtersAttention` predicate that narrows the Needs Attention queue (with a sticky "Filter: <label> · Clear" chip) or navigates to a deep link; clicking fires the new `command_center_pulse_filter_applied` audit action only on a meaningful change. (4) Audit & Security saved-view chip row replaced with an Operational Lens card grid (icon + label + per-view count badge computed by an inline `countForView` helper against `allRows`) and an "Audit Command Bar" card sits above the table summarizing visible/total rows, active lens, severity/category/tenant/search chips, a prominent High-Risk Only toggle button, and CSV export — preserving every existing `data-testid="audit-saved-view-${id}"`. (5) Support Tools polish made visible: SLA pill in case rows widened with state-colored vertical bar and microcopy line; macro picker is now a 2-step UX (select → preview block with body excerpt → "Insert Template" button, audit fires only on actual insert); drawer escalation banner widened with icon, reason, escalated-by/at, and an inline De-escalate button; Related Entities grouped into 3 cards (Source Audit Event / Recent Tenant Audits / Tenant Domains) each with a colored band and count badge. No public type, audit action id (besides the additive `command_center_pulse_filter_applied`), route, AccessGuard rule, `feature` key, or unrelated surface (Domains, Platform Settings, Team Management, Shipping, Add-on Governance, Plans, Tenant Features, Store Permissions, POS, PII, Billing) was changed; only `platformOpsDerive.ts`, `platformOpsAudit.ts`, `CommandCenterPage.tsx`, `AuditSecurityPage.tsx`, and `SupportToolsPage.tsx` were edited.
-   **Phase 1.1.1 — Interactive Mission Control & Operational Polish**: Strictly additive on top of locked Phase 1.1. Replaces the basic Command Center header with a Mission Control panel that shows an overall platform state (`deriveOverallPlatformState` → Healthy/Watch/At Risk/Critical), a one-line summary, active signal list, last-refreshed badge with refresh button, time-range chips (Today/7d/30d/All) and focus-mode chips (Normal/Watch/Incident Review). Adds a 7-metric Operational Pulse strip (`deriveOperationalPulse`: open cases, overdue SLA, escalated, high-risk audits, domain issues, tenants at risk, pending actions) and an 8-widget operational grid: 5 distribution widgets (Cases by Severity, SLA Pressure, Tenant Risk Distribution, Audit by Severity, Domain Snapshot) computed by `deriveWidgetDistributions`, plus 3 list widgets (High-Risk Stream, Escalated Cases, Add-on/Commercial Attention). Adds a Next Best Actions panel powered by `deriveNextBestActions` (deterministic rule-based — explicitly NOT AI: ranks overdue urgent cases, escalated cases, critical/high-risk audits, failed domains, SLA-at-risk cases, at-risk/critical tenants, and unresolved security notes by fixed priority order, each with an in-app deep-link). Adds a Tenant 360 read-only drawer (`deriveTenant360`) opened by clicking a tenant name in either the Needs Attention table or the Tenant Risk Summary; the drawer shows derived risk, open cases with SLA + escalation badges, recent audit events with high-risk flags, domain issues, and a link to the full tenant page. Time-range filtering uses a shared `filterByTimeRange` helper applied to audits and cases (domains/notes are point-in-time and unfiltered). Focus mode uses `filterByFocusMode` applied to the Needs Attention queue (Normal=all, Watch=non-low, Incident=critical+high). Audit & Security gains an informational Severity Summary Strip (info/notice/warning/critical bars + counts; does not mutate filters) and stronger active saved-view chip styling. Support Tools gains a top-of-drawer red Escalated banner, matching saved-view chip emphasis, and native browser tooltips that preview macro bodies on hover before insert. Four new audit action ids are registered in `platformOpsAudit.ts` and fire only on meaningful operator actions: `command_center_refreshed`, `command_center_time_range_changed`, `command_center_focus_mode_changed`, `command_center_tenant360_opened` (no spam on hover, scroll, or filter changes). All values are derived from in-session `support_cases_v1` / `audit_logs` / `tenant_domains_v1` / `platform_security_notes` snapshots — there is no real DNS/SSL/SSO/notifications/AI/uptime monitoring and no external network call. Existing surfaces (Domains, Platform Settings, Team Management, Shipping, Add-on Governance, Plans, Tenant Features, Store Permissions, POS, PII rules) and existing public types/routes/AccessGuard rules/`feature` keys are unchanged. The benchmark direction (Datadog status banners, PagerDuty incident review modes, Zendesk SLA emphasis, Atlassian Jira Service Management Tenant 360 drawers) is documented from prior knowledge — no live external browsing was performed during this build.

-   **Phase 1.1.3A Correction — Dev Session Role + Global Permissions Matrix**: Strictly additive on top of Phase 1.1.3A. Removes the local "Acting as" selector from Support Tools — the active platform role is now sourced exclusively from the Dev Session / `useAccess().session.role`, and the operator display name comes from `session.user.name`; the page exposes a read-only "Active platform role" badge instead. The advisory `can()` matrix in `platformOpsDerive.ts` is preserved unchanged for the existing escalation lifecycle gates and is fed via a small `PLATFORM_ROLE_TO_OPS_ROLE` map (system_owner→platform_owner, operations_admin→platform_admin, support_admin→platform_lead, security_admin→platform_security, billing_admin→platform_billing, tenant roles→platform_readonly). New module `src/owner/platformPermissionsConfig.ts` defines an 11-feature platform catalog (Command Center, Audit & Security, Support Tools, Tenant Management, Billing & Subscriptions, Platform Settings, Domains, Team Management, Provisioning, Feature Matrix / Plans, Commercial Controls / Add-on Governance) and ships per-feature sub-permissions (id / label / description / threshold / `sensitive?` flag) covering the actions wired in this pass (e.g. `view_support_tools`, `create_support_case`, `escalate_assigned_case`, `escalate_any_case`, `change_escalation_level`, `resolve_escalation`, `close_with_active_escalation`, `export_audit_csv`, `add_security_note`, `delete_security_note`, `create_support_case_from_audit`, `view_command_center`, `view_tenant_360`, `use_command_quick_actions`, `act_on_nba_recommendations`, etc.). The 7-level hierarchy is reused unchanged (None / View Only / Create / Edit / Approve / Manage / Full Access) but a separate `platformPermissionMeets(actual, threshold)` is exposed for the spec semantics — Approve / Manage / Full Access all satisfy Approve threshold; Manage / Full Access satisfy Manage threshold; Full Access satisfies all — without altering the existing `meetsPermissionLevel()` ordering used by the rest of the app. `DEFAULT_PLATFORM_FEATURE_LEVELS` encodes the per-role defaults (System Owner=Full across all 11; Support Admin=full for Support Tools / manage for Command Center; Billing Admin=full for Billing / manage for Feature Matrix + Add-on Governance; Operations Admin=full for Tenant Mgmt / Provisioning / Domains; Security Admin=full for Audit & Security / approve for Support Tools); tenant-side roles default to all `none`. Resolution rule: explicit sub-permission level wins, otherwise fall through to the parent feature level. Overrides persist to `sessionStorage.platform_permissions_v1` as `{ [role]: { features: {...}, subs: {...} } }` and emit a custom `platform_permissions:changed` window event for live cross-page sync; System Owner is locked at Full Access and is intentionally not editable. The Global Permissions Matrix in Team Management is rewritten on this storage: 11 expandable feature rows, per-role columns for the 5 hardcoded platform roles only (custom roles created via "Create Role" remain on the Roles tab), each row showing the parent dropdown plus an "X / N enabled" sub-permission counter, sub-rows revealed by a chevron expander with their own 7-level dropdowns and per-row enabled/disabled badges; sensitive sub-permissions are tagged with a "sensitive" pill and their thresholds are surfaced inline. A truthful-limitations banner is shown above the matrix ("Permissions are UI-enforced only — no server-side RBAC, PIM, or PAM in this phase. Server enforcement planned for Phase 1.3."). Audit emission is preserved and extended: parent edits emit `platform_permission_changed`, sub-permission edits emit the new `platform_sub_permission_changed` action (added to `PlatformAuditAction`). Permission enforcement is wired in three places using `hasPlatformPermission(role, subKey)`: Support Tools (page header now shows the active platform role + a gated "+ New Case" button on `create_support_case`; existing escalation lifecycle gates continue to use `can()` against the mapped `PlatformOpsRole`), Audit & Security ("Active platform role:" line under the page title; `Export CSV` gated by `export_audit_csv` Approve+, `Add Note` gated by `add_security_note` Create+, per-note `Delete` gated by `delete_security_note` Approve+, `Create Support Case from this Event` gated by both `create_support_case_from_audit` Create+ and `create_support_case` Create+), and Command Center ("Active platform role:" line under the truth label; Quick Action buttons gated by `use_command_quick_actions` plus the destination feature's view permission; Tenant 360 click-throughs in Needs Attention and Tenant Risk Summary gated by `view_tenant_360`; pulse cells stay visible at the View level so users with read-only access still see counts). Disabled buttons keep the same layout and surface a tooltip explaining the missing permission. Non-regression scope: no public type renames, no route changes, no AccessGuard rule edits, no `feature` key changes, no Store Permissions Matrix edits, no tenant-facing UI changes, no PII/log changes; Store Permissions Matrix and tenant employee gating remain governed independently by `accessConfig.ts`. Future direction: server-side RBAC + audit-log signature verification + optional PIM/PAM are scoped for Phase 1.3.

-   **Phase 1.1.3A — Operating Model + Permission-Aware Escalation**: Strictly additive on locked Phase 1.1 / 1.1.1 / 1.1.2. Introduces an *advisory* operating model on top of the existing in-session support / audit data — no real RBAC enforcement, no real notifications, on-call schedules, PagerDuty/Slack/SMS/email integrations, and no AI. `platformOpsDerive.ts` adds a `PlatformOpsRole` union (system_owner / support_admin / billing_admin / operations_admin / security_audit_admin) with `PLATFORM_OPS_ROLE_LABEL`, structured `EscalationStatus` (none/escalated/acknowledged/in_review/deescalated/resolved), `EscalationLevel` (l1/l2/l3/critical), `EscalationReasonCode`, and `EscalationTargetTeam` unions with matching label and Tailwind style maps, plus `effectiveEscalationStatus()` that derives a status from the legacy `escalated` boolean for back-compat. A pure `can(role, action, ctx)` helper returns `{ allowed, reason }` for every escalation lifecycle action against a fixed `CAN_MATRIX`; it is used purely as a UX guard (disable + tooltip) and never as a real authorization boundary. `SupportViewFilters` is extended with an `escalation` token (`active | unacknowledged | overdue_ack | critical | unassigned | deescalated | resolved | mine | team`); `predefinedSupportViews` gains 9 new escalation queues; `countCasesForSupportView` honors the new token (with `mine` / `team` resolved against an in-app advisory operator/team context). `mockData.ts` adds optional escalation fields to `SupportCaseRecord` (`escalationStatus`, `escalationLevel`, `escalationReason`, `escalationOwner`, `escalationTeam`, `escalationAcknowledgedAt`, `acknowledgementDueAt`, `escalationHistory[]`, `escalatedByRole`) and seeds 2-3 cases at different lifecycle stages so saved views and Command Center signals have representative data. Support Tools gains a sticky "Acting as: <role>" selector (localStorage), a structured Escalate modal (reason code + note + level + target team + owner + ack-due time), and Assign/Reassign, Acknowledge, Change Level, Resolve Escalation, De-escalate (with reason), and Close-with-active-escalation-warning modals. Each case row shows escalation status / level pills, owner-or-team chip, and ack state; the case drawer renders a full Escalation Card with role-permission explanation and `can()`-gated buttons. Every transition emits exactly one audit event, one timeline note (`kind='escalation'`), and one `escalationHistory` entry, while keeping the legacy `escalated` boolean in sync. `platformOpsAudit.ts` registers 7 new action ids — `support_case_escalation_assigned`, `support_case_escalation_reassigned`, `support_case_escalation_acknowledged`, `support_case_escalation_level_changed`, `support_case_escalation_resolved`, `support_case_close_with_active_escalation_warning`, `support_case_assignment_changed` — with default severities. Command Center's Operational Pulse adds 3 cells (Unacknowledged Escalations / Overdue Ack / Unassigned Escalations) routed via `PULSE_ROUTING`, the Needs Attention queue gains 5 escalation item types (overdue ack, unacknowledged, critical level, no owner, SLA-breached) de-duped against `alreadyQueuedCaseIds`, and `deriveNextBestActions` appends 6 escalation-lifecycle NBAs (ack overdue, unack, no owner, critical level, SLA-breached, acked-stalled-48h). Audit & Security adds an "Escalation Lifecycle" saved view (matched via a new `actionPrefix` filter on `support_case_escalat`) and renders a structured before/after card in the audit detail drawer for any `support_case_escalat*` event. Visible truth labels (`PHASE_113A_TRUTH_LABEL`, `PHASE_113A_PERMISSION_LABEL`) are exposed in Mission Control and Support Tools so the demo / advisory nature of the model is unambiguous. Touched files: `platformOpsDerive.ts`, `platformOpsAudit.ts`, `mockData.ts`, `SupportToolsPage.tsx`, `CommandCenterPage.tsx`, `AuditSecurityPage.tsx`, `replit.md`. Next planned phases will layer real RBAC / on-call / notifications / SLA policy on top of this advisory operating model — none of those are present in 1.1.3A.

# External Dependencies

-   **Firebase**: Firestore and Authentication.
-   **Google AI Studio / Gemini API**: Integrated via `@google/genai`.
-   **Recharts**: Used for charts and data visualizations.
-   **Framer Motion**: Employed for UI animations and transitions.
-   **EasyPost**: Shipping API for label generation and tracking.
-   **Shippo**: Shipping API for label generation and tracking.
-   **ShipStation**: Shipping API for label generation and tracking.